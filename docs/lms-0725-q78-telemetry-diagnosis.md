# LMS-0725 / 0.1.547 — Q78 telemetry deadline diagnosis

**DIAGNOSIS COMPLETE; STOP FOR REVIEW. NOT PRODUCTION ACCEPTED.** No Q78 retry, backfill, SQL execution, deployment, application correction, OpenAI call, remaining-case execution or View-As parity work.

1. **Q78 identity/result.** Exact benchmark question: **When does registration open for Weekday?** Normal signed-in `/api/ask-lwr`, not View-As or Live LMS. Document policy-date intent, league-date object, Weekday scope (classification supported by code and the observed official-date answer; Q78's missing diagnostic payload cannot itself prove those fields). Correct answer: Weekday registration opens Monday, September 7, 2026. One official Weekday Important Dates source; log confirms one evidence item and result shape `answer`. HTTP 200. Telemetry event `capture_failed`, stage `persistence`, operation `capture_ai_quality`, reason `DEADLINE`. Correlation ID `77848361-efa8-400c-be6b-dbb8648039b2`.

2. **Exact path.** `POST` authorizes the normal user, then awaits `observeQualityRequest`. Its `run` completes the safe generated answer and source validation. The observer freezes the completion timestamp and invokes `persistQuality` with a payload-building closure. `qualityOutcome` allowlists scalar metadata and sanitizes `diagnostic_snapshot` through `safePolicyDiagnostic`: bounded enums, UUID references and counts, at most eight candidate references. For a successful `answer`, `qualityException` returns null occurrence and route. RPC arguments are `{p_outcome, p_occurrence:null, p_route:null, p_feedback_id:null}`. `persistQuality` awaits `supabase.rpc('capture_ai_quality', args).abortSignal(controller.signal)` in a race with its timer. Capture failure returns false, the observer returns the original execution, and the route returns the successful answer. The source/answer text is not copied into this success telemetry payload; exception/feedback text sanitization is not invoked for Q78.

3. **Meaning of DEADLINE.** In `aiQualityCapture.js`, the timer callback calls `controller.abort()` and rejects with `Error('quality_timeout')`. `captureDiagnostic` maps that exact error message to `DEADLINE`. The timeout branch won the observed race; a generic abort/RPC error would map differently. This is distinct from `DATABASE_ERROR` with an allowlisted SQLSTATE. Q78 has SQLSTATE null. The installed PostgREST client passes this signal to fetch, including response processing, so aborting the client does not establish server rollback. No database timeout/error response was captured.

4. **Configured limits.** `QUALITY_CAPTURE_BUDGET_MS=500` at `app/lib/aiQualityCapture.js:5`; the normal observer supplies no override. Timer begins before synchronous payload construction, not at the database. The saved RPC definition at `supabase-ai-assistant-lms-0716-stage7a.sql:126` declares `statement_timeout='450ms'` and `lock_timeout='150ms'`. Those declarations are not measured durations and must not be added together as a 600 ms budget. Their current effective server behavior was not inspected by SQL in this diagnosis. Prior Q55 diagnosis recorded a matching production function body; no SQL deployment occurred since. A function-level setting is not proof that a particular request was terminated by it.

5. **502 ms breakdown.** This is rounded `performance.now() - started` for the entire build-and-persist operation. It exceeds the configured application timer by approximately 2 ms, consistent with scheduling/rounding; that difference is not proof of two milliseconds of database overrun. The final log time is 19:19:05.202 UTC, placing the operation start approximately at 19:19:04.700 UTC. The operation marker changed to `capture_ai_quality`, proving payload construction returned and the RPC path was reached. No build, serialization, connection, network, server-execution, lock-wait or acknowledgement sub-timings were retained. Therefore the exact substage consuming the 502 ms cannot be identified. Generation happened outside this timer. The answer is fail-open, but **not fully asynchronous**: HTTP delivery currently awaits capture success or failure and can incur this bounded delay.

6. **Commit classification: A — NOT PERSISTED at the verification checkpoint.** Read-only REST checks at **19:27:33.576 UTC**, over eight minutes after failure, found zero matching rows in `ai_request_outcomes` by ID, `ai_review_occurrences` and `ai_answer_feedback_events` by answer ID, and `ai_manager_review_events` by operation ID. All checks returned HTTP 200 and exact zero counts. Earlier post-completion reads also found no outcome. Thus there is no observed late commit or missed acknowledgement of a persisted Q78 outcome. This certifies durable absence at the checkpoint, not the original transaction's unseen rollback/cancellation mechanism or an absolute assertion about an unobserved in-flight request. Reconcile again before any future historical write; none is authorized here.

7. **Duplicate/idempotency status.** No Q78 duplicate or related event exists at the checkpoint. The saved RPC obtains a per-answer transaction advisory lock, looks up the primary-key outcome, and accepts an identical existing outcome after excluding only `recorded_at` from comparison; changed payload raises `quality_outcome_mismatch`. A successful answer has null occurrence and returns before grouping/review/feedback work. For exception/feedback calls, the function checks the existing occurrence by answer ID and avoids recreating it; downstream events depend on new occurrence/case state. Prior accepted isolated tests exercised identical replay. This is an existing database idempotency primitive, not an implemented application retry: `persistQuality` makes one attempt. POST RPC is not a retryable method in the installed client's normal automatic retry path; aborted requests are not retried. Reissuing the Ask question creates a new UUID and model call, so it is not a persistence retry.

8. **Successful-event comparison.** The following are all normal document-policy answers, one evidence/source, the same RPC and outcome-only argument shape. Persisted diagnostics have ten top-level keys and eight candidate references (one selected). Bytes below are compact UTF-8 serialization of the saved diagnostic object, **not original full HTTP payload size**.

   | Case | Diagnostic bytes | Capture duration |
   |---|---:|---:|
   | Q57 | 1,688 | 450 ms |
   | Q71 | 1,691 | 49 ms |
   | Q75 | 1,691 | 39 ms |
   | Q76 | 1,691 | 126 ms |
   | Q77 | 1,691 | 417 ms |
   | Q78 | Not retained | 502 ms, DEADLINE |

   Q78 has the same success-path schema and one-item count, with null occurrence/route/feedback. Its exact diagnostic reference count, metadata contents, full payload bytes and serialized size were not retained and cannot honestly be reconstructed as historical evidence. The normal builder would use the same bounded fields, but that is a code inference. There is no evidence of a structural anomaly or larger payload causing Q78. Q56 succeeded with three sources in 36 ms; Q55/locked/change with four sources took 107/27/34 ms. Source count alone does not explain the slow tail. A successful answer does not run the heavier occurrence grouping/review branch.

9. **Historical Q23/Q36.** Those also delivered correct normal document answers but logged capture failure; their exact error reason, correlation ID, duration and original bytes were not retained. They had four/three visible sources. The same 500 ms application boundary makes a deadline failure a plausible common class, but **Q78 does not prove either historical cause**. Historical failures remain preserved; no retry, rewrite or backfill.

10. **Latency/boundary assessment.** Existing hosting logs were deduplicated by correlation ID, yielding all 21 successful captures from this continuation: **min 27 ms, p50 52 ms, p95 417 ms, max 450 ms** (nearest-rank percentiles). Q78 is the single deadline failure among 22 submissions, 4.5% of this small, intentionally selected acceptance sample; this is not an organic production error-rate estimate. Four immediately preceding successes have p50 49 ms, p95/max 417 ms. Failed latency is censored at 502 ms; its eventual uncapped duration is unknown. Two successes at 417 and 450 ms show limited 500 ms headroom, so this is not a sample where every other request is uniformly far below the boundary. No sufficient long-run distribution exists here to select a new timeout scientifically. Saved `recorded_at` is assigned using SQL `now()`; it is not a measured commit/finish timestamp. Comparing it to application clocks cannot isolate database execution. Connection acquisition, request transit and server load remain unmeasured. No query plan, expensive operation or lock contention was proven; do not optimize indexes or change database budgets on this evidence alone.

11. **Narrowest proven cause.** The outcome-only persistence operation failed to deliver a successful acknowledgement before its application-wide 500 ms timer; the timer aborted the client wait and the logger reported 502 ms. No durable Q78 telemetry row was found afterwards. The underlying delay's location is unproven. This is a proven **application capture-deadline failure with observed missing persistence**, not a proven database timeout, payload rejection, network outage or connection-pool issue.

12. **Smallest safe correction proposal, not implemented.** First close the demonstrated application reliability/diagnostic gap: construct and freeze the sanitized payload once; record separate build and RPC-wait timings plus byte/key/reference counts; on a deadline, perform a bounded, ID-based reconciliation read and report `persisted`, `absent_at_check`, `unknown` or `mismatch` explicitly. A matching existing row must be treated as acknowledged without another write. This is an application-only candidate using existing tables/RPC and adds no generation. For a recoverable absent/unknown case, evaluate **at most one identical-payload persistence retry** under one explicit total budget and the existing per-ID lock/idempotency contract, using isolated delayed-commit/acknowledgement tests before approval. Never retry a payload mismatch/validation/authorization failure. That retry strategy remains conditional; neither its deadline nor reliability is certified by current logs. Merely raising 500 ms does not reconcile commits or prevent loss; unmanaged fire-and-forget can lose work when the invocation ends; a durable queue/outbox is a larger design requiring separate review. No exact transport/performance fix can yet be claimed proven. If a stronger durable-delivery guarantee is required, choose and review that separately rather than claiming bounded retries guarantee delivery.

13. **SQL boundary.** No SQL was executed. The proposed payload freezing, phase diagnostics and reconciliation can be application-only. Existing RPC already supplies the candidate replay contract. No schema/RPC/index/timeout migration is currently justified or proposed. If later validation shows that durable queuing or an RPC change is required, stop and return its exact proposed migration before production mutation. Reading saved SQL source in this diagnosis was not executing it.

14. **Retry implications.** Preserve the original UUID, request/completion timestamps, sanitized JSON arguments and any existing feedback ID across persistence attempts. Do not rerun the answer pipeline, regenerate sources or rebuild timestamps for a retry. Use a bounded total lifetime, a fresh request AbortController for each permitted operation, and explicit unknown-commit handling. A timeout is not a negative commit acknowledgement. Concurrent/late completion must converge to one outcome and, where applicable, one occurrence/feedback observation/review event; payload mismatch must remain an error. Historical Q78's full original payload is unavailable, so it must not be invented for replay. A separately approved fresh acceptance question is a new observation, leaving this historical failure intact.

15. **Privacy/security.** Read-only data access was limited to safe correlation IDs, counts, timestamps and bounded diagnostic references from telemetry. No credentials, authentication tokens, protected Live values, member data or prompt content were exposed. Source references stayed bounded. Normal origin is explicit; no View-As session, authorization change, server mutation bypass or production setting change. Governing cost policy and GPT-5.5 configuration remain intact. Diagnosis added zero OpenAI traffic; prior cost figures are unchanged.

16. **Exact local validation plan after correction approval.** Use fake RPC/REST adapters, a fake clock and existing sanitized response shapes; fail any unexpected network/model call. Cover immediate success; slow build versus slow RPC; acknowledgement just before/after the budget; abort before send; abort with late commit; committed row found by reconciliation; absent row; reconciliation timeout; row mismatch; at most two identical write attempts if retry is approved; one terminal health result; correct answer returned unchanged under every telemetry failure. Assert fixed UUID/timestamps/payload across replay, no prompt/excerpt/PII in logs, bounded counts/sizes, and normal/View-As separation. Cover exception/feedback duplicate semantics with approved deterministic fixtures; any new SQL-based local replay needs to be explicitly within the correction's approved scope. Run focused telemetry/privacy tests, then required full deterministic tests, lint, typecheck, PDF bundle and build checks. Retain accepted 103/103 routes, 82/82 generated answers and 906/906 historical tests; do not claim they were rerun here. No generated model case is required merely for telemetry logic.

17. **Exact production sequence, only after review and separate correction/deployment approval.** Complete the reviewed local change/checks. Deploy application-only if that remains the approved design. First reconcile historical Q78 by ID read-only without writing it. Run one separately approved fresh Q78 question to certify answer delivery and successful telemetry; retain the historical failed ID. On any answer, capture, reconciliation or duplicate-safety failure, stop. If it passes, continue **Q79, Q80, Q81, Q82, Q83, Q84, Q85, Q86, Q87, Q88, Q89**, checking telemetry and cost attribution as each completes. Do not restart earlier passes or the full generated benchmark. Then finish outstanding production mobile/View-As/integrity gates within their approvals. Only declare production acceptance when all required gates pass. No View-As UI parity or next version starts during this diagnosis.

Evidence: [late-commit/duplicate read](lms-0725-q78-commit-check.json), [deduplicated capture timings](lms-0725-q78-runtime-timings.json), [original failure log](lms-0725-targeted-failure-log.json), [prior targeted acceptance report](lms-0725-targeted-production-stop.md), [governing cost policy](api-cost-policy.md).