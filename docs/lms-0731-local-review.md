# LMS-0731 / 0.1.553 — Draft Save UX correction

**LOCAL READY FOR REVIEW — STOP BEFORE DEPLOYMENT.** No production deployment, SQL migration, production record changes, or OpenAI calls. Owner approval governs this bounded correction.

1. **Version:** LMS-0731 / 0.1.553; current production remains LMS-0730 with its accepted PBCC/help-label corrections preserved.
2. **Root cause:** reported Save Draft requests failed; error was placed above a long form without focus/scroll, and success lacked explicit confirmation. The exact historical HTTP 400 validation reason remains unretained; no guessed field is treated as proven.
3. **Runtime files:** `app/ai-assistant/review/ApprovedAnswersPanel.js`, `approved.module.css`, `page.js`; new `approvedDraftFeedback.js`; `app/lib/aiApprovedAnswersService.js` adds only the linked original question to existing authorized detail reads; version/package files. [Exact baseline comparison](lms-0731-source-comparison.json): seven existing runtime/package files changed, one helper added; 308 baseline files unchanged. Tests: new `test/approvedDraftFeedback.test.mjs`, updated detail fixture in `test/aiApprovedAnswersService.test.mjs`, permanent browser script `scripts/verify-approved-draft-ux.cjs`.
4. **Failed save:** message under Save Draft, role=alert, programmatic keyboard focus and scroll into view. Entered values remain. No success on rejected/invalid response. Retry operation identity retained; no re-creation on review-load failure.
5. **Field validation:** existing shared validator runs locally, with unchanged server validation. Useful reasons are prefixed `Draft was not saved.` Opaque HTTP/Bad Request/network/technical failures use the approved actionable fallback. Policy-key syntax guidance added. No relaxed authority/privacy/source checks.
6. **Save confirmation:** only after a successful mutation acknowledgement with valid revision identity: `Draft saved. Status: DRAFT`. Saving still does not embed, activate, retest or resolve a case. Successful save plus failed detail load explicitly says saved but review unavailable; receipt remains available for retry. Listing refresh is separate from mutation success.
7. **Next actions:** Review / Activate Draft opens the existing revision workflow; existing Close creation / dialog Close remain available. List uses the existing query and All/Draft filters; uppercase status presentation is explicit.
8. **Activation:** existing Review activation, confirmation and Activate retained. Success shows Status ACTIVE, with future-effective/expired/current-scope wording. No automatic activation on save. All authority/source/overlap checks unchanged.
9. **Retest:** existing explicit Retest Question prefills the linked original case question when available, falling back to the revision canonical question for unlinked knowledge. It never submits automatically; case resolution stays separate.
10. **Persistence/reopen:** actual component tested against a synthetic disk-backed HTTP fixture. Response follows fixture persistence. Leave/reopen restores the saved content and DRAFT row; View → review → explicit activation becomes ACTIVE. This is browser transport/persistence evidence; existing database/service tests separately retain real SQL contract coverage. No production draft was created or activated.
11. **Duplicates:** synchronous pending ref plus disabled form/save controls prevents rapid resubmission; existing operation UUID reused on retry. Browser asserts one save request and one logical revision from double click.
12. **Accessibility/navigation:** error focus, keyboard Enter submit and visible message pass at 1280/390/320. Explicit textarea names preserve stable labels when reopened with content. Scoped unsaved warning covers Close creation, revision Close/Escape, review-tab switches and detectable anchor navigation; beforeunload covers reload/window departure. Browser-history and arbitrary programmatic navigation interception is deferred rather than introducing a navigation framework. Screenshots `lms-0731-save-{error,success}-{1280,390,320}.png` inspected. Existing browser runtime used because agent-browser CLI is not installed.
13. **Retrieval:** deterministic controls verify drafts excluded and eligible active revisions accepted only within existing scope/date/authority rules. Ask LWR routing/retrieval code unchanged. Activation uses fixture responses; zero real embedding/model calls.
14. **SQL:** none required or applied. No grants, RLS, schema, business workflow or role changes.
15. **Validation:** see `lms-0731-{tests,lint,types,pdf,build,browser,diff}.log`. `npm test` **1053/1053 PASS**; lint **0 errors / 11 existing warnings**; `npx tsc --noEmit --incremental false`, PDF server bundle, production build and `git diff --check` PASS. Browser lifecycle/failure/unknown error/duplicate/failed detail reload/unsaved cancellation/keyboard/mobile checks PASS. Initial browser fixture label mismatch and existing service mock were corrected; final runs pass. No tests skipped.
16. **Cost:** zero OpenAI/embedding calls; $0 incremental project model cost. Tests use synthetic local data and mocked providers only.
17. **Normal LMS:** exact comparison preserves PBCC, View-As, normal dashboards, rosters, schedules, matches, scoring, standings, ratings, roles and normal Ask routing. No live acceptance performed for an undeployed candidate.
18. **Production sequence:** stop for review. After explicit authorization, freeze candidate/hash, confirm accepted production baseline, deploy exact app with no SQL/data edits, verify normal LMS first, then read-only Approved Answers opening/status/detail checks. Do not create/activate real knowledge as a smoke test; owner deliberately completes that lifecycle. Keep preceding deployment available for rollback.

## Reproduce browser controls

Run `node scripts/verify-approved-draft-ux.cjs` from `lwrpc-admin` with `PLAYWRIGHT_MODULE` pointing to an installed Playwright runtime (the bundled Codex runtime was used here). The harness binds localhost, rejects nonlocal browser traffic, persists synthetic fixture data in a temporary directory, and removes that fixture on exit. It does not load application environment secrets or call production.
