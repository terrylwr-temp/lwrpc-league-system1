# LMS-0730 / 0.1.552 — LOCAL IMPLEMENTATION COMPLETE

STOP FOR REVIEW. Production remains LMS-0729 / 0.1.551. No production SQL, deployment, business-data mutation or OpenAI call. The approved [design](lms-0730-schedule-captain-parity-design.md) governs this candidate.

1. **Exact runtime files changed:** `lwrpc-admin/app/api/view-as/read/route.js`, `app/captain-dashboard/page.js`, `app/player-dashboard/page.js`, `app/lib/viewAsPageState.js`, `app/lib/version.js`, `package.json`, `package-lock.json`; new `app/lib/scheduleCaptainNames.js`. One new migration listed below. No dependency changes. [Source comparison](lms-0730-source-comparison.json): 255 existing app files unchanged, including TeamScheduleModal and all normal queries after removing only the added overlay call/import.
2. **Root cause confirmed:** the general competition projection omitted other-team leadership IDs and people omitted corresponding name rows. Those broad projections remain unchanged; the new scoped response supplies the missing nested display objects.
3. **Schedule-scoped read:** existing `page_read` boundary accepts `schedule_captains` with exactly `{divisionId}`. It obtains the locked effective viewer through the existing dashboard authorization, validates the normal active-division selector or target-linked initial division, reads only active teams in that division, and revalidates the viewer. No caller-provided team/member/actor/role list. Client overlay intersects with existing schedule team IDs and never appends a team or changes match data. No global members enrichment or persistent name cache.
4. **Returned fields:** each response team has `id`, `captain`, `co_captain_1`, `co_captain_2`; a leadership object contains only `id`, `first_name`, `last_name`, `full_name`, or null. Existing team relationship columns remain authoritative.
5. **Email exclusion:** response SQL never selects email, phone, RF, ratings or profile fields. Overlay reconstructs safe objects, also removing an existing email fallback from that schedule-only object. Missing/failing response yields empty safe names, not stale names or contact fallback.
6. **Allowed difference:** **normal UI email fallback is intentionally NOT required View-As parity**. Normal queries and renderer keep it. Browser fixture proves normal `synthetic6@example.invalid` appears while View-As omits it. No email disguised as a name.
7. **Captain parity:** same-target normal/View-As browser comparison passes with an other-team primary Captain and Co-Captain. Populated match schedule text and team IDs compare equal after replacing only the permitted leadership display difference.
8. **Player parity:** same comparison passes on shared Player dashboard, including refresh/direct route and Exit. SQL also verifies a valid implicit Player without an Auth account; no target session or role creation.
9. **Club Pro parity:** same comparison passes on the existing Captain dashboard. Scope is effective-member based; the Commissioner's role is only used to initiate/validate the protected context.
10. **Co-Captains:** both positions retain normal order and comma-separated formatting. SQL matrix covers Captain and both authorized Co-Captain targets separately. No new formatting/deduplication and no fourth Club Pro display slot.
11. **Missing/fresh names:** null assignment returns null; name-empty/email-present stays empty under View-As; shared modal does not fail. Final targeted tests update a synthetic leadership assignment and verify the next read sees the replacement, then restore it. No separate View-As name cache.
12. **Schedule-set equality:** browser compares complete modal text (excluding approved name difference) and team option IDs for all three roles against the same populated fixture. SQL compares the entire existing dashboard output before/after scoped reads. Dates, times, location, home/away and match/team data remain supplied by untouched queries.
13. **Active-team filtering:** SQL returns only `is_active=true` teams in the permitted division; existing normal/display team filter is unchanged. Targeted fixture inactivates an other team and verifies exclusion. Unrelated inactive division and invalid scope deny.
14. **Privacy/security:** permanent tests assert exact response key allowlists, reject extra team/member args and forged actor, deny ended context, and exclude email even if present in prior display objects. Dedicated origin, proof locking, effective identity, mutation rejection, maintenance and Exit are reused. Full regression includes existing View-As security controls. No member-detail or normal authorization expansion.
15. **SQL requirement:** one helper `lms_read_private.schedule_captains(jsonb,jsonb)` and one bounded branch added to existing `view_as_private.page_read(jsonb,text,jsonb)`. Existing `lock_viewer`, `competition`, `people`, dispatcher, business policies and tables unchanged. No new general directory RPC.
16. **Exact migration:** `lwrpc-admin/supabase/migrations/20260909231834_lms0730_schedule_captain_names.sql`; SHA-256 **`49c845f024a9854a9634aba577ddce630a165fcf76084217c562b161b9ad8966`**. Created using Supabase CLI. Source/owner/search_path/ACL guards protect existing wrapper and repeat application. No production authorization is inferred from this local approval.
17. **Grants/RLS:** existing restricted reader owns the new SECURITY INVOKER helper, empty search_path. Existing wrapper remains reader-owned SECURITY DEFINER, empty search_path, executor-only caller boundary. Helper excludes PUBLIC/anon/authenticated/service_role/executor direct EXECUTE. No business SELECT/UPDATE/DELETE grants added. Temporary SET membership and CREATE on the two private schemas are removed at commit. PostgreSQL 17.11 under a production-compatible non-superuser passes unchanged table/column grants, RLS, memberships and schema privileges, migration replay, rollback and reapply. [PostgreSQL evidence](lms-0730-postgres-results.json), [local rollback candidate](lms-0730-rollback.sql).
18. **Enumeration denial:** browser cannot submit arbitrary team/member lists; exact argument validation occurs in JS and SQL. SQL resolves selected division server-side using existing normal visibility, not the real actor's management scope. An active division already available in the normal selector remains selectable; this is not restricted to the user's own division or broadened to all divisions in one response. Direct private helper calls by browser/service/executor roles deny. The authenticated context wrapper is mandatory.
19. **Browser/mobile/accessibility:** Captain/Player/Club Pro, desktop 1280, 390, 320; long-name display, native selector focus, normal navigation, refresh, explicit supported dashboard route, existing Close button and Exit PASS. [Browser results](lms-0730-browser-results.json). Shared modal markup, labels, formatting and normal accessibility semantics are unchanged. No new standalone schedule URL or full accessibility conformance claim. Screenshots: `lms-0730-{captain,player,club_pro}-{1280,390,320}.png`. Inspected mobile captures show wrapped names within the existing card. Dev cold-load handoff retries were needed; no production code changed to accommodate the fixture.
20. **LMS-0729/0728/0727:** complete deterministic regression passes implicit Live identity, records/counts/points, zero-model path, deferred rank, implicit Player View-As and exact cross-community Rule 3.5 controls. Ask LWR source is unchanged. No generated benchmark.
21. **Normal LMS:** exact source comparison proves normal schedule SELECTs and shared rendering unchanged. Same-user normal/browser checks cover the shared schedule and dashboard. Full existing tests retain roster/Match Setup/score/authorization behavior. Genuine normal Captain/Player production acceptance remains a later deployment gate, not replaced by these fixtures.
22. **Required validation:** `npm test` **1050/1050 PASS**, zero failures/skips; final targeted schedule tests PASS; lint PASS with 0 errors/11 baseline warnings; `npx tsc --noEmit --incremental false` PASS; PDF server bundle PASS; `npm run build` PASS; `git diff --check` PASS. Windows cache permissions required elevated local build execution. PostgreSQL and all three browser role matrices PASS. Logs use `docs/lms-0730-{tests,targeted,lint,types,pdf,build,postgres,browser-final,diff}.log`. Tests mutate synthetic rows only to construct/revert cases; migration itself performs no business-row writes.
23. **Calls/cost:** project answer-model calls 0, embedding calls 0, generated benchmark calls 0; incremental project model cost $0. No production data or credentials loaded by browser fixtures.
24. **Exact later production sequence:** separate review/authorization; freeze/recalculate migration and application hashes; confirm accepted LMS-0729 deployment, exact function/role/grant baseline, maintenance, recovery and 19 business fingerprints; apply only this exact migration once; verify helper/wrapper metadata, temporary-grant cleanup, no RLS/business changes before app deployment; deploy exact candidate; NORMAL LMS FIRST with legitimate Commissioner/Captain/Player checks; integrity checkpoint; only then same-target schedule/role/privacy/active-team/mobile View-As acceptance; final integrity/security/maintenance/context cleanup and explicit acceptance. Stop on regression/drift/leakage; no automatic corrective SQL. Restore accepted application if authorized; reviewed SQL rollback restores only wrapper/helper state and never deletes business data.

## Local support files and review status

New test files: `lwrpc-admin/test/lms0730ScheduleNames.test.mjs`, `test/helpers/scheduleNamesMatrix.mjs`. Reproducible validation scripts: `scripts/lms0730-freeze.mjs`, `lms0730-postgres.mjs`, `lms0730-local-browser.mjs`, `lms0730-browser-check.mjs`, `lms0730-source-check.mjs`. The fixture launchers generate local `.lms0730-*-generated.mjs` files; they are test artifacts, not application/deployment files. Roadmap and the linked evidence/report/rollback artifacts are updated.

**LOCAL READY FOR REVIEW. Do not deploy or apply production SQL.** Rank consistency, contact-directory parity and broad security hardening remain deferred. No next release started.
