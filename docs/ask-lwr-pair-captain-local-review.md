# ELIGIBILITY_PAIR — Captain prospective-player local review

September 12, 2026. **LOCAL IMPLEMENTATION COMPLETE — STOP BEFORE PRODUCTION.** This implements the owner's approved Captain prospective-player scope. No production SQL, deployment, production lookup/audit call, Approved Answer, or production business-data mutation was performed.

Source checkout: `C:/lwrpc-league-system/.local-validation/fast-fix-ratings-success`, branch `codex/eligibility-pair-local`, based on accepted review commit `76a31c2` / accepted application `74df01d2b38d5950d053371bd26fe0ca22cb955d`.

1. **Exact SQL/function change.** One new migration replaces only `ai_live_private.eligibility_pair(uuid,uuid,jsonb)`. It adds Captain managed-team context, a bounded prospective population, context reauthorization and team-aware audit/fingerprint fields. The public `ai_live_lookup` dispatcher is unchanged. No new schema, table, column, public RPC, table grant or RLS policy is introduced. A normalized function-body hash gate accepts only the reviewed predecessor or this exact implementation, permitting safe migration replay while rejecting unrelated source drift.

2. **Managed-team authorization.** Trusted `resolve_identity(p_actor)` determines the actual role/member. The Captain branch requires an active team in an active division, league and season, and uses the existing management relationship: the actor member matches Captain, either co-Captain, or the existing club-pro assignment slot. It does not infer management from roster membership or a browser role. Team hierarchy and assignment rows are locked/rechecked for each resolve/read/audit transaction. Facts require the team's exact division; same-season access alone is insufficient.

3. **Prospective-player resolution.** Only the two specifically named subjects in an ELIGIBILITY_PAIR request are resolved. Managed team selection happens first. Candidate names must meet the existing minimum three-character, maximum 100-character input bound; wildcard/directory syntax is rejected. Captain matching starts at a name word boundary, so `Tim` does not match `Stimpson`. Full unique names resolve directly; ambiguous names return at most five identity labels. Selected references are reauthorized each time. No member list or roster-picker browser read is used.

   Prospective candidates must be explicitly active (`is_active_member = true`); legacy null-active members remain excluded, preserving the accepted pair predicate. For leagues with home-community-only restriction, prospects must match the managed team's home location ID or its nonempty normalized location name, using the existing roster-screen name normalization. Missing home context fails closed for prospects. For other leagues, explicitly active named prospects may be evaluated for the authorized team. Existing self and active managed-roster relationships in the applicable season remain available, including when prospect community filtering would exclude them. This retains rostered-player rating checks without asserting community/roster approval.

4. **Minimum facts exposed.** Name resolution returns only references/display labels. The trusted evaluator receives the two players' applicable Season DUPR or PrimeTime Season DUPR, RF, source NR classification and relevant same-season placement division references. Community fields are read internally solely for candidate authorization and are not returned. User-facing output contains the two names, applicable rating/NR basis, calculation, official Rules source and specified team/division.

5. **Captain/co-Captain behavior.** Synthetic actual-SQL tests cover Captain and both co-Captains with rostered pairs, prospective pairs and mixed pairs. Role plus managed-team assignment is required. Changing the actor to ordinary Player or removing the management assignment invalidates subsequent reads. The club-pro role does not receive prospective access; its previously accepted managed-roster access remains unchanged.

6. **Multiple-team clarification.** With multiple matching active managed teams, Ask LWR asks “Which of your teams are you asking about?” It returns at most eight authorized team choices. If there are more matches it states the bound and permits the specific team page or a more specific question. A unique matching managed team is selected automatically. Team names/division context in the question can narrow managed choices; they never authorize an unrelated team. A team page ID is only a hint until SQL validates it.

7. **Division-context inheritance.** Selected team, league, division and season are retained in the encrypted, session-bound continuation state. Subsequent player choices retain earlier subject references. The team's division is inherited automatically; no redundant division question is asked. Final facts and audit carry the same team. Changed hierarchy, forged/cross-session receipts or mismatched team/season/division context cannot produce an answer.

8. **NR behavior.** The deterministic evaluator and current Rules binding are unchanged. Prospective NR with blank numerical Season DUPR uses the proposed managed-team division plus relevant existing placements and the accepted highest-adjustment treatment. It does not automatically fail because the stored number is blank, and no number is written. Rated individual limits, combined limits, PrimeTime selection, unknown inputs and current-document/configuration conflict handling remain as accepted.

9. **Unauthorized-team denial.** A Captain cannot use another Captain's team, an unrelated division, or another season with this authorization. Read/audit without managed team context is denied. Team ID is included in the facts fingerprint, so changing even to a second authorized team requires a fresh facts evaluation. Inactive hierarchy and removed assignments fail closed.

10. **Ordinary Player denial.** Explicit and implicit ordinary Player identities remain denied by the actual SQL role gate. Selecting a managed-team ID does not elevate a Player.

11. **View-As denial.** Application denial occurs before catalog/member lookup. Private SQL independently rejects View-As origin, including a real Commissioner actor. No effective-context privilege bleed is introduced.

12. **Commissioner regression.** Commissioner/league-manager population and optional division clarification remain unchanged. Actual-SQL integration verifies a Commissioner receives the existing pair answer without Captain team clarification. Existing pair questions in both name orders, calculation controls, stale-rule/facts checks and source requirements pass. No hardcoded Terry/Tim identities exist in implementation.

13. **Privacy and result wording.** No email, phone, DOB, Auth, membership/account fields or unrelated history are returned. Browser choices contain opaque signed choice keys and authorized labels. RF/source inputs stay inside the evaluator. The answer establishes pair rating requirements for the named team/division and retains the explicit limitation that this is not roster approval; other participation/community/roster conditions and unverified PrimeTime age requirements remain applicable.

14. **Audit.** Existing operational attempt limits and required access audit remain. Audit records actor/effective member, target references, managed team, league/season/division, Rules version/basis and result. Team-context requests and invalid-team attempts have minimal audit context. Final answer requires successful audit with an unchanged fingerprint. Names, contact details and numeric ratings are not duplicated into audit JSON. Operational audit/attempt records are the only runtime writes; there are no business-table or eligibility-result writes.

15. **Migration filename/hash and rollback.** CLI-created migration: `lwrpc-admin/supabase/migrations/20260912200340_eligibility_pair_captain_prospects.sql`. SHA-256: `c73de4b52911d593481be2eb5546e345e863e3369853efb3d71b62e304f45eff`. Rollback: `docs/ask-lwr-pair-captain-rollback.sql`. SHA-256: `0c01cc2662b3c6d82db697b661cdf18bd186ddd2ca9dbb00f5e7e013ca606cc9`. Rollback restores the accepted pair helper and retains dispatcher/audit history. Native fixtures verify transaction rollback and exact predecessor function restoration.

16. **Grants/security.** Function remains private, `SECURITY INVOKER`, with fixed empty `search_path`. Explicit execution revokes cover PUBLIC, anon, authenticated and service_role, followed by the single service_role execution grant. Browser roles cannot directly invoke either helper or public trusted wrapper. No RLS relaxation or member-table grants are made. Team authorization uses trusted actor identity and database relationships, not client filtering. Existing service-role placement behind the trusted server handler is preserved. Security design follows the [Supabase database-function guidance](https://supabase.com/docs/guides/database/functions).

17. **Tests/build.** Final focused run: **40 tests passed, 0 failed**. It includes actual-SQL Captain/co-Captain boundaries, prospective/mixed/rostered pairs, context and identity retention, both exact question orders, owner language variants, ordinary Player/View-As denial, Commissioner behavior, NR blank numerical value without writes, minimum projection, foreign-season denial, PrimeTime facts, stale audit, audit failure, replay and rollback. Native PostgreSQL **17.11**, loopback only, passes the same security matrix; fixture business snapshots remain unchanged. `npm run build` passes. `npm run lint` passes with zero errors and six pre-existing warnings. Existing Node module-type warnings remain. Full-suite comparison: accepted baseline **1,331 tests / 1,312 passed / 19 failed**; candidate **1,340 tests / 1,321 passed / the same 19 failures**, with zero candidate-only failures. Both fixture exports use matching LF normalization for historical SQL hash tests. The pre-existing failures are not represented as a green suite. Results are recorded in `captain-pair-baseline-differential.json`.

18. **Controlled production sequence — proposed, not executed or authorized by this local approval.** Obtain review approval for the exact migration/hash and application commit. First verify normal LMS health, current active Rules/division compatibility, actual function/ACL drift, deployment identity and tested recovery artifacts. Prepare the reviewed application artifact before the change window. Apply only the reviewed migration transaction, verify private privileges and function identity, and promptly promote the matching reviewed application; do not leave the old application paired with the new Captain clarification contract. If the application cannot be promoted, restore the helper immediately with the reviewed rollback. Then perform controlled read-only replays as an actual Captain/co-Captain on an authorized team, both exact name orders, prospective/NR context and denied unrelated-team/Player/View-As controls, followed by normal Ask LWR and Tier 1 LMS health checks. Any replay requires the separately approved production phase because operational audits are written. No production test rosters, teams, members or ratings are needed. Retain accepted application `74df01d2b38d5950d053371bd26fe0ca22cb955d` / deployment `dpl_5hYSN6eZN7SVJUQi7Uch7s5D2Nwd` and the exact helper rollback as recovery. Stop and restore on regression; no production business-data mutation is included.

Local evidence files: `captain-pair-focused.log`, `captain-pair-native-postgres.json`, `captain-pair-native-postgres.log`, `captain-pair-lint.log`, `captain-pair-build.log`, and `captain-pair-baseline-differential.json`. Baseline/candidate full logs and fixture export provenance are retained under `C:/lwrpc-league-system/.local-validation/captain-pair-gates`.

These are local synthetic evidence and source-review results, not production acceptance. Generation/model calls: zero; generation cost: $0. No deployment or production SQL is authorized by this report.
