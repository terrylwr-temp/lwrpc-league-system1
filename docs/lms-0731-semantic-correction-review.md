# LMS-0731 / 0.1.553 — semantic variant correction LOCAL READY

2026-09-10. **STOP FOR REVIEW.** Production remains the deployed original LMS-0731, NOT PRODUCTION ACCEPTED. No corrected deployment, SQL mutation, publication change, or new Approved Answer. Team-registration/Important Dates and LMS-0732 remain deferred.

1. **Root cause:** `chooseApprovedEvidence` used a generic cosine similarity minimum of 0.65 as a necessary relevance condition for every managed candidate. Its compatibility check handled days, numbers and scheduling but did not recognize substitute-eligibility concepts or inverse requirement/permission wording. Thus retrieval success alone did not establish evidence applicability. This was a relevance-selection failure before generation, not a reversed answer from the model, missing persistence, or failed activation. The database computes cosine similarity against the stored policy representation; it also returns a lexical score which this gate does not use. There is no exact-question-only SQL macro.
2. **First divergence:** normalization differs naturally, but both questions remain standalone document-policy requests, use All scope, perform one managed search and retrieve one candidate. Exact original question proceeded to `managed_only` and generated; inverse question reached `formal_only_or_none`, no selected evidence, skipped generation. With current publication/manifest and compatibility conditions verified, the first rejecting condition is the application semantic relevance gate. Historical numeric managed scores were not retained and are not invented; the failing score is inferred to be below 0.65 from that gate and the observed branch. Local controls deliberately replay score 0.5 to prove independent concept applicability, not claim it was the historical score.

| Stage | Exact question | Inverse question before correction |
|---|---|---|
| Raw | Do subs have to meet the DUPR rating range? | Can my sub be outside the DUPR range? |
| Normalized | Lowercase original, punctuation retained | Lowercase original, punctuation retained |
| Existing intent/keyphrases | No direct substitute intent support | No direct substitute intent support |
| Managed search | 1 candidate, no extra embedding | 1 candidate, no extra embedding |
| Status/date/scope/authority | Current active all-league standing revision | Same revision and controls |
| Application relevance | Pass | Rejected before eligible evidence selection |
| Generation | Approved Answer selected, GPT-5.5 answered | No evidence, model skipped |

3. **Correction:** new `aiSubstituteApplicability.js` recognizes a bounded substitute-eligibility policy family in both the question and canonical policy. It requires explicit supporting division/DUPR eligibility content, and NR content when NR is requested. This supplies a deterministic relevance reason for an already retrieved immutable candidate. No candidate injection, SQL, vector schema or stored embedding change. Generic 0.65 threshold remains unchanged; unrelated policy retrieval retains existing calibration.
4. **Semantic/inverse behavior:** within-range requirements and outside/above/below/exceed/not-in-range/waiver wording map to the same policy subject, without changing or constructing the policy answer. Existing numeric compatibility restrictions still reject unsupported numerical personal conclusions. Other policy topics do not inherit this match.
5. **Terminology:** sub, subs, substitute(s), substitute player(s), replacement player(s), within division/DUPR/eligibility context. Unrelated meanings, equipment, community, scheduling, registration, playing-rule and personal-rating requests are excluded. This is bounded intent recognition, not universal language understanding.
6. **Polarity:** managed-generation instructions now explicitly treat the Approved Answer as evidence rather than a literal Yes/No macro. The model must answer the current question's polarity and preserve qualifications, including NR, without certifying personal eligibility. All three generated inverse cases begin No; exact/qualification/NR cases begin Yes. Manual review confirms complete substantive requirements, not merely the opening word.
7. **Authority/scope:** no change to `approvedEligible`, source identity/hash revalidation, manifest, effective/expiration dates, season/league scoping, conflict rules or formal-evidence priority. Existing formal evidence retains priority; no blanket managed-source promotion. Bounded manager diagnostics now show at most four revision IDs, rounded scores, fixed reason codes and selected/rejected/not-selected decisions through existing retrieval diagnostics. No new persisted question/member data or telemetry schema.
8. **Unrelated policies:** required five negative questions all reject this policy even with injected semantic score 0.99. Additional rejection controls include personal substitute rating, equipment/serve, registration and non-league meanings. Unrelated low-score Approved Answers still do not qualify for the new family match.
9. **Lifecycle:** Draft, Retired, missing activation, future effective date, expired date (exclusive), wrong league, wrong season and stale manifest all exclude. Matching weekday scope passes. Unsupported NR facet excludes. Existing retirement-during-generation, source validation and authority/conflict suites pass.
10. **SQL:** none. Read-only production verification found the same active revision `bdccf8d7-340f-48bb-a50e-6531943f3282`, no expiry/retirement, all-league standing scope, effective 2026-09-10 and current authority manifest. No production database mutation, embedding request, publication edit or recreation.
11. **Tests:** targeted suite 90/90 PASS; full `npm test` **1090/1090 PASS**, zero skipped. Lint 0 errors/11 existing warnings; `npx tsc --noEmit --incremental false`, PDF verification, production build, `git diff --check` PASS. Evidence: `lms-0731-semantic-{tests,lint,types,pdf,build,diff}.log`. New deterministic controls cover all nine required variants plus six further terminology/inverse variants. Existing normal routing and authorization suites remain included.
12. **Generated scenarios:** six only, inverse first: outside range; exact original; above limit; below minimum; division qualification; NR rules. **6/6 PASS**, with exact unchanged approved policy text and isolated source fixtures. Uses production generation function/selection and configured production model; database/source lifecycle is fixture-based and separately tested, not a fresh production acceptance. No broad benchmark or re-embedding. [Results and exact responses](lms-0731-semantic-generated.json).
13. **Cost:** six `LOCAL_BENCHMARK` answer-model calls; requested `gpt-5.5`, returned `gpt-5.5-2026-04-23`; input **8,729**, cached input **0**, output **477**, total **9,206**. Existing configured pricing estimate **$0.057955**. Zero embedding calls in this local correction/validation. Runner records provider numeric usage, not prompts or credentials; it refuses to overwrite existing results and stops on the first failed scenario. Production model and accounting implementation unchanged.
14. **Normal Ask LWR:** complete suite preserves document/Important Dates routing, Rule 3.5, SELF rating, team-record zero-model path, procedural routing and existing managed evidence calibration. Existing unresolved team-registration/Important Dates findings are not claimed fixed or bundled. No normal-source override.
15. **Normal LMS:** comparison against the deployed 316-file manifest finds only `app/lib/aiApprovedAnswersSelection.js` and `app/lib/aiAnswerGeneration.js` changed, plus new applicability helper. No UI, View-As, role, member, team, roster, schedule, match, score, standing, rating or configuration implementation changed. Existing View-As/PostgreSQL authorization regressions pass. [Candidate hashes](lms-0731-semantic-candidate.json).
16. **Controlled continuation after owner review:** verify candidate hashes and current deployed identity; verify same active revision and operational/security fingerprints; stage prior exact package plus these three runtime files; no SQL; deploy and verify READY; normal LMS smoke first; integrity checkpoint; targeted inverse retest first, then exact/qualification/NR and representative Rules question under approved cost limits. Verify Approved Answer authority citation, one revision, linked case still independently New, and final fingerprints. No new draft/activation needed. Stop on any material normal regression, failed variant, lifecycle/security failure or operational change. Do not begin another release automatically.

Current status: **LMS-0731 / 0.1.553 DEPLOYED — NOT PRODUCTION ACCEPTED; local correction validated, awaiting review.**
