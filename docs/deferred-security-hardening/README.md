# Deferred security-hardening implementation

Preserved from the broader LMS-0726 foundation; NOT part of the scoped View-As release. No archived migration has been applied to production. Paths beneath workspace/ mirror their original repository paths. Original docs remain at the repository docs/ directory, including policy inputs, specifications, evidence and test logs.

To resume the separately authorized hardening project, restore this mirrored tree into an isolated checkout of the recorded baseline, together with the retained original docs and current dependencies. Do not run these scripts directly in the archive: their relative paths intentionally describe the original workspace. Review/reconcile migrations before restoring them to any active migration directory. The mixed rating/viewer domain test is preserved whole; scoped parity gets its own focused tests.

The original app/lib/lmsViewer.js identity helper remains in the app for scoped adaptation. The LMS-0725 privacy-test correction and all accepted 0724/0725 implementation are retained. No mini-LMS/security infrastructure was removed by this archival.
