# LMS-0725 / 0.1.547 — policy family and observability correction

September 8, 2026. **Locally validated; STOP BEFORE DEPLOYMENT. Production remains NOT accepted.** No SQL, corpus/Approved Answer change, historical backfill, production question or View-As UI parity work.

1. **Q55 policy family.** `aiRequestIntent.js` recognizes rating establishment, set, lock, duration, remain/stay, change/reset and update operations before Live lookup. It uses operation/concept matching rather than exact sentences. Q55 already routed correctly before this correction; its historical empty-selection operational cause remains unproven. The healthy evidence path remains bounded to active official sources. No timeout increase or speculative transport repair was introduced.

2. **Locked correction.** Both “When is my Season DUPR locked?” and “When does my Season DUPR lock?” now select document policy. The no-possessive version remains in Q101; the possessive and verb variants are permanent supplementary controls.

3. **Change correction.** “Can my Season DUPR change during the season?”, “Does my Season DUPR stay the same all season?”, “How long does my Season DUPR remain in effect?” and the reset paraphrase take the official-policy path. They do not fetch personal ratings. Current Season/PrimeTime rating-value questions retain SELF_RATING; current official DUPR retains its capability limitation.

4. **Rule 4.1 evidence.** Verified by a fresh read-only query before implementation: current Rules version `f0aad5ad-cf08-46c2-94fd-686ceb1271c0`, chunk `d51e615b-a4f2-460f-8a81-da9abfbd46af`, page 3. Exact extracted wording:

   > 4.1. Season DUPR Ratings: A player's Season DUPR Rating is established on the date
   > communicated to all captains prior to the first scheduled league match and remains in
   > eƯect for the duration of the season.

   The extraction spelling is preserved. [Current governing evidence and source identities](lms-0725-q55-current-evidence.json) also contains Important Dates and the separate qualifications. Current Fall 2026 recording is September 27 for Weekday, Saturday and PrimeTime; its year comes from verified document metadata, not today or a hardcoded application answer.

5. **Material qualifications.** Establishment answers preserve timing AND season duration. The selector also collects separately documented season-rating reset provisions if present, retaining individual exact ranges; a synthetic local test proves this behavior without creating or modifying an official source. No current longer-season reset provision was found, so none is invented. PrimeTime applicability requires the current age-based rule's express application of the rating/division rules, selected separately from Rule 4.1 and the PrimeTime recording event. Methodology remains distinct and includes the selected truncation/NR/aggregate qualifications. Independent source revalidation remains mandatory.

6. **Empty-evidence diagnostics.** Existing bounded quality JSON now retains interpreted policy kind/object, league scope, retrieval/completion counts, up to eight chunk/version references, selection statuses, count before validation, validation result, final count, and the stage producing zero evidence. Selected references are prioritized and truncation is explicit. Completion retains catalog/chunks stage, bounded duration and failure reason. No passages, raw questions, credentials or personal Live values are added. Failed source validation emits the same sanitized metadata through existing operational logs; player response fields are unchanged.

7. **Reason design.** Reasons include `POLICY_COMPLETION_UNAVAILABLE`, `NO_RETRIEVAL_CANDIDATES`, `APPLICABILITY_REJECTED`, `SCOPE_REJECTED`, `DEDUP_CONFLICT`, `SELECTED`, `NOT_SELECTED`, `NOT_VALIDATED`, `SOURCE_REVALIDATION_FAILED`, `CONFLICT_BLOCKED`, `NO_FINAL_EVIDENCE` and `VALIDATED`. Completion distinguishes `READ_FAILED`, `CATALOG_BOUND`, `CHUNK_BOUND` and `DEADLINE`. Codes describe the observed branch, not an inferred historical cause. Conflicting duplicate references still fail closed; empty or rejected evidence still makes zero model calls. This uses existing telemetry/log retention, with no new indefinite debug store.

8. **Telemetry observability.** `persistQuality` records build versus `capture_ai_quality` operation, result class, evidence count, answer correlation/idempotency reference, origin, attempt count and duration alongside the existing timestamp/version. It distinguishes `BUILD_FAILED`, `RPC_FAILED`, `DATABASE_ERROR`, `DEADLINE` and allowlisted internal quality errors. Only allowlisted database/PostgREST codes survive; arbitrary error messages, credentials, member values and payloads do not. Build failures also receive the observer's answer ID. Normal answer delivery remains fail-open. Existing SQL and the 500ms application budget are unchanged; no retries were added.

9. **Q23/Q36 history.** Both historical answers succeeded; telemetry persistence failed. **Exact historical database error unavailable.** The correction does not claim their source count/shape caused failure and does not manufacture replacement outcomes. Their existing evidence files remain intact. Future production retests will be new events.

10. **Duplicate/replay behavior.** Existing RPC idempotency and transaction behavior are unchanged. Isolated PostgreSQL tests accept the new bounded diagnostic snapshot under the existing 4096-byte limit and retain one outcome after identical replay. Existing occurrence, review-case/event and feedback duplicate/replay controls pass. No SQL or historical recovery mechanism was introduced.

11. **103-question benchmark.** All previous 99 cases retained; Q100–Q103 appended. **103/103 routing/expected-classification PASS.** Final classifications: 82 generated document answers, 16 deterministic Live/protected cases, four clarifications and one expected insufficient-evidence case. The 12-question generated policy-family suite includes six supplemental paraphrases without changing the 103-case benchmark denominator. [Permanent cases](lms-0725-q55-cases.json), [preflight](lms-0725-q55-full-preflight.json).

12. **Generated results and truthful ledger.** **82/82 final generated benchmark answers PASS; 12/12 final policy-family answers PASS** (six overlap the benchmark and six are supplemental). The final audit combines the full regression with final revalidation of the affected policy answers and Q57; the last prompt change applies only to rating policy/date questions. The unchanged answer paths retain their completed full-run evidence.

   Two local omissions were found and preserved: Q29 initially omitted the selected ineligible-player/blocked-score restriction; the generic requirement-coverage instruction was strengthened and Q29 passed its targeted retest and full regression. The first full-run Q103 omitted season duration; a rating-policy completeness instruction corrected this and the final affected-policy suite passed. These failures were not discarded or mislabeled as passes. [Final audit and individual evidence](lms-0725-q55-final-audit.json), [final family answers](lms-0725-q55-family-model-results.json).

   This correction made **132 model request attempts: 131 completed responses and one request failure with no response**. This includes failed/intermediate checks and revalidation, not just the final benchmark. The initial network failure and all intermediate answer artifacts are preserved in the audit ledger. Existing configured `gpt-5.5` was used with `store:false`; no model/config change. Zero embedding calls, zero Live member/rating data sent to the model, zero production Ask requests. Validation uses saved official evidence, fixture retrieval/signing and the actual selectors/source gate/generation integration; it is not production retrieval acceptance.

13. **Final observed quality totals.** Cross-league leakage 0; synthetic excerpts accepted 0; mechanics-as-applicability errors 0; state-versus-policy errors in required controls 0; material qualifications omitted 0; unsupported grounded answers 0; incorrect grounded dates 0; incorrect grounded years 0. These describe the final reviewed local evidence set, not a guarantee of all future generated responses. Historical local omissions remain in the ledger. The production Q55 operational failure still requires an instrumented production retest after deployment approval.

14. **View-As regression.** Target-effective Live authorization, dedicated origin, mutation blocking, feedback separation and read-only behavior remain intact. Document-policy questions clear existing Live clarification without calling a personal lookup, including forged Commissioner/member fields. Policy diagnostic logs use an explicit `view_as` origin and request correlation ID. The normal player quality pipeline is not introduced into the isolated View-As route. Boundary, schema/database, lock/maintenance and existing DUPR receipt/choice tests pass. No UI layout or help changes were made; compact welcome, keyboard-accessible help and actionable examples retain existing regression coverage. No new production/browser acceptance is claimed.

15. **Validation.** `npm test`: **906/906 PASS**. Focused policy/telemetry/PostgreSQL/View-As run: 54 PASS. `npm run lint`: zero errors, the same 10 pre-existing warnings. `npx tsc --noEmit --incremental false`, `npm run verify:ai-pdf-server-bundle`, `npm run build`, and `git diff --check`: PASS. The sandboxed build failed twice writing generated `.next/cache/.tsbuildinfo`; the approved build outside the sandbox passed without an application workaround or deployment. Final audit script lint also passes. [Test output](lms-0725-q55-test.txt), [build](lms-0725-q55-build.txt), [lint](lms-0725-q55-lint.txt), [focused checks](lms-0725-q55-focused.txt).

16. **Application-only.** No SQL was created or applied. Production RPC/schema, permissions, source documents, Approved Answers, historical outcomes and release version were not changed. Implementation: `aiRequestIntent.js`, `aiPolicyEvidence.js`, new `aiPolicyDiagnostics.js`, `aiAnswerGeneration.js`, `aiQualitySnapshots.js`, `aiQualityCapture.js`, and correlation/origin plumbing in `askLwrPlayerAnswer.js`, `liveLmsService.js`, `api/view-as/read/route.js`. Tests/runners/documentation accompany these changes. No speculative timeout/retry correction is represented as a proven fix for historical failures.

17. **Exact production continuation — only after separate review/deployment approval.** Deploy the reviewed LMS-0725 / 0.1.547 application-only change. First ask Q55 verbatim: “When is my Season DUPR established?” Require document route, active Rule 4.1 timing/duration plus applicable recording date, valid exact citation, and successful telemetry with meaningful pipeline diagnostics. Then retest Q23 and Q36 and reconcile persistence individually. Run the new policy contrasts and target-effective View-As rating/policy pair as approved. Only after these pass resume the remaining 28 production cases from the stopped point, stopping on any material failure. Reconcile answers versus telemetry and complete all other acceptance requirements before marking production accepted. View-As UI parity remains the next mandatory work only after that acceptance.

**STOP FOR REVIEW. No deployment performed.**