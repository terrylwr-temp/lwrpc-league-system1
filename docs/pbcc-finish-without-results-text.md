# PBCC Finish Match — optional results text

## Deployed

Owner explicitly authorized deployment. Production deployment `dpl_4ygupFd65bHgHAGFAGSV5WGQvbYZ` is READY at `lwrpc-admin-2j7s9zcie-terry-lwrpc.vercel.app`. Production build passed. Exact package has one changed file, SHA-256 `741fa4ae3bb684382f1144e812c7aab8c44462fe23573beab803a934efe919b6`; all other files match accepted LMS-0730. Version label remains LMS-0730 for this bounded UI correction. [Package manifest](pbcc-no-results-text-production-manifest.json).

Post-deployment normal Commissioner dashboard and PBCC administration/match listing load successfully. Public deployed PBCC chunk `0fvv~lgf8laic.js` contains both `OK - Send Results` and `Don't send text` and the skip handler. All 10 PBCC table counts/fingerprints unchanged before/after deployment, including matches, sessions, results and activity log. No SQL migration, live match completion, notifications or OpenAI calls. Full live Finish interaction was deliberately not exercised because it changes real data and sends next-match invitations. Prior deployment `dpl_FstBtuYRBGBQ6vaRFdSt2nBYJoaE` retained for rollback; no rollback needed.

## Implementation and local verification

Owner request: offer both the existing results-send action and **Don't send text** on the final statistics screen. Owner clarified that only the completed-match results notification is skipped; next-match invitations remain enabled.

Local change in `lwrpc-admin/app/round-robin/[id]/admin/page.js`: after successful `completeSession`, the existing statistics modal offers `OK - Send Results` and `Don't send text`. The latter clears the pending-send state, closes statistics and uses the same existing exit callback, without invoking `sendSessionResultsText`. Both buttons disable while a send is in progress. Ordinary Stats viewing retains its single Close action. Buttons stack on narrow screens and wrap on larger screens using existing styling.

Completion already occurs before this modal: scores/results are saved, scoring closes, and the recurring next match is generated by `completeSession` with results SMS disabled. The separate results action sends only on explicit selection. Existing next-match invitation behavior is unchanged, as expressly requested. No server/API/SQL/auth changes. The separate player-page host Finish confirmation does not use this post-completion statistics screen and is unchanged by this bounded presentation correction.

Validation: lint passes with the same 11 baseline warnings and zero errors. Build PASS after rerun with local cache access (initial attempt encountered the known Windows cache EPERM). No live match finished for testing; no SMS/push/email or OpenAI calls made. Deployment status is recorded above.
