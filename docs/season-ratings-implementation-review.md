# Season Ratings — local implementation review

Candidate release: **LMS-0733 / 0.1.555**. Exact source and baseline identities are recorded in `docs/lms-0733-release-identity.md`; production review remains paused until that candidate is reviewed.
LOCAL ONLY — STOP FOR REVIEW. No production SQL, ratings import, deployment, reclassification, Clean/Delete/Copy action, or model generation occurred. Existing unrelated working-tree changes were retained.

The owner's latest clarification controls: **no hardcoded RF cutoff**. Clean Ratings uses its existing prompted threshold. Ask LWR and deterministic eligibility extract the inclusive threshold from verified active Rules evidence. Import preview shows raw source RF; it does not classify or clean ratings. There is no default cutoff if evidence is unavailable. The currently active rule yields 28/29 → NR and 30 → Rated under the RF rule alone; independent raw NR evidence still applies.

## Requested review results

1. **Current active Rules:** `6ae10e5f-fdde-41be-a941-d1b7ed360d1a`, label `v20260910133443-6ae10e5f`; activated 2026-09-10 13:34:54.794991 UTC. Document `9c200d0f-be41-4c73-9f47-41c18dcd0132`. Read-only production verification found active/ready status, 73 chunks, 71 searchable. RF chunk `8834ecc0-0c39-405f-bed9-0b9b6d3d25d0` is searchable with an embedding. [Evidence](season-ratings-active-rf-policy.json).

2. **Exact RF evidence:** Rule 4.1.1, page 3: “A player with a DUPR Reliability Factor of 29 or below will be classified as ‘Not Rated’ (NR) for League purposes and will be subject to the provisions of Rule 4.5.” The retained JSON contains the original punctuation/text.

3. **Current application paths:** `app/ratings/page.js` → existing prompted `isReliabilityNrAdjustment` and cleanup preview/apply helpers. `app/lib/rfPolicy.js` → validates/extracts the rule threshold and evaluates inclusive comparison with a mandatory supplied threshold. `aiEligibilityPolicy.js` → scoped Rules extraction, personal and policy eligibility. `aiEligibilityService.js` → bounded supplied-RF questions using the same verified evidence. `aiEligibilityIntent.js` routes those questions; `aiPolicyEvidence.js` selects supporting passages. Importer/preview displays raw RF only. Normal and View-As project their respective effective inputs into the same eligibility evaluator. `ratingEligibility.js` checks numeric division ranges and has no RF cutoff. SQL lookups project RF/raw-NR; they do not classify using a competing cutoff.

4. **Changed versus historical occurrences:** Current Rules extraction/answer wording and current eligibility fixture/expectations now use inclusive evidence. No hardcoded 29 constant, comparison, or answer cutoff remains in the new governing application paths. The old hold and design discrepancy inventory are explicitly historical. Older accepted-release evidence, reports, and synthetic historical retrieval/answer tests retain their original text. `aiPolicyEvidence.js` can recognize historical and inclusive phrasing for evidence selection, without deciding the threshold. Unrelated numeric/semantic-score comparisons remain untouched. New current tests use actual active evidence; fixed 28/29/30 values are regression inputs, not application settings.

5. **Clean Ratings:** Prompt, configurable threshold/off switch, comparison, cleanup calculations, notes, and persistence functions unchanged. Default/prompted behavior is not replaced by a constant. Actual helper regression tests cover inclusive boundary, missing RF, and disabled threshold. [11 unchanged function hashes](season-ratings-preserved-functions.json).

6. **Ask LWR:** All three requested supplied-RF questions pass deterministically, cite the verified active version, and do not perform personal RF lookup. Current evidence yields NR at 28/29 and no RF-triggered NR at 30. A different synthetic Rules threshold is honored in a separate extraction test, proving no fallback hardcode. Missing evidence fails closed.

7. **Deterministic eligibility:** Current-policy controls pass, including 29.0 NR and 29.01 Rated; accepted RF precision is up to three decimals. Independent raw NR takes precedence over a numerically high RF. Division/season scope, pairing uncertainty, and existing eligibility limits remain enforced.

8. **View-As:** Database regression sets effective target RF=29 and real commissioner RF=30; the target remains NR through existing View-As projection/evaluation. Import route rejects View-As marker, context bearer, and binding cookie before database access. No commissioner input bleed.

9. **Substitute policy:** Read-only verification found active Approved Answer `bdccf8d7-340f-48bb-a50e-6531943f3282`, activated 2026-09-10 11:32:59.680775 UTC. Its text was not rewritten. Existing substitute tests plus a new RF29 control show its applicable-NR-rules instruction uses current NR eligibility; NR is not an unconditional full-eligibility approval.

10. **Locked-season protection:** Import never writes `member_season_ratings`, including existing raw Doubles, RF, Season DUPR, PrimeTime, and notes. Existing source/season separation is explicit. No automatic Clean Ratings or source promotion/reset. Tests fingerprint business rows across success, repeat, failure, and RF refresh.

11. **Existing-data reclassification:** Read-only aggregate inspection found two Fall rating rows and 1,010 Saturday rating rows; neither season had an RF=29 row, including rows with established season values. No observed row requires a reset solely for this boundary correction. This is a snapshot, not an inferred lock state or authorization to reset. No reset/backfill was executed; other discrepancies are not resolved by this finding.

12. **Importer:** Strict CSV parsing, normalized exact DUPR ID only, no name/email fallback or member auto-create. Primary `doublesReliability`; `doublesRe` only when primary absent; conflicts invalid. Age source `metrics.subscores.doubles.over_65`, then missing 65+ falls back to `over_50`. Preserve prior source age when unavailable, with presence metadata. Numeric/JSON errors remain invalid; no permissive text stripping. Maximum 1,000 records / 2 MiB CSV. All preview rows are accessible through pagination.

13. **879-row fixture:** Actual Downloads export SHA256 `345f2a01399fa28933893c3423187e001e6295cae9332785abcfd8adf4eb2a6b`: 295 use 65+, 394 use 50+, 190 lack age metric; 73 raw NR. Recorded read-only identity counts: 695 unique, 178 not found, six ambiguous; 36 unique matches inactive, leaving 659 active/null-active candidates. Sanitized actual-value fixture reproduces those recorded identity categories using synthetic members; it is not a fresh production-ready count. Ready count must come from a fresh authorized preview after source-store deployment.

14. **Source storage:** Additive `ratings_source_private.sources` keyed by season/member, source JSON, revision, timestamp/import ID; `batches` contains exact reviewed payload, before rows, result, actor/file provenance. No foreign keys that alter existing deletion behavior. RLS and grants deny direct browser access; service-only wrappers reuse canonical manager/commissioner identity resolution. No production schema application or business backfill.

15. **Preview/commit:** File selection sends a read-only preview request. Persistent Import Matched Ratings requires ready rows; explicit confirmation names actual season and ID, counts and source fields. Preview includes source before/after and preserved season values. Cancellation retains review. Zero-ready repeated file has no commit action. Signed receipt binds actor, access session, file hash, exact changes, season, expected source revisions, expiry and import ID. Importer shows raw RF only, honoring the final owner clarification.

16. **Transaction/revalidation:** Single database transaction rechecks role, active season, unique exact member ID, activity, source revision, allowed fields and bounds. Whole batch rolls back on any error. Same import ID/payload returns durable result; stale identity/revision aborts. Short SHARE locks prevent duplicate-ID phantoms while retaining normal member-write priority through a one-second lock timeout. Source imports serialize per season; configured five-second statement limit and explicit four-second elapsed guard bound work. Native PostgreSQL 17.11 tests prove blocked identity writes, duplicate rejection, lock timeout and outer rollback. Final 879-row timing: 1,456 ms under concurrent local validation. This is rehearsal evidence, not a production latency guarantee.

17. **Migration:** `20260910134349_season_ratings_source_import.sql` (created with Supabase migration CLI). SHA256: `8f532af5b44644e749696d3903785236c6fe173b3119408ca896a220f4543df4`. This is source-store SQL only; threshold synchronization requires no additional production SQL/data mutation.

18. **Validation totals:** **1,154/1,154 tests passed**, zero failures/skips; focused policy suite **57/57 passed**. Lint: zero errors, 11 existing warnings. TypeScript, production build, post-build PDF bundle verifier and `git diff --check` passed. Logs: [full suite](season-ratings-all-tests.txt), [focused policy](season-ratings-focused.txt), [lint](season-ratings-lint.txt), [types](season-ratings-types.txt), [build](season-ratings-build.txt), [PDF bundle](season-ratings-pdf.txt), [native PostgreSQL](season-ratings-postgres-results.json), [browser](season-ratings-browser-results.json). Local database fixtures are synthetic; no production acceptance mutations occurred.

19. **Model calls/cost:** Zero generated model calls, zero generated tokens, $0 generation cost. No broad generated benchmark.

20. **Normal LMS:** Full existing normal/identity/authorization/View-As/eligibility regression suite executed. Clean/Delete/Copy functions remain identical; browser displays existing season values unchanged after source commit. Source SQL writes only its own two tables. Teams, rosters, schedules, matches, Match Setup, scores and standings were not altered by this work. This does not claim an exhaustive live-production UI acceptance run; production acceptance remains gated below.

21. **Controlled production sequence:** See the ordered gate below. No step is authorized merely by this report.

## Controlled production sequence — separate approval required

1. Review this exact candidate/diff and migration hash, including other pre-existing workspace changes before selecting a release. Preserve the accepted app identity and database recovery evidence. Verify normal LMS first, read-only; recapture source-store/schema/grants and business fingerprints, current Rules, effective role resolution and migration dependencies. Do not mix deferred security hardening.
2. Obtain explicit approval for the additive migration. Apply only the reviewed source-store SQL in a controlled window, with bounded timeouts and verified recovery path; no backfill or business updates. If prior schema state/dependencies differ, stop for review.
3. Separately authorize the application release. Deploy only the reviewed compatible candidate; verify normal role-based LMS behavior first, then import read-only preview, current Rules answers and View-As isolation. No production test rows. Compare business fingerprints and verify Clean/Delete/Copy remain unchanged.
4. A production ratings import requires a fresh actual CSV preview and separate explicit owner confirmation of target, exact source changes, exclusions and protected values. Stale preview requires another review; no automatic import after deployment or automatic cleanup afterward.
5. If regression occurs, disable importer/restore accepted compatible application; retain additive source/audit records. Application rollback cannot undo data. Any source correction needs its own reviewed before/after, concurrency checks and explicit approval. Never delete season data or restore the whole live database over legitimate new activity.
6. Establishment or mid-season reset remains a separate reviewed workflow. It is not included in this importer release and cannot be inferred from blank ratings, dates or roster locks.
