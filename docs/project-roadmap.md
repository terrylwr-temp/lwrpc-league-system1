## Local scheduling change — Copy Setting (not deployed)

2026-09-22: Schedule Settings now offers Copy Setting for a saved division setting. The copy receives a distinct name, preserves only configuration fields, starts as a draft, and does not create matches or byes. Its generated badge reads only matches linked to the copied setting; a copied draft cannot invoke Delete Schedule. New generator rows link matches through the existing `matches.schedule_setting_id` and byes through an additive nullable `team_byes.schedule_setting_id` column. Legacy settings continue to recognize unlinked historical matches/byes, while linked rows belonging to a copy are excluded from another setting's status and deletion. The additive migration also adds `league_schedule_settings.is_copy` with default false; it does not rewrite operational rows. The previous application is compatible with the added columns, so application rollback can retain the migration without deleting later league data. Three focused app/database tests and the production build pass. Lint has zero errors and 11 existing warnings. The optional full repository suite was started but stopped during its lengthy unrelated database tests; no full-suite result is claimed. No production migration, business-data change, deployment, or live schedule replay has occurred.

## Current production — LMS-0751 / 0.1.574 — PRODUCTION ACCEPTED

2026-09-20 EDT: Owner-authorized PBCC ten-player/two-court correction deployed from commit `3a086d67a9108da377ab143ab4514056045c7f0d` as READY `dpl_4F3Ch2FMsD18NuosH7gXTP8LzdpF`; prior accepted LMS-0750 READY `dpl_7TCcZGcsr7Y8UMfsoBk2puvBHnbe` retained for rollback. Exact-tree 12/12 complete-night scenarios, 1,412/1,412 tests, lint 0 errors/11 existing warnings, and production build passed. Normal LMS first and PBCC read-only checks passed under LMS-0751; ten of ten PBCC operational table fingerprints were unchanged. Real ten-player assignment generation was intentionally not invoked because Start Session/Next Game persist matches. [Full PBCC production acceptance](pbcc-ten-player-two-court-production-acceptance.md).

## Previous production — LMS-0750 / 0.1.573 — PRODUCTION ACCEPTED

2026-09-20: Controlled Ask LWR multiple-team and Stage 2.2 PDF normalization release deployed as commit `ebe215ee2c085ec39c04053f24106c7104f50d0e`, READY `dpl_7TCcZGcsr7Y8UMfsoBk2puvBHnbe`; prior READY `dpl_EP8usAGHkKhqmCMBLkTVbdi6pb1B` retained for rollback. Real Commissioner normal LMS dashboard, Teams & Rosters, and Members checks passed first. Four live multiple-roster questions gave the qualified Rule 5.1.2 answer; the same-community control gave Rule 3.7 and its League/DUPR rating condition. All five telemetry outcomes show one selected official evidence item and unchanged .35 threshold; runtime errors after acceptance zero. All 21 operational business-table fingerprints matched predeployment. Final active AI corpus has zero malformed `Ư`, with 27 corrected through normal processing and 537 occurrences retained only in superseded history. [Full LMS-0750 production acceptance](lms-0750-production-acceptance.md).

## Previous production — LMS-0749 / 0.1.572 — PRODUCTION ACCEPTED

2026-09-20 predeployment checkpoint (historical): LMS-0750 / 0.1.573 local release candidate for Ask LWR multiple-team retrieval and Stage 2.2 PDF `Ư`→`ff` normalization. Owner-authorized normal reprocessing activated Rules `efe8e0ff-9e8e-40c3-88f4-cb6bf068ddbf`, Captains Guide `bfe6d170-a683-4a43-9a1d-c59244012173`, and Important Dates `af622e44-b128-45a5-919f-29a3901af0c9` after exact chunk/metadata comparisons and full embedding checks. All 27 active malformed glyphs (23/3/1) were corrected; the active corpus had zero, while 537 historical occurrences remained in superseded versions. Six general retrieval phrasings cited Rule 5.1.2 and the same-community control cited Rule 3.7. The final 1,409/1,409 tests, 11/11 PDF tests, lint 0 errors/11 existing warnings, and build passed; lint/build also passed after the version bump. Deployment and production acceptance were pending at this checkpoint and are recorded above. [PDF normalization review](ai-pdf-ff-normalization-review.md).

2026-09-20: Stage 2.2 PDF `Ư`/`ff` normalization is implemented locally and tested. Read-only corpus audit found 537 stored occurrences (27 active before repair), all in English `ff` contexts. Owner explicitly authorized full Rules text to OpenAI embeddings and new version creation after an automatic approval rejection. Controlled reprocess created and activated ready Rules version `efe8e0ff-9e8e-40c3-88f4-cb6bf068ddbf` at 15:51:14 UTC: all 68 chunks match the exact local `Ư`→`ff` preflight, 66/66 searchable chunks embedded, zero active Rules glyphs, no other corpus difference. Six general retrieval variants cite Rule 5.1.2; same-community control cites Rule 3.7. Four active glyphs remain in Captains Guide/Important Dates pending scope; historical rows retain 537 old occurrences. Full tests 1,409/1,409, lint 0 errors/11 existing warnings, build pass. No application version bump/commit/deployment or business-data change yet. [PDF normalization and processing review](ai-pdf-ff-normalization-review.md).

2026-09-20: Ask LWR multiple-team controlled release review is **NOT production accepted**. Previous/current active Rules versions were compared completely: only new 5.1 Team Structure/5.1.1/5.1.2 and consequent Section 5 renumbering changed; Rule 3.7 remains distinct. All 66 current searchable chunks are embedded, six general live retrieval controls cite Rule 5.1.2, and a same-community control cites Rule 3.7. Full tests 1,408/1,408, lint and build pass. The active Rule 5.1.2 chunk contains malformed `suƯicient` in adjacent Rule 5.1.1, so the required clean-extraction gate is unmet. No version bump, commit, deployment, corpus write, or business-data write. [Controlled review and blocker](ai-multiple-team-controlled-release-review.md).

2026-09-19: Owner-authorized AI Feedback Drill-Down & Question History deployed as exact commit `2bf916b8b2ecf9863a2c518a0e62af7d80ae6855`, READY `dpl_Dr68zt62cthVFL5HHb5diAwKE12B`. Additive server-only read function applied once as production migration `20260919112713_ai_feedback_question_history`; no existing object/grant/RLS change. Normal Commissioner Dashboard/Teams checks passed first; Active Seasons zero is expected before October season start, while All Seasons shows 100 teams/16 roster assignments. All five real Not Helpful full question/answer pairs match retained records; cards, date/search, 27-row Helpful pagination, diagnostics and desktop/mobile pass. Ask LWR ball, incomplete-match and authorized self-rating regressions pass. The current official source numbers Incomplete Matches 5.8 (renumbered before release). All 26 non-outcome fingerprints and all 648 existing outcome rows unchanged; only four authorized regression outcomes added. Feedback counts unchanged. Retained rollback `dpl_ALQweBELZjWggy3wUGtbDcKZ1Ukw` remains READY. Real Commissioner only; owner explicitly accepted other-role production session coverage limits, backed by server tests and actual anon/authenticated database denial. No next release started; unrelated local schedule sorting edits excluded. [Full production acceptance, identities, recovery and coverage limits](lms-0749-production-acceptance.md). Earlier local-only and production status entries below are historical.

## Historical production — LMS-0732 / 0.1.554 — PRODUCTION ACCEPTED

2026-09-10: exact candidate deployed READY (`dpl_414rqyGtFAFrcQ6JNRTVH9Qs2cGU`). Normal Commissioner smoke and owner Captain PASS preceded targeted Ask LWR acceptance. Original registration and both schedule failures corrected; explicit league/season controls, official sources, Live contrasts and substitute No polarity pass. Cross-league/season/intent leakage: zero observed. Eleven targeted production generations, 31,617 input/1,968 output, estimated $0.217125 excluding embeddings; cached usage unavailable. Four Live controls zero-model. All 19 business fingerprints, security snapshot, Approved Answer revisions and seven active source versions unchanged. [26-item production acceptance report](lms-0732-production-acceptance.md), [numeric integrity ledger](lms-0732-production-integrity.json). Rank consistency and broader security hardening remain deferred. No next release started.

### Historical local-ready review

Owner-approved bounded implementation complete. [25-item local review](lms-0732-local-review.md) and [exact candidate hashes](lms-0732-local-candidate.json). Registration procedure intent retains league/season and uses both guide pages; schedule-release event uses exact Important Dates passages and avoids unnecessary common-date clarification. Signed choices remain constrained; Live contrasts protected. Final 1117/1117 tests, lint (0 errors/11 baseline warnings), types/PDF/build/diff pass. Four affected GPT-5.5 generations pass: 10,178 input, 0 cached, 602 output, estimated $0.068950; no embeddings/full model benchmark. No SQL, corpus/Approved Answer or normal business changes; no deployment. Production remains LMS-0731.

### Accepted diagnosis and source verification

2026-09-10: **DIAGNOSIS COMPLETE — READY FOR BOUNDED LOCAL IMPLEMENTATION APPROVAL.** [Final 25-item diagnosis/design and six-step implementation plan](lms-0732-routing-diagnosis-design.md), [29-question offline baseline trace](lms-0732-offline-routing-diagnosis.json), [authorized current-source verification](lms-0732-current-source-verification.json), [retrieval eligibility](lms-0732-source-retrieval-eligibility.json). Current production confirms Captains Guide pages 4–5 and Important Dates Oct. 7 for all three leagues: five exact text/version matches, all searchable with embeddings, active/ready documents. Registration intent is unresolved before SELF_RATING misrouting; schedule-release event/completion is absent. No corpus, Approved Answer or SQL mutation needed. Supplementary other-guide content query was blocked as out of scope and not retried; controlling-source verification is complete. No implementation, deployment, production mutation or OpenAI calls. STOP FOR REVIEW; production remains LMS-0731 below.

## Current production — LMS-0731 / 0.1.553 PRODUCTION ACCEPTED

2026-09-10: reviewed semantic correction deployed READY (`dpl_DKqa65yCcNr7E12VBw6Tb6uaFJca`). Normal smoke first PASS; six substitute-policy variants PASS with correct Yes/No polarity and one unchanged active revision. Official Rules regression and zero-model Live SELF TEAM PASS. All 19 operational fingerprints/security snapshot unchanged; linked case still New. Seven targeted generations, 11,034 input/576 output, estimated $0.072450 excluding embeddings; no broad benchmark. [Final 20-item acceptance report](lms-0731-production-acceptance.md). No next release started.

**Now in LMS-0732 diagnosis only:** PrimeTime 2026 Fall team-registration procedure misrouting AND Important Dates schedule-release retrieval. Implementation requires design review. Standings rank and broader security hardening remain deferred.

### Historical acceptance stop and local correction (superseded by acceptance above)

**Semantic correction LOCAL READY — STOP FOR REVIEW:** generic 0.65-only managed relevance gate rejected the inverse substitute wording after candidate retrieval. Added bounded substitute-eligibility concept applicability and explicit generated-answer polarity guidance; threshold, authority/lifecycle/source priority and active production revision unchanged. 1090/1090 tests, lint/types/PDF/build/diff PASS; six affected GPT-5.5 generations PASS (8,729 input, 0 cached, 477 output; $0.057955). No SQL/production changes or deployment. [16-item local correction review and controlled continuation](lms-0731-semantic-correction-review.md). Production status below remains unchanged.

Owner-approved application deployed READY (`dpl_Cg1M8avg9kzxHtXMogrfAmC7XZxc`). Normal smoke and all 19 operational fingerprints/security comparison PASS. Real Substitute Player Eligibility draft saved visibly, reopened, remained excluded while Draft, explicitly activated, and answered the exact question correctly with Approved Answer citation. First variant **“Can my sub be outside the DUPR range?” fails with insufficient evidence despite one managed retrieval candidate**. STOP before correction; remaining variants/Rules generation not run. Exactly one active revision retained and linked case still New. One agent GPT-5.5 generation, 1,379 input/71 output, estimated $0.009025 excluding embeddings; cached usage unavailable. [21-item controlled production report](lms-0731-production-review.md). **Not production accepted; no next release started.**

**Next mandatory separate Ask LWR correction:** [ASK LWR MUST-FIX — Explicit PrimeTime/Season Team Registration Procedure Misrouting](post-lms-0731-team-registration-misrouting.md). Owner reports an explicit PrimeTime / 2026 Fall how-to request routed to SELF_RATING with unrelated Saturday season clarification. After the current small release completes, diagnose intent/context retention and current authoritative procedural sources; preserve rating/state contrasts and zero cross-league/season leakage. Recorded only: no implementation, production changes, SQL or OpenAI calls. Does not expand LMS-0731.

Accepted local evidence: Approved Answer draft-save UX correction, nearby focused failure feedback, explicit DRAFT receipt, existing activation path, ACTIVE confirmation, linked-question prefill and bounded unsaved warnings. No authority/retrieval/SQL changes. 1053/1053 tests, lint (0 errors/11 baseline warnings), types/PDF/build/diff and 1280/390/320 browser lifecycle controls PASS. [Historical local review](lms-0731-local-review.md); production results above supersede its pre-deployment status.

## Last accepted production baseline - LMS-0730 / 0.1.552 PRODUCTION ACCEPTED

**Approved Answers diagnosis — stop for review:** reported substitute-rating draft does not exist (zero drafts; only two retired scheduling revisions). HTTP 400 POSTs align with reported save attempts; exact error text not retained. List All includes drafts correctly. Confirmed UI failure-feedback weakness: error at top of long form without focus/scroll; no explicit save success. Owner saw no error. [Read-only findings, limits and proposed smallest correction](approved-answer-missing-draft-diagnosis.md). No implementation/deployment/data changes/model calls.

**Deployed Ask LWR label correction:** `What can I ask?` now has no leading question mark. One-file owner-approved deployment `dpl_EipkEDCWmWtEdggT87caEyc39pfH` READY. Lint/build and production label/help open/close/focus checks pass. No SQL/data changes/model calls. [Verification](ask-help-label-deployment.md).

**Deployed PBCC correction:** owner-approved optional `Don't send text` action after Finish Match. Skip completed-match results only; preserve automatic next match and its invitation. One-file deployment `dpl_4ygupFd65bHgHAGFAGSV5WGQvbYZ` READY; normal LMS/PBCC screens and deployed button code verified, all 10 PBCC fingerprints unchanged. No live finish or notification sent in verification. [Deployment and limits](pbcc-finish-without-results-text.md). Version label remains LMS-0730.

Exact approved migration applied once and verified; exact application deployed READY. Normal Commissioner and owner Captain/Player PASS preceded View-As checks. Nick's other-team schedule now displays Todd Moorehead; own-team Nick/Jorge names, MPT 7 and MDUPR6 active-team sets and refresh pass. Implicit Player dashboard/Exit pass. Both acceptance contexts exited with credentials cleared. All 19 business fingerprints and verified security state unchanged; maintenance healthy. Zero agent model calls. Production lacks populated matches/rosters/Club Pro assignment: accepted local parity/mobile/privacy evidence retained. [Final 25-item acceptance report](lms-0730-production-acceptance.md). No next release started; rank consistency and broad security hardening remain open.

### Accepted local review (historical)

Owner-approved local implementation complete. [24-item review, exact migration hash and production sequence](lms-0730-local-implementation-review.md). Selected-schedule name-only helper uses existing grants; normal queries/shared modal unchanged. Captain/Player/Club Pro populated browser parity, 1280/390/320, refresh/direct routes, email exclusion and Exit pass. PostgreSQL 17.11 replay/rollback/privilege equality passes; 1050/1050 tests, lint/types/PDF/build/diff pass. No production SQL, deployment, business mutation or model calls. STOP FOR REVIEW; LMS-0729 remains production accepted. [Governing design](lms-0730-schedule-captain-parity-design.md).

## Previous production - LMS-0729 / 0.1.551 PRODUCTION ACCEPTED

2026-09-09: exact approved migration applied once, restricted reader/business privileges unchanged, exact application READY. Normal Commissioner and owner Captain/Player PASS preceded targeted Live and three effective-role View-As checks. All 19 business fingerprints unchanged; all agent contexts exited with credentials cleared. Five deterministic acceptance questions, zero model/embedding calls. Production has no standings rows; accepted local populated-count/multi-team evidence retained. [Final 31-item production acceptance report](lms-0729-production-acceptance.md). [Accepted 1047-test local review](lms-0729-local-implementation-review.md). No next release started.

**New separate View-As parity follow-up:** [Division Team Schedules other-team Captain names](post-lms-0729-view-as-schedule-captain-names.md), reported by owner during this release. Source diagnosis confirms other-team leadership references/name rows are omitted by the accepted projection. Review a minimal name-only effective-role correction separately; not bundled into LMS-0729. Also retain existing confirmation Escape focus-return limitation for separate UI correction.

[Rank consistency follow-up](post-lms-0729-rank-consistency.md) remains deferred. [Original design](lms-0729-live-identity-team-record-design.md), [owner/rank review](lms-0729-owner-decisions-rank-review.md), [historical SQL-boundary checkpoint](lms-0729-local-implementation-checkpoint.md).

## Previous production — LMS-0728 / 0.1.550 PRODUCTION ACCEPTED

2026-09-09: Exact approved migration applied once and verified; reviewed application READY. Normal Commissioner checks and owner Captain/Player PASS preceded Marilyn/Mark/Nick View-As acceptance. Correct identities, shared read-only UI, deterministic SELF and Exit passed; all three agent contexts cleared. All 19 business fingerprints and reviewed security catalog unchanged. Zero agent OpenAI calls. [Final 25-item acceptance report and limits](lms-0728-production-acceptance.md). Supersedes local-only status below; no next release started.

**Ask LWR team-record follow-up:** “What is our teams record?” exposes an existing unsupported Live capability; possessive wording misclassifies as SELF_TEAM. [Diagnosis and future controls](post-lms-0728-team-record-intent.md). Recorded without implementation, separate from normal implicit-Player identity.

## Historical local review — LMS-0728 / 0.1.550 implicit Player consistency

2026-09-09: **LOCAL IMPLEMENTATION COMPLETE — STOP FOR REVIEW.** Owner-approved two private View-As functions now recognize an otherwise-valid implicit Player; shared banner uses effective Player when stored roles are absent. No role/account/Auth mutation or backfill. [24-item review, function contracts, exact migration hash and production sequence](lms-0728-local-review.md). 1,042 tests pass; final targeted controls, lint (0 errors/11 existing warnings), types/PDF/build pass. PostgreSQL 17.11 non-superuser baseline apply/replay/partial recovery/rollback and metadata/RLS/business integrity pass. Synthetic implicit Player Member Detail -> shared Player Dashboard -> read-only denial -> Exit verified at desktop/390/320; normal Player/Captain controls pass. Only banner plus version files differ from accepted application. Zero OpenAI calls/cost. No production SQL/deployment; production remains LMS-0727 / 0.1.549.

**POST-LMS-0728 MUST-FIX — normal Ask LWR Live identity for implicit Players.** Classification B: the exact View-As correction does not change normal `ai_live_private.lookup` or account-entry linkage, which still requires an Auth-linked role row. Diagnose a bounded authenticated-member identity solution separately; no email fallback, account creation or role backfill is authorized. Do not combine with deferred broad security hardening.

**Recorded existing accessibility follow-up:** shared confirmation Escape closes without returning focus to the triggering button. Confirmed in local View-As browser check; accepted dialog/button code unchanged. No dialog redesign included in LMS-0728. See review limitations.

## Governing implicit Player diagnosis (historical design gate)

2026-09-09: **OWNER RESOLVED: PLAYER IS IMPLICIT BASELINE ROLE.** Members' default Player label is correct; no backfill or account mutation. Align View-As target resolution and View-As Live SELF lookup with otherwise-valid implicit Player semantics, retaining explicit higher/multi-role behavior, real-actor authorization and read-only security. [Twelve-part diagnosis/design](implicit-player-diagnosis-design.md); [resolved policy record](post-lms-0726-member-role-semantics.md). Minimum proposed correction requires two existing private SQL function bodies plus banner fallback; **STOP for design review before implementation/mutation**. Related normal Live identity-linkage inconsistency is documented, not silently expanded into scope. No version changed, production changes or OpenAI calls. Deferred broad security hardening remains HIGH PRIORITY / OPEN and separate. This owner-directed next item supersedes earlier deferral of the policy question.

## Current accepted production — LMS-0727 / 0.1.549

2026-09-09: **PRODUCTION ACCEPTED.** Deployment `dpl_8AoUUZpZy4iNyCkYiAAZwqWAwCN8` READY on both production origins. Normal Commissioner checks and owner normal Captain PASS; minimal shared Captain View-As and Exit PASS. Seven targeted Ask LWR cases PASS against current Rule 3.5, including both original failures and supplied-fact/division controls. All 19 business fingerprints and security catalog unchanged; no SQL/corpus/Approved Answer mutation. Seven production acceptance generations: 11,894 input / 703 output tokens, cached usage unavailable, estimated $0.08056 excluding embeddings. Full benchmark not rerun. [Final 21-item acceptance report](lms-0727-production-acceptance.md); [local review](lms-0727-cross-community-review.md). Cross-community must-fix completed. Member-role semantics and security hardening remain deferred; no next version/project starts automatically. Earlier status entries below are historical and superseded.

## Previously accepted production — LMS-0726 / 0.1.548

2026-09-09: PRODUCTION ACCEPTED. Corrected deployment dpl_39U8T1potC5w7bhUiHFwTG1cXYBb is READY on both production origins. Active-only schedule correction passed normal-first checks, owner normal Captain PASS, mixed-status View-As controls (MDUPR6 4 active/6 excluded; MDUPR7 9 active/2 excluded), valid Player available-state check and Exit/isolation. All 19 business fingerprints and security catalog unchanged. Maintenance healthy; zero agent OpenAI calls/cost. Final report: docs/lms-0726-active-schedule-production-acceptance.md. Earlier in-progress entries below are historical and superseded.

No new release starts automatically. Member-role semantics remains deferred without account mutation; the cross-community Ask LWR must-fix was subsequently completed in LMS-0727 above; broader Data API/security hardening stays HIGH PRIORITY / OPEN as a separately staged release.
LMS-0726 corrected production SQL applied once and verified; fresh 19-table fingerprints unchanged. Application deployment/normal-first acceptance in progress, NOT accepted. Permanent production-protection rule recorded. [Current review](lms-0726-corrected-production-review.md).

LMS-0726 role-drift diagnosis proven locally: non-superuser CREATEROLE automatic ADMIN-only membership tripped the original no-membership guard. Corrected SQL candidate and production-compatible fixture pass; application unchanged; no production retry. STOP FOR REVIEW. [Diagnosis, matrix, hash and continuation](lms-0726-role-drift-diagnosis.md).

LMS-0726 strict production stop: directly authorized exact migration failed its View-As read role drift guard and rolled back. No application deployment/correction/retry. 19 business fingerprints unchanged; accepted dispatcher/maintenance preserved. [Failure checkpoint](lms-0726-controlled-production-review.md).

LMS-0726 production review: Commissioner sign-in confirmed; exact migration blocked before execution by automatic approval review requiring direct authorization. Production unchanged; 19 business-table fingerprints match. No deployment/OpenAI calls. [Checkpoint](lms-0726-controlled-production-review.md).

LMS-0726 controlled production review approved: read-only identity/hash/history/maintenance checks pass; awaiting normal production sign-in before migration/deployment so normal-LMS-first checks are available. No production mutation. See [review checkpoint](lms-0726-controlled-production-review.md).

**2026-09-09 current status — LMS-0726 / 0.1.548: READY FOR CONTROLLED PRODUCTION REVIEW.** Member-directory read RPC is proven read-only and correctly classified in the fixture. Accepted/candidate/restored Member Administration, normal safe member/roster/Match Setup writes, isolated View-As, SQL removal/application rollback and zero migration-caused business-row changes verified. Final 1,010/1,010 tests; build/types/PDF/diff PASS; lint 0 errors/10 existing warnings. No application/candidate SQL changes for this correction; no deployment, production mutation or OpenAI calls. [Final report, evidence limits and production sequence](lms-0726-member-final-readiness.md). This supersedes earlier NOT READY fixture-stop entries; production acceptance remains pending.

**2026-09-09 current status — LMS-0726 / 0.1.548: NOT READY.** Commissioner count fixture fixed and PostgreSQL-verified; 1,007 tests passed; scoped migration/application rollback compatibility verified. Restored accepted Member Administration exposes a second fixture gap: `admin_member_directory_page` read RPC is rejected by the synthetic write allowlist. Comprehensive normal/rollback acceptance is incomplete. Earlier page-presence pass claims are superseded by the [current gate report](lms-0726-fixture-final-gates.md). No application/SQL change for this fixture correction; no production mutation/deployment/OpenAI calls.

**LMS-0726 FINAL SAFETY GATES — NOT READY (September 9, 2026).** [Final gate report](lms-0726-final-safety-gates.md). Exact accepted-deployment source provenance is now verified for all 300 app/config/public files. Accepted normal Commissioner baseline fails in the isolated REST fixture: nested games-played count uses unsupported `not.is.null` filtering. Stopped before rehearsal migration/candidate/rollback, per owner instruction. This is a fixture limitation, not a demonstrated LMS-0726 regression. Replace the normal-fixture dependency on the View-As query adapter with independent normal backend support, then complete normal workflows, rollback/maintenance/integrity and final checks. Candidate app/SQL unchanged; no production mutation/deployment/OpenAI calls. Deferred hardening remains separate.

**CURRENT LMS-0726 / 0.1.548 — LOCAL RESCOPED IMPLEMENTATION APPROVED; NORMAL LMS SAFETY FIRST.** Reuse actual Player/Captain/shared pages through bounded View-As effective-user reads. Preserve all normal workflows and authorization; no production SQL/deployment/OpenAI calls. The [approved parity plan](lms-0726-rescoped-parity-plan.md) is supplemented by the [normal-LMS safety gate](lms-0726-normal-lms-safety.md). Deferred foundation code/migrations/tests are preserved outside active release paths in [the hardening archive](deferred-security-hardening/README.md). No broad privilege tightening, business-data migration or normal write redesign. Normal regression evidence is required before View-As acceptance. Implementation and final validation remain in progress.

**LMS-0726 / 0.1.548 — RESCOPED LOCAL IMPLEMENTATION AND REVIEW PACKAGE (September 9, 2026).** [Implementation/evidence/rollout report](lms-0726-local-parity-acceptance.md), [exact source register](lms-0726-final-source-change-register.json). Actual shared Player/Captain/roster/Match Setup/published match/Standings/Ask UI now runs under isolated effective context. Mini presentation removed (67 duplicate lines / 12,120 bytes); accepted security retained. Four-function scoped migration plus dispatcher extension only; clean/replay/concurrency tests on PostgreSQL 17.11 pass. Full local suite 1003/1003; lint 0 errors/10 warnings; types/PDF/build/diff pass. Normal synthetic Add/Remove and paired core dashboards verified; no claim of every normal workflow receiving a new write-through browser test. **Production review remains qualified by exact accepted-artifact rollback provenance/rehearsal, not yet completed.** No production SQL, deployment, production business mutation or OpenAI calls. High-priority deferred normal Data API/security work remains separate and unresolved. This supersedes earlier implementation checkpoint status, not the deferred security evidence.


**POST-LMS-0726 MUST-FIX — Ask LWR Cross-Community Eligibility Intent/Retrieval.** [Mandatory correction and permanent acceptance controls](post-lms-0726-cross-community-eligibility.md). Owner-reported personal cross-community questions incorrectly return insufficient evidence. Return the exact active official policy immediately; distinguish it from personal eligibility requiring authorized/current community, division, own-community team and availability facts. Preserve the AND restriction, correct source badges, zero cross-league/division leakage, exact evidence, personal-data/model boundary and API cost policy. Record only: no implementation, production changes or OpenAI calls; do not interrupt or expand LMS-0726.

**NEXT AFTER VIEW-AS PARITY — HIGH-PRIORITY SECURITY HARDENING: DIRECT DATA API / NORMAL LMS AUTHORIZATION.** Preserve and address unrelated email exposure, raw RF, unrelated ratings/private team data, 292 read dependencies, 112 browser writes (82 proposed migrations / 30 retained), authoritative admission/removal/lineup transactions, notification idempotency and privilege tightening. Existing reports, inventories, proposed migrations, tests and owner policy decisions remain evidence for that separate project. See [foundation checkpoint](lms-0726-local-implementation-checkpoint.md), [Data API findings](lms-0726-final-design-gate.md), [write manifest](lms-0726-final-82-contracts.json) and [migration specification](lms-0726-final-migration-spec.md). Exposure is not claimed fixed. No release number assigned; do not start that implementation during scoped LMS-0726.

Earlier LMS-0726 roadmap entries below are historical and superseded where they require broad security hardening before parity.
# LWRPC League Management System Roadmap

**LMS-0726 / 0.1.548 — READY FOR IMPLEMENTATION (DESIGN GATE ONLY); STOP FOR REVIEW.** [Final design specification](lms-0726-final-readiness-gate.md) incorporates final owner decisions: Captain/authorized Co-Captain + LM/Commissioner removal via one bounded locked operation; Club Pro alone/Player/View-As deny; BLOCK removal until future/unplayed saved setup/lineup corrected, identically for every authorized remover; preserve completed history. No new Remove notification; existing correction/save notices are commit-bound and durably deduplicated. PT9 stays closed. Inventory:360 scoped admission controls/18 divisions,82 write contracts and notification rows,292 read mappings,14 proposed functions (six reads/four business mutations/three lock helpers/one shared eligibility evaluator). Missing required facts deterministically hold. Full Phase1/Phase2/replay/drift/cutover/rollback specs in linked artifacts. Implementation, SQL mutation, deployment, deletion and OpenAI calls have NOT occurred; actual tests/production acceptance still required after explicit implementation approval. Production remains accepted0.1.547.

**LMS-0726 / 0.1.548 — FINAL READINESS / REMOVAL CLARIFICATION; DESIGN ONLY.** [Final gate](lms-0726-final-readiness-gate.md): active guides prove documented Captain removal; owner final direction retains Captain/authorized Co-Captain removal with transactional locks and identical downstream behavior to LM/Commissioner. Club Pro alone/Player/View-As deny. Current DB DELETE bypasses roster locks: correction required, no direct DELETE retained after Phase2. PT9 remains closed. [324 condition/scope rows](lms-0726-final-admission-contracts.json), [82 operation contracts](lms-0726-final-82-contracts.json), [notification census](lms-0726-final-notification-matrix.md), [292 read mappings](lms-0726-final-read-cutover.md), [14-function migration specification](lms-0726-final-migration-spec.md). Owner question pending only for affected future/unplayed lineup handling/notice on committed removal; completed history immutable. No app changes, SQL mutation, deployment, deletion or OpenAI calls. Production remains accepted0.1.547.

**LMS-0726 / 0.1.548 — PT9 OWNER CORRECTION VERIFIED; DISCREPANCY CLOSED.** [Owner resolution and remaining readiness](lms-0726-pt9-owner-resolution.md). Read-only production returned MPT 9/WPT 9 min=3.4, max=4.8; owner explicitly approved normalized 4.8 for one-decimal Season DUPR versus current Rules page12 3.4–4.899. No precision migration needed. Prior 3.3–4.9 values are historical, not an active blocker. Generic future conflict safeguard retained; current normalized comparison passes for both, synthetic changed-bound fixtures reject. Overall CONDITIONAL: remaining structured policy/stage mappings and home-only reconciliation; lineup/copy disposition; notification ambiguity/channels; final migration ACL/specification. No app implementation, production mutation, deployment, deletion or OpenAI calls.

**LMS-0726 / 0.1.548 — FOUR-BLOCKER REVIEW; CONDITIONAL, STOP FOR REVIEW.** [Resolution report](lms-0726-four-blockers-resolution.md), [current 18-division admission matrix](lms-0726-authoritative-admission-matrix.md), [82-write operation manifest](lms-0726-82-write-operations.md). Read-only live configuration/Rules/catalog review found PT9 configured 3.3–4.9 vs current Rules 3.4–4.899; no values changed. Rules remain v20260908162017-f0aad5ad. Structured policy binding is missing; unknown required facts hold, relevant conflicts return POLICY_CONFIGURATION_CONFLICT. Remove has no admission recheck/new match restriction/notification; saved history remains. Proposed 13-function inventory includes six reads, Add/Remove/lineup save/reset and three protected lock helpers with no broad UPDATE-for-lock grants. Thirty retained paths have manager DML predicates; template policies need protected-read dependency repair and TRUNCATE grants require narrow review. Remaining decisions: policy configuration, unknown lineup/copy disposition, notification ambiguity/channels, complete migration ACLs. No implementation, SQL mutation, deployment, deletion, OpenAI or notification sends; production remains accepted 0.1.547.

**LMS-0726 / 0.1.548 — FINAL ATOMIC ADD / CUTOVER DESIGN; STOP FOR REVIEW.** [Final gate report](lms-0726-atomic-add-player-gate.md) supersedes the generic mutation/audit proposal. Separate bounded Add/Remove; unknown required facts return REVIEW_REQUIRED without membership/pending record; pair aggregate stays at Match Setup. [Exact write/cutover manifest](lms-0726-final-cutover-manifest.md): A=0, B=82, C=30, D/E/F=0; B takes precedence over manager classification. Direct assigned Club Pro mutation preserved; location-only visibility does not grant it. Remaining gates: authoritative admission source/stage mappings, serializable transaction/lock privileges, Remove/lineup clear behavior, successful-operation receipt/notification deduplication. No concurrency PASS claim. Real-LMS View-As parity only AFTER Phase 2 security acceptance; mini deletion last; Member Detail M01–M15 unchanged. Documentation only: no implementation, SQL mutation, deployment, deletion or OpenAI calls. Production remains accepted 0.1.547.

**LMS-0726 / 0.1.548 — SECURITY FOUNDATION PLAN; STOP FOR REVIEW.** [Security-foundation implementation plan](lms-0726-security-foundation-plan.md) is the current continuation. Owner selected HOLD FOR LEAGUE REVIEW for unknown required Add Player facts: no provisional roster row. Six shared read functions remain; one separate atomic normal-write function and internal audit table are proposed for review. No implementation or production mutation. [Read/write dependency manifest](lms-0726-foundation-dependencies.md): 292 read expressions; 162 write/storage/RPC expressions including 112 direct browser writes across 18 pages. Proposed two-stage additive/app/cutover sequence removes browser privileges on 11 protected tables only after replacements pass. Fifteen-minute proposed compatibility window with explicit failure/rollback; no permanent dual paths. Pair aggregate enforced at lineup stage; unknown required admission holds, no implicit manager override. No View-As UI implementation, mini deletion, production SQL, deployment or OpenAI calls; accepted production remains 0.1.547.

**LMS-0726 / 0.1.548 — DESIGN BLOCKED: DIRECT DATA API BYPASSES CONFIRMED.** [Final design-gate report](lms-0726-final-design-gate.md), [exact grants/RLS/dependencies](lms-0726-data-api-inventory.md), [isolated proof](lms-0726-direct-read-proof.json). PostgreSQL 17.11 synthetic SELECT proof: Player/Captain/Club Pro can read unrelated email, raw RF, unrelated ratings and private teams/rosters; anon sees no rows. Owner RF stop condition honored; no further probes/correction. Candidate full eligibility remains UNKNOWN without authorized complete facts; numeric rating does not prove RATED. Add Player database policy validates managed team, not independent admission rules. Six read functions alone are insufficient. Exact privilege/write-dependency cutover and trusted admission design remain required before implementation approval. No app implementation, production data reads/mutations, SQL migration, deployment, mini deletion or model calls. Production remains accepted 0.1.547.

**LMS-0726 / 0.1.548 — OWNER-DIRECTED TEAMS / CANDIDATE / ROUTE REVIEW; STOP BEFORE IMPLEMENTATION.** [Sixteen-deliverable decision report](lms-0726-review-decisions.md). Global Teams proposed LM/Commissioner-only; Captain/Co-Captain/Club Pro retain assigned-team Manage Roster. Active Rules v20260908162017-f0aad5ad rechecked: cross-community exception depends on own-community team AND roster availability; unknown facts are not invented. Candidate projection removes email/contact/raw RF and unrelated ratings, retains one rating/status and legitimate cross-community discovery. Shared server guards for implicated normal/View-As routes; six functions/two projections unchanged. Explicit unchanged/change lists and RD01–RD22 added. Previously known broad authenticated Data API read policies remain a separately documented security sign-off limitation, not fixed by route guards. No app implementation, SQL mutation, deletion, deployment or model calls; accepted production remains 0.1.547.

**LMS-0726 / 0.1.548 — READ-BOUNDARY DESIGN CONTINUATION; STOP FOR REVIEW (September 8, 2026).** [Shared page-read/database proposal](lms-0726-read-boundary-design.md), [exact current query appendix](lms-0726-current-reads.md), [explicit field matrix](lms-0726-read-fields.md), and schema-only metadata recorded. SQL is required: proposed six new server-only functions (two composable data projections), one restricted read role/private schema, and one additive existing dispatcher operation. Both normal and View-As use the same page contracts. Review G1–G3: legacy broad Captain Teams/Team Detail scope, normal cross-location roster-candidate behavior, and unguarded legacy detail/display routes. These cannot be silently changed while claiming unchanged normal parity. Member Detail button and all security/mini-removal gates retained. No app implementation, deletion, SQL mutation, deployment or OpenAI calls. Production stays accepted 0.1.547. This entry supersedes the tentative “likely SQL” statement below.


**LMS-0726 / 0.1.548 - VIEW-AS REAL LMS UI PARITY: DIAGNOSIS/DESIGN COMPLETE; STOP FOR REVIEW (September 8, 2026).** [26-part architecture and cleanup design](lms-0726-design.md), [source/route/loader/API inventory](lms-0726-inventory.md), and [role/acceptance matrix](lms-0726-acceptance-matrix.md). Preserve accepted LMS-0724 security, serve one real LMS UI with normal or secure effective-viewer adapters, then delete obsolete mini presentation after parity proof. Actual Captain Dashboard includes Match Setup/Manage Roster; full reads exceed current snapshot and likely require reviewed bounded SQL function/grant adaptation. **Member Detail entry-point requirement included:** View As User in normal action row/style, visible only to authorized REAL Commissioner/League Manager and valid target; no gap/disabled advertisement; existing confirmation/handoff preserved; desktop/390px/320px/accessibility gates M01-M15. No implementation, deletion, SQL, deployment or OpenAI calls in this diagnosis. Production/application version remains accepted LMS-0725 / 0.1.547; 0.1.548 is planned only. Review architecture and exact read-projection dependency before implementation.


**LMS-0725 / 0.1.547 - PRODUCTION ACCEPTED (September 8, 2026).** READY deployment dpl_7V7DQdKkpz8w9T39eGt6KbqeeMN8. All targeted document/hybrid/Live badge controls and Player/Manager/View-As provenance pass; effective-user SELF audit verified. Q87 clarification, Q88 Oct. 17, 2026 Saturday start and Q89 unsupported-2027 fallback PASS. Eleven normal/manager outcomes plus three View-As outcomes; no duplicates. Accepted 990/990 tests, 44/44 classification controls, 120/120 deterministic cases retained. Six answer generations; five retain 11,720 input/426 output tokens ($0.071380 uncached subtotal); View-As generation usage unavailable, embeddings excluded. No SQL/model/corpus/business mutation observed. [20-item final acceptance report, integrity limits and cost ledger](lms-0725-final-production-acceptance.md). **Next mandatory: real LMS View-As UI parity and mini-LMS removal. Not started automatically; no new version assigned.** Earlier NOT ACCEPTED entries below are historical and superseded by this completed acceptance.


**LMS-0725 / 0.1.547 - OFFICIAL RULES BADGE COMPLETENESS CORRECTED LOCALLY; STOP FOR REVIEW (September 8, 2026).** Trusted document provenance now propagates through ordinary player/View-As and manager result shapes using the existing telemetry family calculation. Manager eligibility presentation also uses the shared source label. 44/44 classification controls, 120/120 deterministic benchmark and 990/990 tests PASS; lint/type/PDF/build PASS (10 existing lint warnings). Desktop/390px/320px badge checks pass. Zero OpenAI calls, $0 model cost; no SQL/deployment/corpus/model/security changes. [14-item correction report and production continuation](lms-0725-official-rules-badge-correction.md). **Production remains NOT accepted; Q87-Q89 paused; stop before deployment.** View-As real LMS UI parity remains next mandatory only after acceptance. The preflight stop below is resolved locally, not yet in production.

**LMS-0725 / 0.1.547 — APPROVED DEPLOYMENT HALTED AT LOCAL PREFLIGHT (September 8, 2026).** The latest mandatory Season DUPR established/change policy controls correctly route to documents and telemetry `lwr`, but actual player rendering lacks the required **OFFICIAL RULES** badge. Existing 26-control assertions expect no badge for ordinary document results; prior green totals do not certify these new expectations. Confirmed with actual adapter/render code and fixture prose, zero network/model calls. **No deployment or correction performed; production remains NOT accepted on READY dpl_4ciHHSLqWSaqh3Dop3aowMKH9F7J. Q87–Q89 paused.** [16-item preflight report and bounded follow-up](lms-0725-source-classification-preflight-stop.md). No SQL, corpus, model or production mutation. View-As parity remains deferred until acceptance. Earlier local completion entry describes the previously reviewed scope.

**LMS-0725 / 0.1.547 — SOURCE CLASSIFICATION CORRECTED LOCALLY; STOP BEFORE DEPLOYMENT (September 8, 2026).** Eligibility intent is now separate from actual document/live/hybrid provenance. Shared normal/View-As badges and telemetry agree; policy-only reads no SELF data; private history exclusion is independent of the badge. 26/26 classification controls, 120/120 deterministic benchmark, 972/972 tests, lint/type/PDF/build PASS. Zero OpenAI calls; no SQL or production mutation. [15-point correction report and continuation](lms-0725-source-classification-correction.md). **Production remains NOT accepted on dpl_4ciHHSLqWSaqh3Dop3aowMKH9F7J; Q87–Q89 paused.** No View-As parity work. Earlier deployment-stop entry remains the current production finding until an approved deployment.


**LMS-0725 / 0.1.547 — RF SQL + APPLICATION DEPLOYED; ACCEPTANCE STOPPED ON POLICY LABEL (September 8, 2026).** Exact approved migration applied once as production version 20260908211419; post-migration security/data integrity PASS. Deployment dpl_4ciHHSLqWSaqh3Dop3aowMKH9F7J READY on normal/View-As origins. First SELF eligibility gate PASS (truthful unknown inputs); policy-only contrast has correct official content/no SELF read but displays hardcoded LIVE LMS DATA / ELIGIBILITY badge, violating state-versus-policy presentation gate. **STOP BEFORE CORRECTION; NOT production accepted; Q87–Q89 and remaining targeted gates paused.** Two requests, two first-attempt outcomes, zero model calls; final business/corpus/RLS integrity and maintenance healthy. [Full 21-point production report](lms-0725-eligibility-production-stop.md). No correction/rollback or View-As parity work. Earlier local-only status is historical.


**LMS-0725 / 0.1.547 — SELF-ONLY RF / ELIGIBILITY IMPLEMENTED LOCALLY; STOP FOR REVIEW (September 8, 2026).** Guarded narrow RPC operation, effective-user View-As binding, RF/independent-NR precedence, partial eligibility and exact policy citations implemented. 120/120 deterministic checks; 946/946 tests; real PostgreSQL 17.11 apply/replay/second replay/security/telemetry/rollback pass. Zero OpenAI calls. RF09 explicitly guards against invented NR failure because pair/participation facts are outside the minimum projection. [Full 25-point review, migration SHA, grants, tests and continuation](lms-0725-eligibility-implementation.md). **No production SQL or deployment; NOT production accepted; production Q87–Q89 paused.** View-As UI parity/obsolete-interface deletion remains the next mandatory item only after AI cleanup production acceptance. Earlier entries below are historical.

**LMS-0725 RELIABILITY FACTOR SECURITY REVIEW — IMPLEMENTATION PAUSED (September 8, 2026).** Owner supersedes the Season-DUPR-only eligibility design. Active Rule 4.1.1 reverified: RF strictly below 29 means NR; complete split Rule 4.5 retained. Proposed narrow SELF eligibility projection requires reviewed RPC/SQL changes; no field/RPC/RLS changes implemented. Captain/Club Pro remain SELF-only; effective target governs View-As; existing administrative access unchanged. Numeric adjusted Season DUPR can coexist with NR and is not inherently contradictory. Legacy rating RLS is broad and cannot be relied on for Ask LWR SELF isolation. [Bounded authorization/security design](lms-0725-reliability-security-design.md) covers projection, roles, policy/season binding, audit and retention. Preserve 120 question cases; add 12 proposed RF scenario controls (not executed passes). No personal-data reads/model calls/SQL mutations/deployment. **NOT PRODUCTION ACCEPTED; wait for security design review.** Q87–Q89 and View-As UI parity remain paused.

**LMS-0725 Q78 DEPLOYED; PERSONAL ELIGIBILITY DIAGNOSED; ACCEPTANCE PAUSED (September 8, 2026).** READY dpl_J1ojp2e3tgqTgx2Fm3BVLuwtHrrM. Q78 and Q79–Q86 PASS; nine first-attempt telemetry successes, Q78 55 ms, no retry/duplicates. Nine generations: 14,188 input / 398 output; USD 0.082880 uncached estimate excluding embeddings. Q87–Q89 unrun after owner stop. Exact personal DUPR5 failure retained: b2c3af0f-404a-4142-9ba8-822f22174376. Current Rules exist; classifier/table matching lacks the hybrid family. SELF_RATING supports a recorded numeric comparison but cannot certify NR/reliability or complete eligibility. Current Weekday MDUPR5/WDUPR5 identified; PT9 policy/configuration conflict recorded without mutation. [12-item diagnosis](lms-0725-eligibility-diagnosis.md); [13-item deployment checkpoint](lms-0725-q78-final-acceptance-pause.md). Permanent scope expanded 103 to 120; proposed cases not implemented/accepted. No model calls or personal-data reads for diagnosis; no new Live permission, SQL mutation, corpus/AA edit or further deployment. **NOT PRODUCTION ACCEPTED; stop before implementation.** Q55/local 921-test evidence and cost policy preserved. Next mandatory after acceptance: real LMS View-As UI parity and mini-LMS deletion; no new version automatically.

**LMS-0725 Q78 RELIABILITY CORRECTION LOCALLY VALIDATED; STOP BEFORE DEPLOYMENT (September 8, 2026).** Application-only frozen-payload recovery via Next.js after(): initial 500 ms unchanged, commit reconciliation, at most one identical retry, 1,500 ms post-response active budget, bounded final diagnostics. Real PostgreSQL 17.11 proves late-commit/concurrent outcome, occurrence, feedback/source and review-event deduplication. 921/921 tests and 15/15 injected controls PASS; lint/type/PDF/build/diff checks PASS with 10 existing lint warnings. [16-item correction report](lms-0725-q78-correction.md). No production SQL, model calls, retry/backfill, deployment or View-As parity. Historical failures intact; 11 cases unrun; NOT production accepted.

**LMS-0725 Q78 DIAGNOSIS COMPLETE; STOP FOR REVIEW (September 8, 2026).** Read-only correlation checks at 19:27:33 UTC find no Q78 outcome/occurrence/feedback/review event. Proven 500 ms application timer failure (502 ms observed), not proven database/network root cause. Deduplicated 21 successful captures: p50 52 ms, p95 417 ms, max 450 ms. [17-item diagnosis, bounded application-only reconciliation proposal and validation/continuation plan](lms-0725-q78-telemetry-diagnosis.md). No retry/backfill/SQL/deployment/model calls/correction; 11 cases remain unrun; NOT production accepted. Cost policy and deferred View-As priority preserved.

**LMS-0725 / 0.1.547 DEPLOYED; TARGETED ACCEPTANCE STOPPED AT Q78 (September 8, 2026).** Deployment dpl_9S7a2B1hMEMZ1VzF1uzAPWfVo9hm is READY on normal/View-As aliases. Five targeted correction gates PASS. Of 28 remaining cases, 17 answers correct; 16 persist successfully; Q78 capture failed DEADLINE (502 ms, attempt 1), so 11 remain unrun. 22 submissions / 21 persisted; 13 generated answers, 12 usage records; known uncached generation estimate USD 0.144615 excluding Q78/embeddings. No retry, backfill, SQL or further deployment. [14-item report](lms-0725-targeted-production-stop.md). **NOT PRODUCTION ACCEPTED; stop for telemetry review.** View-As parity deferred; no new version started.


**GOVERNING API COST POLICY (September 8, 2026).** Owner accepts the cost diagnosis. No large OpenAI benchmark without explicit approval; deterministic then affected-case-first; production GPT-5.5 unchanged. [Policy and pending fixture-only usage-accounting maintenance](api-cost-policy.md). Continue the approved targeted LMS-0725 production acceptance; no new version automatically. Numeric cached-token/category/pricing enhancements are recorded for operational maintenance without delaying this deployed correction.


**LMS-0725 COST CONTROL - LARGE MODEL RUNS PAUSED (September 8, 2026).** Read-only cost diagnosis completed without SQL, model calls or production changes. Same GPT-5.5 snapshot in production and validation. Local ledger: 597 attempts / 595 responses, estimated $8.60 uncached; production generated-answer mean 2,269 input / 116 output tokens, about 1.48 cents. Test traffic dominates the observed mix; exact organic-user and account-billed totals remain unavailable. [All 16 findings, ledger, caching limits and monthly projections](lms-0725-cost-diagnosis.md). Keep the locally validated correction intact. No further large OpenAI run before review of these findings and its proposed bounded scope/cost. No deployment/model/corpus/SQL change; View-As parity remains deferred.

**LMS-0725 / 0.1.547 - POLICY FAMILY / OBSERVABILITY LOCALLY VALIDATED; STOP BEFORE DEPLOYMENT (September 8, 2026).** 103/103 routing, 82/82 final generated benchmark answers, 12/12 policy-family controls and 906/906 tests pass. Locked/change/duration policy stays document-grounded; bounded reference-only pipeline diagnostics and safe telemetry error/correlation codes added. Q29/Q103 local omissions preserved, corrected and revalidated. 132 model attempts / 131 responses / one request failure; zero Live model data, embeddings or production questions. Required local checks pass; lint retains 10 warnings. [17-item correction report and production sequence](lms-0725-q55-correction.md). **No SQL/deployment; production remains NOT accepted, 28 cases unrun. Historical Q55/Q23/Q36 causes remain unproven; no backfill. View-As parity deferred.**

**LMS-0725 / 0.1.547 - Q55 / TELEMETRY DIAGNOSIS COMPLETE; STOP FOR REVIEW (September 8, 2026).** Q55 already routes to document policy; current Rule 4.1 exists. First observable failure is empty Stage 4 selection; completion diagnostics were discarded, so historical timeout/cause is unproven. New contrasts expose unresolved locked-policy intent and incorrect Live routing for season-change policy. Q23/Q36 delivered correct answers but persistence failed; exact build/RPC/deadline errors were discarded. Representative metadata-only payloads and replay pass isolated PostgreSQL; 24 telemetry/database tests pass. Permanent 103-question benchmark plan recorded. [14-item diagnosis and smallest application-only proposal](lms-0725-q55-telemetry-diagnosis.md). **No application/SQL/production correction or model calls; 70 production answer/routing passes, Q55 failed, 28 unrun. NOT accepted; View-As parity deferred.**

**LMS-0725 / 0.1.547 - DUPR LIMITATION DEPLOYED; ACCEPTANCE STOPPED AT Q55 (September 8, 2026).** READY dpl_4m9BT41z6J2fGpthJpmHYHo2VSyy. Generic DUPR limitation, supported choices and resolved/missing-data production retest pass. Cumulative answer/routing observations: 70 pass, Q55 fails (When is my Season DUPR established?), 28 unrun. Q23/Q36 correct answers have two persistence failures, discovered at final reconciliation. Local 882 tests and all required checks pass. Corpus/Approved Answers/security/history unchanged; one concurrent team and two role additions with all prior rows unchanged. [Full 16-item report and evidence](lms-0725-dupr-production-stop.md). **NOT production accepted; no correction after stop; View-As parity deferred.** Earlier entries below are historical.

**LMS-0725 / 0.1.547 - DEPLOYED; ACCEPTANCE STOPPED BEFORE CORRECTION (September 8, 2026).** READY deployment dpl_6ajhMvN7dTuRaZXJaDP3RrPM92dL. 22 production generated answers passed, including all 10 PrimeTime age/event controls. Q50 combined DUPR clarification omits the explicitly required current-official-DUPR limitation: required gate FAIL. 76 benchmark cases not run. Corpus, Approved Answers, business/identity/security and historical outcomes unchanged; 23 requests and 2 intentional feedback votes. [Full 27-item report and evidence](lms-0725-final-production-stop.md). **NOT production accepted.** No correction after stop, no new SQL/version or View-As parity work. Earlier entries below are historical.

**LMS-0725 / 0.1.547 — AGE-REFERENCE CORRECTION VALIDATED; AUTHORIZED DEPLOYMENT / ACCEPTANCE IN PROGRESS (September 8, 2026).** Current Rule 6.3 now supplies PrimeTime age-reference policy, separate from season-start events. All prior cases retained: 99/99 cases, 78/78 generated answers, 872/872 tests and required checks pass. 79 additional local model calls, all completed. [Correction and evidence](lms-0725-age-reference-correction.md). No SQL/corpus/Approved Answer/model configuration change. NOT production accepted; stop before correction on material production failure. View-As parity remains deferred. Earlier entries below are historical.

**LMS-0725 / 0.1.547 — LEAGUE DATES + COMPACT HELP VALIDATED LOCALLY; STOP FOR REVIEW (September 8, 2026).** Both Women's Weekday failures now return Oct. 14, 2026 with exact Important Dates evidence. The initial Ask LWR screen stays compact; `? What can I ask?` opens accessible, actionable grouped help. Final current-source benchmark: 89/89 routes, 68/68 generated answers (45 existing + 23 new); 857/857 tests and required checks pass. Desktop/390px/320px keyboard, focus, examples and effective-user View-As transport checks pass. Owner confirmed Rules upload `f0aad5ad-cf08-46c2-94fd-686ceb1271c0`; refreshed source validation includes it. **223 local model request attempts (222 completed responses + 1 network failure)**, including preserved intermediate runs/rechecks; earlier correction ledgers remain separate. [Full 15-point report, evidence, ledger and production sequence](lms-0725-league-dates-and-help.md).

The preceding approved corrected deployment `dpl_EbnsLmMufCfuaTWZR8MWteji6B7y` is READY; three targeted production questions passed before the owner paused acceptance for these new findings. [Deployment and pause record](lms-0725-corrected-production-pause.md). **The new dates/help correction has NOT been deployed; LMS-0725 remains NOT production accepted.** No new migration, corpus processing, Approved Answer or View-As parity change. View-As UI parity/obsolete presentation deletion remains the next mandatory work only after AI cleanup production acceptance. Earlier entries below are historical.

**LMS-0725 / 0.1.547 — FINAL LOCAL CORRECTION PASS; STOP FOR REVIEW (September 8, 2026).** Shared exact-range evidence gate closes Q57 and other synthetic passage paths; grounded period metadata, bounded current-source completion and material-requirement generation contract corrected. Final run: 63/63 routes, 45/45 generated answers, 0 observed source/scope/year/material-completeness failures; 826/826 automated tests and all required checks pass. **121 authorized model calls in this correction**, including diagnosed intermediate failures and rechecks; the prior 42 calls are separate. [Full correction report, call ledger, source audit, performance and production sequence](lms-0725-final-correction.md). No deployment, SQL, corpus reprocessing, Approved Answer or View-As parity work. Deployed LMS-0725 remains NOT production accepted; LMS-0724 is last accepted. Next mandatory work remains View-As UI parity only after AI cleanup production acceptance. Earlier status entries below are historical.

**LMS-0725 / 0.1.547 — AUTHORIZED MODEL VALIDATION COMPLETE; QUALITY GATES NOT MET; STOP FOR REVIEW (September 8, 2026).** 42 calls to the existing configured OpenAI model; 63/63 routes pass; 0 observed cross-league leakage or mechanics-as-applicability errors. Roster/scoring correction generates supported core answers, but exact year wording is inconsistent; Q11/Q29/Q46/Q54–Q56 have clarification/evidence/completeness gaps. **Q57 reveals another legacy synthetic heading/date passage: accepted by the existing compatibility validator, fails strict literal source fidelity (41/42 pass).** Runner now blocks it before future dispatch. [Full model results, privacy, failed cases and next steps](lms-0725-authorized-model-validation.md). No app changes, deployment, SQL, corpus changes or View-As parity work in this validation turn. Production remains deployed but not accepted. Earlier authorization-block statuses are superseded; earlier implementation history is retained.

**LMS-0725 / 0.1.547 — EXACT-EVIDENCE CORRECTION IMPLEMENTED LOCALLY; STOP BEFORE PRODUCTION (September 8, 2026).** Separate exact excerpt ranges and verified scope metadata replace synthetic passages; strong validator unchanged. 820 tests and 70 focused fidelity tests pass; lint/type/PDF/build pass. Local 63-case routing/source-gate audit validates selected sources in 44 document cases; it is not a 63-answer production pass. Live model validation is pending explicit authorization after automatic approval review blocked sending saved non-public official excerpts to OpenAI. [Exact files, before/after, evidence, coverage limits and proposed sequence](lms-0725-exact-evidence-correction.md). No deployment/SQL/corpus/Approved Answer changes. Production remains deployed but NOT accepted (1 failed / 62 not run). View-As UI parity remains deferred until AI cleanup production acceptance. Earlier status entries are historical.

**LMS-0725 / 0.1.547 — HTTP 500 DIAGNOSIS COMPLETE; STOP FOR REVIEW (September 8, 2026).** The new policy selector synthesizes noncontiguous heading/passage text that the existing citation-source validator rejects. Exact blocker reproduced through unchanged functions and current official source metadata; affects roster dates and scoped scoring selections. Source remains active; routing correctly selects document policy/date, not TEAM_ROSTER. No code/SQL/deployment/corpus correction or production replay in diagnosis. [Proven defect, evidence limits and smallest correction plan](lms-0725-http500-diagnosis.md). Release remains deployed, not accepted; production benchmark remains 1 failed / 62 not run.

**LMS-0725 / 0.1.547 — DEPLOYED; NOT PRODUCTION ACCEPTED; STOP BEFORE CORRECTION (September 8, 2026).** Exact reviewed SQL applied once as production migration `20260908123627`; security/data integrity checks pass. Deployment `dpl_3BDCFztdq2rX1CN2JV9fT7sUv4zc` is READY and live. First mandatory roster-date question returned HTTP 500 / technical_error, no date or citation; acceptance stopped immediately. Benchmark: 1 failed, 62 not run. No retry, correction or rollback. [Production evidence, timings and remaining gates](lms-0725-production-acceptance.md). The earlier no-SQL scope stop is superseded by owner clarification. LMS-0724 remains the last accepted release; View-As UI parity stays deferred.

**LMS-0725 / 0.1.547 — PRODUCTION PREFLIGHT STOP (September 8, 2026).** Latest deployment approval prohibits SQL, while the reviewed implementation requires a guarded lookup migration for combined choices and roster counts. No deployment or production changes attempted. [Exact dependency and review decision](lms-0725-production-preflight-stop.md). Resolve this scope conflict before proceeding; LMS-0724 remains the recorded accepted production baseline.

**LMS-0725 / 0.1.547 — IMPLEMENTED; LOCAL VALIDATION COMPLETE; STOP BEFORE PRODUCTION (September 8, 2026).** [Implementation report, exact files, results and controlled acceptance sequence](lms-0725-implementation-report.md). Approved generic intent, official applicability and clarification corrections implemented. Mandatory local benchmark: 63/63 route matches, 0 mismatches; full suite: 750/750 pass; lint (6 existing warnings), typecheck, PDF server bundle and production build pass. Review-only guarded SQL and rollback tested in an isolated database. No production deployment, SQL application, corpus reprocessing or Approved Answers. Exact production replay and request overhead remain acceptance gates. LMS-0724 / 0.1.546 remains the accepted production baseline. View-As UI parity remains the next mandatory work after LMS-0725 production acceptance.

## Next mandatory work after AI cleanup production acceptance — View As User UI parity / cleanup

**Owner priority recorded September 8, 2026. Future work; do not implement during the current Ask LWR / AI quality cleanup.** Immediately after the current AI cleanup release is completed and production accepted, return to the accepted LMS-0724 View As User feature for its product-parity correction. This is the next mandatory work item; it does not interrupt or expand the current LMS-0725 cleanup. No new release number is assigned by this entry.

Required outcome:

- Preserve the accepted LMS-0724 security infrastructure and secure target-effective authorization.
- Replace the separate simplified View-As mini-LMS presentation with the **same existing LMS UI/screens the target user normally sees**, running under the secure View-As effective-user context.
- Retain the persistent **VIEWING AS / READ-ONLY** banner and Exit control.
- Preserve all server-side mutation blocking and dedicated-origin isolation; presentation reuse must not broaden the target's authority or enable writes.
- Validate parity of the existing screens/navigation and target-effective data, alongside security regression checks, before removing the obsolete presentation.
- After parity is validated, **delete obsolete parallel View-As presentation routes, components, navigation, data loaders, CSS and presentation-specific tests**. Preserve or adapt security tests that remain necessary to verify the accepted infrastructure. Do not retain two LMS interfaces.

Final architecture: **ONE LMS UI**, operating with either normal-user context or secure View-As effective-user context. Completion requires both validated parity and removal of the obsolete mini-LMS presentation, not merely adding the real-LMS presentation alongside it.

**FINAL STATUS — LMS-0724 / 0.1.546 — PRODUCTION ACCEPTED (September 8, 2026).** Remaining testable gates passed. Independent Player session unavailable; blank embedded PDF reproduces in a control without LMS security headers while the identical PDF renders top-level. Measured timings and unavailable subdivisions are explicit. Maintenance: 36 successes/0 failures at the final 11:17 UTC checkpoint; no active or unsanitized ended contexts. Integrity verified, with concurrent team/Captain registration accounted for. [Final acceptance report](lms-0724-final-production-acceptance.md). PRODUCTION PROBE BLOCKED BY SAFETY CONTROL retained. No next version or redeployment. Entries below are historical unless explicitly identified as future work.

**Latest September 8, 11:04 UTC — signed-in acceptance resumed:** valid Player preview survived natural maintenance tick with credentials/metadata intact; explicit Exit sanitized it and retained audit, zero active contexts afterward. Job 3: 23 successes/0 failures. Commissioner session retained. In-app PDF still blank; independent normal Player, normal-browser PDF and detailed timing evidence remain pending. Sign-in blocker resolved; LMS-0724 remains NOT accepted, LMS-0723 last accepted. [Evidence](lms-0724-maintenance-correction.md). No new version or deployment.

**CURRENT September 8, 2026 — LMS-0724 maintenance correction VERIFIED; NOT PRODUCTION ACCEPTED.** Explicitly approved exact schedule created once: job 3, `lms0724-view-as-maintenance`, every minute, postgres. Natural runs 8304/8305 succeeded, expired ciphertext/code/context erased, all three context metadata fingerprints preserved, one expiration audit added with no repeat mutation. No grants/RLS/function/application/deployment change. Focused isolated tests: 18/18 pass. Browser continuation reached inactivity sign-out; owner sign-in, legitimate normal-Player session, normal-browser PDF verification and remaining timing/broader gates are pending. [Full evidence and exact SQL](lms-0724-maintenance-correction.md). LMS-0723 / 0.1.545 remains last accepted. PRODUCTION PROBE BLOCKED BY SAFETY CONTROL retained. No next version. Older status entries below are historical.

**LMS-0724 maintenance diagnosis — READ ONLY COMPLETE; WAIT FOR APPROVAL.** Existing protected cleanup function is deployed; periodic schedule was never created in observed production setup. pg_cron is enabled, zero matching jobs/runs, one expired context retains ciphertext awaiting cleanup. Proposed smallest correction: one guarded every-minute database-local job calling the existing function, with isolated unattended-expiry/retention coverage and scheduled production verification. [Diagnosis and review-only SQL](lms-0724-maintenance-diagnosis.md). No production mutation, manual cleanup, code change, deployment or version increment in this pass. LMS-0724 / 0.1.546 remains NOT accepted; other pending gates remain recorded.

**CURRENT LMS-0724 / 0.1.546 — LOCK CORRECTION APPLIED; NOT PRODUCTION ACCEPTED.** Exact approved correction applied once as production migration history `20260908014218`. SELF missing-rating and cross-player denial pass; helper permissions, unchanged RLS and seven removed UPDATE privileges verified. Representative Rally qualification and PrimeTime scope pass. STOP: no observed periodic View-As maintenance schedule; expired context ciphertext awaits cleanup. No further scheduler/SQL change attempted. PDF visual confirmation and remaining gates are recorded separately. [Production continuation and blocker](lms-0724-production-acceptance.md). LMS-0723 / 0.1.545 remains last accepted. No app redeployment/version change. Earlier entries below are historical.


**Historical LMS-0724 stop — Live SELF gate 24 (subsequently corrected):** original executor FOR SHARE/RLS denial was diagnosed, reviewed and corrected by the exact separately approved migration above. Preserve this history; the current blocker is maintenance scheduling, not SELF authorization. [Production evidence](lms-0724-production-acceptance.md).

**CURRENT LMS-0724 — production migration blocked by automatic approval review.** DNS/HTTPS and three approved production View-As variables are configured. Exact migration rejected before execution twice; no migration or application deployment. [Completed setup and review block](lms-0724-production-review-block.md). Production application remains LMS-0723 / 0.1.545; do not regenerate the configured encryption key on resume.

**CURRENT LMS-0724 production continuation — DNS GATE:** final hash verified; approved view-as.lwrpickleballclub.com attached to lwrpc-admin in Vercel. Bluehost DNS is missing (A view-as → 76.76.21.21). No migration, new environment/key configuration or application deployment yet; LMS-0723 / 0.1.545 remains accepted production. [Preflight, domain action and resume point](lms-0724-production-dns-gate.md).

**CURRENT LMS-0724 / 0.1.546 — replay correction validated locally; STOP BEFORE PRODUCTION.** Exact-state no-op dispatcher replay preserves executor ownership; non-superuser clean apply/two replays/partial recovery/drift/concurrency pass. 666 tests and isolated build pass. [Final correction, hash and validation](lms-0724-replay-ownership-correction.md). No production changes; LMS-0723 / 0.1.545 remains accepted. Earlier blocker/status entries below are historical.

**LMS-0724 correction status — STOP FOR REVIEW:** home_location_id SQL/grant/fixture correction and schema/Club Pro tests pass locally. Non-superuser initial migration succeeds, but replay fails: must be owner of function lms_view_as. No further security correction or production changes. [Full correction and validation report](lms-0724-home-location-correction.md). Earlier preflight and validation statements below are historical.

**Production preflight STOP (2026-09-07):** reviewed View-As SQL references nonexistent `teams.location_id`; production uses `teams.home_location_id`. Fixture mismatch masked the incompatibility. No migration, domain/configuration, deployment or production data mutation attempted. [Evidence and proposed bounded correction](lms-0724-production-preflight-stop.md). LMS-0723 / 0.1.545 remains accepted production; LMS-0724 awaits review.

## MUST-FIX after LMS-0724 — Weekday Rally Scoring applicability (2026-09-07)

Classification: **AI/Retrieval Review — scoring-method applicability**, not missing knowledge or Approved Answer. Immediate post-LMS-0724 AI quality/routing refinement, alongside Live rating clarification UX, clickable choices, resolved-question display, natural missing-rating wording and roster-entry policy/date routing. Record only: no production trace, corpus inspection, implementation or version assignment in this pass. Do not interrupt LMS-0724 View As User acceptance unless evidence establishes an LMS-0724 regression; none is established by this recording. Do not implement before LMS-0724 acceptance unless separately authorized.

Owner-reported production question: `Does the weekday dupr league use rally scoring`. The incorrect answer says Yes and describes general Rally Scoring mechanics, including a point every rally and the serving requirement for the winning point. Its two Official Sources reportedly come from the same page 15 League Managers material. Mechanics and a correct winning-point qualification do not establish whether that scoring method applies to Weekday.

Owner-confirmed current Rule 5.3, **Point Scoring Methods**, states:

> Unless otherwise specifically stated, all games shall use Standard Scoring as defined by the current USA Pickleball (USAP) Rules.
>
> Rally Scoring shall be used only when a particular game or match format expressly identifies Rally Scoring as the required scoring method.

The owner confirms that the general Weekday section does not establish Rally Scoring; the identified exception is **Weekday 9.1's Picklebreaker, if played**. This is owner-supplied policy/evidence, not an independently verified active-version finding in this documentation pass. During diagnosis verify the exact active Rule 5.3 and Weekday 9.1 exception, document/version, rule identity and page before implementation. Do not assume historical policy remains current.

Expected answer substance from those controlling provisions: **No, general Weekday games use Standard Scoring unless their applicable format expressly states otherwise; Weekday 9.1 has a Rally Scoring exception for its Picklebreaker, if played.** Derive actual wording from active evidence, never hardcode the answer or score policy. Do not promote the Picklebreaker exception to all Weekday 9.1 games, all Weekday divisions or other leagues.

Core generic distinction:

- **Mechanics:** “How does Rally Scoring work?” needs applicable scoring instructions and material qualifications.
- **Applicability:** “Does [league/division/game] use Rally Scoring?” needs the controlling default plus the specific league/division/match-format/game provisions. An affirmative answer requires an express applicable relationship; terminology overlap with mechanics is insufficient.
- Express applicable Rally Scoring requirement → Yes. Express Standard Scoring requirement → No / Standard Scoring. Explicit default plus review of applicable format provisions with no express exception → Standard Scoring. Do not generalize this into “no retrieved evidence means No”; the conclusion depends on the explicit governing default and adequate review of applicable exceptions.

Future diagnosis must trace raw/effective question, detected question type/intent and league/division/game scope; Stage 3 candidate ranks/scores (including Rule 5.3, Weekday format and Rally mechanics); Stage 4 applicability and selected/excluded evidence with reasons; evidence actually supplied to the model; generated propositions; and citation construction. Explain specifically why Rule 5.3 was not selected/supplied. Compare with the accepted broad-rule/material-qualification correction: this is related but distinct, because the default plus express-exception structure governs whether mechanics apply at all.

Preserve scope at **league → division → match format → game** throughout selection, generation and citation. Applicability answers should lead with the governing default and applicable exception at its exact scope, not mechanics. Cite Rule 5.3 and the exact Weekday 9.1 exception when discussed; duplicate mechanics passages cannot substitute for applicability evidence.

Required future MUST-PASS controls:

- `Does the Weekday DUPR League use Rally Scoring?`
- `Does the Weekday League use Rally Scoring?`
- `What scoring method does the Weekday League use?`
- `Do regular Weekday games use Rally Scoring?`
- `Does Weekday 9.1 use Rally Scoring?`
- `Does the Weekday 9.1 Picklebreaker use Rally Scoring?`
- `How does Rally Scoring work?`
- `How does Rally Scoring work in a Picklebreaker?`
- `Do I have to be serving to win with Rally Scoring?`

Add paired Saturday, PrimeTime and Weekday applicability controls using each league's current controlling provisions independently. Assert both correct supported propositions and absence of another league's/game's provisions: **Cross-League Leakage = 0**. Saturday/PrimeTime mechanics or format rules must not establish Weekday applicability; the Weekday 9.1 Picklebreaker exception must not become division-wide policy. Preserve the accepted winning-point serving qualification for mechanics questions.

No Approved Answer, PDF/corpus edit or policy invention. If active official Rules contain the default and exception, correct retrieval/applicability/selection generically under subsequent authorization. This finding does not reopen or replace completed historical benchmark evidence; it adds a new applicability MUST-FIX control for the immediate follow-up release.

## Deferred Live LMS UX refinement — DUPR/rating clarification (2026-09-07)

Owner-requested follow-up; do not interrupt LMS-0724 View As User. No version assigned, implementation or production change in this recording pass. The existing shared Live service owns serial rating/context clarification and receipt choices (app/lib/liveLmsService.js); generic clarification/missing messages are in app/lib/liveLmsIntent.js. This is more than a View-As-only presentation edit: combined authorized options, receipt interpretation and player-facing resolved-question presentation require coordinated shared-flow work. Keep it separate from the completed local View-As boundary implementation.

Observed flow: “What's my DUPR” → rating-type clarification → “Season DUPR” → numbered season clarification (2026 Fall Season / 26/27 Saturday Season) → “1” → generic missing-value message. These names are owner-reported examples, not hardcoded options or a verified current data inventory.

Required future behavior and acceptance controls:

- When authorized types and season contexts are already known, combine clarification into one useful grouped choice: “Which DUPR rating would you like?” with Season DUPR and PrimeTime Season DUPR groups and their applicable authorized contexts. Do not imply a stored value exists simply because its context is selectable.
- Render bounded choices as accessible buttons/chips, operable by keyboard with visible focus. Retain safely resolved typed numbers, unambiguous aliases such as “Fall,” and season names. Ambiguous text must still clarify.
- Generate choices only from server-authorized contexts. Treat each selection as untrusted input; reauthorize and refetch. Never reveal another player's seasons/ratings or broaden existing role/relationship access.
- Preserve raw user input for existing permitted diagnostics, while presenting its resolved meaning to the player: “What's my Season DUPR for the 2026 Fall Season?” or “Season DUPR” plus “Selected context: 2026 Fall Season.” Do not display a final substantive question consisting only of “1.” Do not expand live-data telemetry retention.
- Use deterministic capability/context-specific missing-value language after successful authorization and context resolution: “You don't currently have a Season DUPR recorded for the 2026 Fall Season.” PrimeTime equivalent: “You don't currently have a PrimeTime Season DUPR recorded for [season].” Use only authorized context information and appropriate subject wording; preserve denials and technical errors as distinct outcomes.
- Preserve the limitation: “Current official DUPR is not currently available through this Live LMS lookup.” Never substitute Season DUPR for official DUPR.
- New Question clears pending rating/context choices and their protected follow-up state. A stale choice must not restore a cleared lookup.
- After success or a missing value, “What about Saturday?” may reuse rating intent only through the existing protected follow-up architecture. Resolve ambiguity safely, reauthorize and refetch; no stale rating reuse or fallback to another subject.
- Answer-model calls = 0; embedding calls = 0. No live rating, season context, or personal data sent to a model. Authorization, rating definitions, NULL semantics and normal privacy boundaries remain unchanged.

Regression matrix: combined vs unavailable/ambiguous contexts; each authorized rating type; click/keyboard/typed equivalence; resolved display with raw wording preserved under existing privacy rules; NULL vs denied vs technical error; official-DUPR limitation; forged/stale/cross-user choices; relationship/role loss between clarification and selection; New Question reset; successful and missing-result Saturday follow-ups; zero model/embedding invocations. Include ordinary Ask LWR and isolated View-As contexts without allowing receipts or choices to cross their security boundaries.

**CURRENT LOCAL — LMS-0724 / 0.1.546 View As User implementation and isolated validation.** Dedicated origin approved; actor-wide locks are superseded. [Implementation/security report and controlled deployment sequence](lms-0724-implementation-report.md). No production changes; LMS-0723 / 0.1.545 remains production accepted. Earlier View-As stop/design entries below are historical.

**LMS-0724 implementation approval — isolated-tab security boundary STOP.** Owner selected independent tabs and mandatory event-code endpoint protection, superseding the actor-wide recommendation. [Concrete request-omission boundary and proposed dedicated-origin resolution](lms-0724-isolated-tab-security-boundary.md). No View-As code/SQL/version/deployment changes. Need confirmation of isolated renderer/server-mediated real-actor authentication before implementation can satisfy the direct-write invariant. Existing member-import correction is separate.

**Member location follow-up (2026-09-07):** Approved 11 missing Esplanade links repaired in production; 13 eligible captain options now linked. Indigo conflict held; 66 additional exact-match missing links and seven unresolved names identified, unchanged. Import correction implemented locally with location snapshot guards and six regression tests; not deployed. [Repair and validation report](member-location-repair-and-import-correction.md). Separate from View As User; no version/schema change.

**LMS-0724 / 0.1.546 — View As User design complete; STOP FOR REVIEW.** [Architecture/security design covering owner requirements 1–121](lms-0724-view-as-user-design.md). Recommends a 30-minute opaque server context, separate real actor/effective user, target-effective server readers, actor-wide read-only enforcement including direct database paths, and isolated AI diagnostic telemetry. Multi-tab scope and independent event-code mutation boundary require approval before implementation. No code/SQL/production/deployment/version changes in this design pass. LMS-0723 / 0.1.545 remains production accepted/current.

**Post-LMS-0723 manager request (2026-09-07):** Teams By Division count/name toggle implemented locally, not deployed. Read-only captain-picker diagnosis confirmed 14 Esplanade at the Heights display names but only two matching location links; 11 NULL links and one Indigo-linked conflict. No member data changed. [Findings, safe repair recommendation and validation](teams-division-sort-and-location-diagnosis.md).

**CURRENT — LMS-0723 / 0.1.545 PRODUCTION ACCEPTED (2026-09-07).** Exact approved 16-candidate repair completed and replayed safely; coordination migration applied once; commit adb4389d9c2a88501a907fe46e6fcbdb46c31002 deployed. All currently testable acceptance gates passed. Owner confirmed concurrent member edits and registration activity as intentional. Real roster/match and logout-revocation limitations remain explicitly recorded. [Final production evidence and limitations](lms-0723-manifest-production-acceptance.md). No new version or View As User work started. Earlier status entries below are historical and superseded by this result.

**CURRENT — Fresh read-only identity review complete; proposed protected manifest awaiting approval.** [Review, checksum, counts and repair procedure](lms-0723-fresh-identity-manifest-review.md). 176 Auth accounts; 15 proposed existing-role links plus one identical split (16 total); 107 no-role deferred. Original historical cohort is not reconstructed. Existing migration cap is 14 links + 1 split and remains unchanged; scope-limit decision required before execution. Manifest saved encrypted outside repository. No production migration/repair/deployment; LMS-0723 / 0.1.545 remains NOT production accepted.

**CURRENT — Controlled production identity continuation stopped before mutation:** the original 15-candidate protected manifest/reviewed state was not retained in the earlier aggregate dry run. [Preflight findings and required recovery/re-review](lms-0723-identity-production-preflight-stop.md). Session migration remains applied; coordination migration remains unapplied. No repair or deployment. LMS-0723 / 0.1.545 remains NOT production accepted.

**CURRENT — Shared identity coordination implemented locally and validated; STOP BEFORE PRODUCTION.** The owner approved the bounded app-owned Auth coordination hook. [Implementation, 19-part review and controlled continuation plan](lms-0723-identity-coordination-implementation.md). Original 14 links + 1 identical split only; 107 no-role accounts deferred. LMS-0723 / 0.1.545 remains deployed, NOT production accepted. No production repair, migration or deployment. Earlier stop/design entries below are historical.

**LATEST — identity repair implementation STOP on demonstrated concurrency defect.** [Real PostgreSQL multi-session evidence](lms-0723-identity-concurrency-stop.md): same-identity/consolidation/no-op tests passed, but a duplicate candidate and repair both committed after replacing now-prohibited broad table locks with row/advisory locks, including SERIALIZABLE repair against a legacy READ COMMITTED writer. An exact reviewed-role-state guard is also needed. Shared-writer coordination requires bounded design review before implementation can resume. Only the 15 approved identities remain in scope; 107 no-role accounts are deferred. No production mutation, migration or deployment. LMS-0723 / 0.1.545 remains NOT production accepted.

**Identity-link repair design/read-only dry run complete — STOP FOR REVIEW.** [Design, exclusive 172-account classification, SQL proposal and 30 synthetic checks](lms-0723-identity-link-repair-design.md). 15 link-only candidates (14 existing member-role links + 1 identical Commissioner split), 39 verified complete no-ops, 118 held including 107 requiring a role-provisioning policy. The split needs audited consolidation to preserve existing single-row role readers; update-only would create a duplicate. No production identity/schema/ACL mutation, backup export or deployment. LMS-0723 / 0.1.545 remains deployed, NOT production accepted.

**Read-only SELF_RATING diagnosis complete:** acceptance identity is split across an Auth-only Commissioner role row (member_id NULL) and a member-only Commissioner row (user_id NULL). The immutable member INNER JOIN returns no row. [Exact cause, population counts and proposed bounded link correction](lms-0723-self-rating-authorization-diagnosis.md). No linking/code/SQL/deployment change performed. LMS-0723 / 0.1.545 remains NOT production accepted.

**LATEST: session redesign migrated and deployed; first SELF_RATING gate DENIED — STOP.** Corrective recorder 20260907132549; READY commit a2d4af0. No further tests/correction after the first self question returned an access denial. [Production evidence and full gate disposition](lms-0723-session-redesign-production-acceptance.md). LMS-0723 / 0.1.545 remains NOT production accepted. Concurrent intentional registration is permitted; earlier registration-baseline stop is superseded.

**Controlled session-redesign preflight STOP before mutation:** two team creations and five updated team rows since the prior acceptance stop require confirmation of the intentional current registration baseline. [Read-only evidence](lms-0723-session-redesign-production-preflight-stop.md). Corrective migration remains unapplied; no redeployment. LMS-0723 / 0.1.545 remains NOT production accepted.

**Session redesign implemented locally; STOP for review before corrective migration/deployment.** [Implementation, security evidence and revocation limitations](lms-0723-session-validation-implementation.md). LMS-0723 / 0.1.545 remains deployed and NOT production accepted; LMS-0722 remains the accepted baseline. Earlier design-only/local status entries are historical.

**2026-09-07 session redesign: diagnosis/design complete; awaiting review.** [Trusted server-auth boundary and exact corrective SQL proposal](lms-0723-session-validation-redesign.md). No implementation, SQL application, permission/RLS change or deployment in this pass. LMS-0723 / 0.1.545 remains deployed, NOT production accepted; LMS-0722 / 0.1.544 remains the accepted baseline. Online revocation and session-lifetime semantics are explicit validation gates. Earlier stop/local-deployment statements below are historical.

Last updated: 2026-09-07

## LMS-0723 / 0.1.545 — deployed; production acceptance blocked

**Auth correction review STOP:** production auth.sessions has RLS enabled, no policies, and the constrained reader lacks ownership/bypass. The approved USAGE-only correction is insufficient; no mutation occurred. [Evidence and required design review](lms-0723-auth-rls-boundary-stop.md).

**Current status:** exact migration applied once, d7da5f6 deployed READY. First live self-rating returned technical_error because the constrained session reader lacks auth schema USAGE; read-only helper probe confirms permission denied. STOP before correction. [Acceptance matrix and diagnosis](lms-0723-production-session-permission-stop.md). No rollback; last accepted release remains LMS-0722. Seasonal empty rosters/matches are owner-accepted limitations, with first legitimate roster/match verification obligations. Earlier local/preflight notes below are historical.

**Controlled-production preflight STOP:** privileged read-only verification found zero roster memberships, zero authorized Captain-to-roster-player pairs, and zero matches. Required positive production fixtures are unavailable; no migration/deployment performed. [Prerequisite evidence and pending gates](lms-0723-production-prerequisite-stop.md).

**Subject-resolution correction implemented locally; STOP FOR OWNER REVIEW.** Both named-person rating and team fallback defects now have permanent synthetic regressions. The router carries explicit subject state; the protected RPC resolves/authorizes named subjects before field retrieval and never defaults a failed explicit request to self. [Correction, exact migration and validation](lms-0723-subject-resolution-report.md). No production migration/deployment performed.

The owner-approved six deterministic capabilities are implemented locally: SELF_RATING, PLAYER_RATING, PLAYER_CONTACT (email only), SELF_TEAM/TEAM_IDENTITY, TEAM_ROSTER and NEXT_MATCH. The protected server uses verified Auth user ID, current session and database-derived relationships; no live facts enter an answer/embedding model, corpus or document-feedback snapshot. Player self/own-team access, explicit managed-roster Captain/co-captain/team-Pro access, bounded manager lookup, sanitized live feedback/review, durable audit/limits and private retention are implemented. Location-only Pro and unsupported/bulk/historical/eligibility lookups remain deferred.

[Implementation and migration review](lms-0723-implementation-report.md). Subject-correction local validation: 633 tests, lint with six existing warnings, standalone type check, PDF bundle verification and isolated production build; normal build compiled then hit the known type-cache lock. **No production migration or deployment has been performed; production remains accepted LMS-0722 / 0.1.544.** Existing legacy broad reads and email-role inconsistencies outside the live boundary remain separate review items. No legitimate document activation occurred; the accepted activation-history limitation remains. No subsequent Live LMS phase has started.

## LMS-0722 / 0.1.544 — production accepted

LMS-0722 is production accepted after the owner-authorized material-qualification correction and per-document collapsed version-history UI. Corrected commit 42778fd, deployment dpl_F1A5PEKbyNdPjqfz7RHD9QkdPHdy READY. 614 automated tests, type/lint/PDF checks and isolated clean build passed; first live Picklebreaker Rally Scoring retest includes the serving/game-winning-point qualification. Required player-count/format controls and 28-question configured-model acceptance run passed, Cross-League Leakage = 0. [Complete acceptance evidence and limitations](lms-0722-production-acceptance.md). Activation-history migration was applied once; no real activation occurred, so the next legitimate live activation is the expressly accepted limitation. Historical values remain Unknown. No next version or Live LMS Intelligence started.

**PrimeTime Picklebreaker source conflict RESOLVED BY OWNER SOURCE CORRECTION.** Current Rules v20260907001227-e4d9bf77 consistently specify 15 by 2 Rally. Prior versions are superseded and retained. [Final source verification](lms-0722-final-source-verification.md). No application score hardcoding; isolated equal-authority conflict coverage remains required.

The four basic PrimeTime/Saturday/Weekday/Weekday-9.1 player-count questions and paired PrimeTime/Saturday game-format/Picklebreaker controls are MUST-PASS production acceptance gates. Scope belongs to numbered passages; the flattened multi-league summary is not governing evidence for a single league.

Activation history is REQUIRED: nullable per-version timestamp and member actor; protected atomic activation/supersession/provenance, local-time display, truthful Unknown for historical activations including the current Rules. Production acceptance requires migration/security/UI checks and a legitimate next activation, or an explicitly retained live-check limitation if none occurs. No fake production document may be activated for testing.

The [diagnosis](lms-0722-quality-hardening-diagnosis.md) and [historical traces](lms-0722-trace-appendix.md) remain provenance, not current-corpus results. General partial-answer semantics, competing-supplement redesign and Live LMS Intelligence remain deferred. Stage 7 semantics and managed threshold .65 remain unchanged.

## Deferred AI quality finding — explicit apparel object resolution

Recorded 2026-09-06 from owner-reported production behavior; diagnosis has not yet been performed. Classification: **conversation/intent/object resolution**, not an LMS-0721 Approved Answers defect. LMS-0721 / 0.1.543 remains production accepted. This item is for future bounded diagnosis, not current implementation.

Reported sequence:

1. `clothing` → insufficient evidence.
2. `We are currently trying to order a blouse for our team. Are there any color restrictions?`

Reported response to the second question: `What are you asking about—the ball, paddle, clothing, or something else?`

The current question itself explicitly identifies apparel. Future diagnosis must inspect the general apparel/object vocabulary, including whether `blouse` is absent; compare this sequence with a fresh standalone request to determine whether the preceding `clothing` turn affects classification. Trace raw/effective question, receipt/context classification, detected object, terminology expansion and retrieval candidates. Inspect active official LWR evidence for clothing/team-color rules and determine whether the correct final result is a grounded answer or insufficient evidence. Do not infer that a policy exists or is missing from this clarification failure alone.

Required principle: an independently unambiguous current object such as blouse, shirt, jersey or equivalent apparel terminology must not trigger the generic ball/paddle/clothing object question. Diagnose vocabulary coverage generally before proposing any bounded correction; do not hardcode blouse alone or globally assume that every reference to team/color means apparel.

Future regression controls:

- `Are there color restrictions for our team shirts?`
- `Can our team wear any color jersey?`
- `We're ordering blouses for the team. Are there color restrictions?`
- `What color can our team wear?` — may legitimately need clarification when clothing is not otherwise established.
- `Are there color restrictions?` — clarify when context does not establish the object.
- The exact reported blouse question, both standalone after New Question and following `clothing`, to distinguish vocabulary from receipt/context effects.

Do not implement in LMS-0721, change its accepted status, or create an Approved Answer. Any later managed-knowledge proposal requires authority review establishing a genuine missing policy rather than an existing-evidence or object-resolution defect. No production replay, code change, corpus change or deployment was performed when recording this item.

**CURRENT STATUS — LMS-0721 / 0.1.543 PRODUCTION ACCEPTED (2026-09-06).** Deployed `38eb7bb` / READY `dpl_4ACBAUDbciy2uqkbx54rArrhPRSZ`. First post-retirement answer now cites exact Rule 5.11 with truthful scheduling heading and no managed source. Final Existing Evidence explanation, historical retired revision-2 viewer, remaining typo/medical/reset/protected sanity, approved isolated security/authority/privacy coverage and production effective permissions/integrity passed. Both scheduling revisions remain retired and unchanged; no Active scheduling Approved Answer. 525 tests and required validation passed (isolated build resolves known cache lock). See [final acceptance](lms-0721-ordinary-sibling-citation-correction.md). Below-.65 recall, ambiguity, document activation display, Saturday/website/rally-scope/kitchen-equivalence quality items remain explicitly deferred, nonblocking. No next version started. Historical checkpoints below are preserved.

**Ordinary sibling citation correction implemented:** shared trusted identity utility now accepts a selected structural sibling inside the revalidated chunk, with all text/boundary/combined-family protections retained. No managed-only override, retrieval change or historical rewrite. 525 tests, lint (six existing warnings), nonincremental type check and PDF check pass; isolated build/deployment/first live retest follow. See [correction report](lms-0721-ordinary-sibling-citation-correction.md). Both managed revisions remain retired. LMS-0721 / 0.1.543 remains NOT production accepted until remaining gates pass.

**Latest lifecycle checkpoint — stop for citation review:** Owner accepted the .65 boundary; below-threshold variants and supplemental ambiguity are nonblocking deferred work. Revision 2 was saved with exact approved wording, Draft-isolated, activated/replaced revision 1, retrieved/cited with feedback, then explicitly retired. Historical revision 1 passed through its original Helpful event. Both revisions now retired; no Active scheduling policy. Post-retirement answer correctly excludes managed knowledge, but labels the independently selected Rule 5.11 passage as Rule 5.10 (correct scheduling heading). Read-only local probe confirms ordinary sibling-identity fallback; no correction made. See [final lifecycle checkpoint](lms-0721-final-lifecycle-acceptance.md). Corpus/Saturday/older-feedback hashes unchanged. Remaining gates paused; **LMS-0721 / 0.1.543 NOT production accepted**. No deployment or new version.

**Conditional recall diagnosis complete — no implementation:** LMS-0721 / 0.1.543 remains NOT production accepted. Independent normal formal retrieval selects exact Rule 5.11 for only the move-another-day variant (rank 1/.4174); both earlier-play variants lack the rule in all 32 candidates. All seven negative controls fail scheduling corroboration or stop before retrieval. No defensible conditional floor is recommended; global .65 and Active revision 1 remain unchanged. A separate formal-recall diagnosis is needed before a complete three-variant solution can be approved. Proposed conditional design requires exact independent passage matching and abstention for competing supplements; no circular binding or rank-only choice. See [complete diagnosis](lms-0721-conditional-recall-diagnosis.md). No implementation/deployment, lifecycle still paused; accepted reschedule/supplement/heading result preserved. Unrelated change-teams tangential selection observed and recorded, not corrected in this diagnosis.

**Latest production checkpoint (supersedes below):** LMS-0721 / 0.1.543 deployed as `46afddd`, Vercel `dpl_8jGHEUkWjUcEectY5KTN2CQvozX9` READY. Required reschedule retest **passes** the supplement-preservation and citation-heading gates: general notice/public email, formal restrictions, both sources, correct heading, zero Authority Warnings. See [exact production result](lms-0721-supplement-preservation-report.md). Stop at independent recall decision; .65 and Active revision 1 unchanged. No revision 2/retirement. Overall LMS-0721 remains **NOT production accepted** pending recall/lifecycle acceptance. Below-.65 positives remain .6147/.6122/.5612; no recall change or further production test.

**Supplement/heading correction checkpoint:** approved generic material-supplement generation contract and trusted provision-heading presentation implemented within LMS-0721 / 0.1.543. Configured-model replay preserves the additional obligation and correct 5.11 heading; 514 regressions pass. See [report](lms-0721-supplement-preservation-report.md). Retrieval, .65, revision 1 and Stage 7 unchanged. Authorized deployment/first-question gate follows final validation; then stop for the independent recall decision. Overall acceptance and revision lifecycle remain paused.

**Current production stop (supersedes checkpoints below):** LMS-0721 / 0.1.543 deployed as `a3e4923`, Vercel `dpl_5ochKYHXsWWC9pQ4QM99utqmbCV1` READY. First reschedule production test now selects Rule 5.11 + Active revision 1, but generation omits the every-change notification obligation; the source heading remains Video Recording despite correct 5.11 identity. Acceptance stopped as instructed, with no further tests/fixes/deployment. See [production result](lms-0721-scheduling-implementation.md). .65 unchanged; below-threshold recall and revision lifecycle remain paused; revision 1 unchanged. **NOT production accepted.**

**Scheduling correction implementation checkpoint:** approved bounded Stage 4 scheduling concept and exact related-passage combination are implemented at LMS-0721 / 0.1.543 with .65 unchanged. 499 tests and required local validation passed; exact production source reads validate 5.11 for four above-threshold positives. See [implementation/acceptance report](lms-0721-scheduling-implementation.md). Authorized deployment follows validation; three below-threshold positives await a separate recall decision. Revision 1 remains Active and unchanged; no revision 2/retirement. LMS-0721 is NOT production accepted.

**Latest LMS-0721 diagnosis checkpoint (supersedes historical checkpoints below):** LMS-0721 / 0.1.543 remains deployed at `941fd96fd6c9188ec480423e1b8fab6eb1b9b117`, **NOT production accepted**. Match Scheduling Changes revision 1 remains Active and unchanged. The scheduling binding diagnosis is complete: `Can we reschedule our match?` passes managed .65 at .6849 but generic formal applicability excludes 5.11 and selects Weekday forfeiture/weather evidence, blocking the supplement's selected-chunk binding. Canonical success was managed-only, not successful formal-plus-managed binding. See [scheduling binding diagnosis](lms-0721-scheduling-binding-diagnosis.md). Formal correction is proposed for review; .65/conditional recall remain unchanged and deferred. No implementation/deployment in this pass; revision lifecycle paused; approved local UX copy preserved. No new LMS version.

Public-contact correction pre-edit stop: local reproduction confirms Stage 7 `redactQualityText` independently omits the whole field containing the approved public club email. Latest approval requires public-contact treatment but explicitly prohibits Stage 7 changes and stops on another defect. No code/deployment change made; clarify the narrow Stage 7 email exception before proceeding. This is field redaction, not a live-data guard classification. See the implementation report.

**Current production checkpoint (supersedes earlier checkpoints below): LMS-0721 / 0.1.543 deployed at `7b4aca4fcfdc169114071f2e561a203de868f901`, READY deployment `dpl_B7isKuEVxuBRK1bF53bE4PLuriVH`; NOT production accepted.** The approved passage-binding migration was applied once and security/data verified. Live Rule 5.11 picker now passes. Saturday Rule 6.2.2 Existing Evidence Yes passed: one audited category change to AI/Retrieval Review, case remains New, original occurrence unchanged, no managed knowledge created. Desktop/mobile decision and non-executing Retest prefill passed. Website 1.1 and NR 4.5 identified without knowledge creation. Scheduling Save Draft is blocked by the previously reported public-email validator rejecting the exact owner-approved `info@lwrpickleballclub.com` policy. No correction or silent rewrite made. All managed tables remain empty; production lifecycle gates remain pending. See the report's current 23-item acceptance matrix. PDF activation history remains deferred, not a blocker. LMS-0720 remains the last fully accepted release; no new version started.

### Earlier LMS-0721 checkpoints (historical)

Latest local LMS-0721 workflow checkpoint: explicit existing-evidence Yes/No flow implemented and validated (467 tests, isolated production build passed). Yes reuses the existing audited category action and keeps cases open; No retains formal-authority blocks; source viewing and decisions are adjacent. No new workflow migration or production action. Pending passage-binding migration unchanged. Saturday case remains unmodified pending deployment/acceptance authorization. See the implementation report latest section.

LMS-0721 existing-evidence UX diagnosis: Saturday mixed-only player question is directly answered by active Rule 6.2.2, with supporting 6.2.5 already among retained candidates. Classify as AI/Retrieval Review, not missing knowledge; no Approved Answer for this example. Proposed explicit Yes/No decision adjacent to evidence, using existing category action plus bounded evidence audit note, with Retest and separate Resolve. No additional schema is needed for that minimal workflow. Diagnosis only; production case unchanged. See `docs/lms-0721-existing-evidence-decision-design.md`.

Latest LMS-0721 correction checkpoint: approved passage-binding correction implemented and validated locally (457 tests; isolated production build passed). STOPPED before new production migration/deployment for review. PDF activation history remains deferred and is not a blocker for this correction. Existing policy-text validation rejects the owner-approved public contact email; that separate issue is reported without changing validation. See the implementation report latest section.

Current production application version: **LMS-0721 / 0.1.543 — DEPLOYED; PRODUCTION ACCEPTANCE BLOCKED.** Manager-origin correction commit `f30927b2eae652fb711d19867d286ee69e76be51`, READY deployment `dpl_CndscDFLorLA3S6cXgNo5yH5Q49D`. Corrective migration/security passed. Live New Approved Answer opens, but required scheduling Rule 5.11 is offered as stored chunk Rule 5.10; stopped before saving a Draft. PDF activation history is explicitly deferred by the latest owner approval. See the implementation report final continuation matrix. Last fully accepted release remains LMS-0720 / 0.1.542. Stages 1–6 and Stage 7A/B remain accepted.

Current implementation: **LMS-0721 / 0.1.543 — Approved LWR Answers / Managed Knowledge, deployed for controlled acceptance.** Includes the approved metadata-only Authority Warning clarification. Migration applied once and application deployed; no acceptance knowledge has been created or activated. See `docs/lms-0721-implementation-report.md` for gate results. Live LMS Intelligence remains deferred.

Current Stage 7A status: final production data, privacy, effective permissions, deployment/source and capture-log review passed. The historical acceptance checkpoint contained eight outcomes, five occurrences and four groups/cases; exact two-event feedback correlation; deterministic tungsten grouping; protected and clarification metadata excluded from the unanswered queue; manager_test separate. Legacy feedback and corpus hashes remained unchanged; no validation synthetic rows remained. Live anonymous capture-health denial was reverified. Authenticated endpoint success was unavailable through the then-current browser tooling, despite the Commissioner UI session; the isolated full role matrix remains evidence. Production failure injection was intentionally not performed. These are owner-authorized accepted limitations, not acceptance blockers. See the implementation report final acceptance section.

Stage 7 governing design is approved in `docs/stage-7-ai-feedback-review-design.md`, including all 18 schema/workflow/UI/security/retention/test sections. Stage 7A is complete. Stage 7B read-only pre-implementation verification is recorded in `docs/lms-0718-stage7b-readiness-report.md`: 42 outcomes, 11 occurrences, nine groups/cases and 13 feedback events at the stated snapshot. Existing tables support the workflow, but atomic manager case/audit operations require a restricted transaction helper with the current Supabase REST architecture. The owner subsequently approved implementation and the function-only migration. Stage 7B function-only migration and application are deployed. One authorized historical community case completed Reviewing/category/note/Resolve/Reopen/Resolve with six appended manager events. Historical snapshots, feedback and corpus hashes remained unchanged. That initial historical-source blocker is corrected and production acceptance completed; see the final implementation-report section. Manual merge/split, assignment tooling, charts, topic classification, retention execution and Live LMS Intelligence remain deferred.

## LMS-0721 — Approved LWR Answers / Managed Knowledge

Manager-origin correction (2026-09-06): implemented locally on LMS-0721 / 0.1.543, pending review. New Approved Answer uses the shared editor and protected mutation path with an explicit NULL case ID; genuine case gates remain intact. New corrective migration preserves the FK/UNIQUE, table security and all lifecycle audit, omitting only nonexistent case linkage. Source, dynamic-data and overlap checks remain mandatory; activation reviews overlapping Drafts as well as Active items. 450 tests pass; lint has six existing warnings; nonincremental TypeScript/PDF verification pass. Normal build compiled then hit the known tsbuildinfo lock; final isolated production build passed 74 pages. Desktop/mobile synthetic creation/retry/origin/focus/scroll checks passed. No correction migration or application deployment occurred. Owner-qualified Match Scheduling Changes is retained for production acceptance after separate review; no production case, Draft or feedback manufactured.

Scheduling candidate authority review (2026-09-06): Rule 5.11, page 5, already governs agreed rescheduling within the same week, Sunday score deadlines and deadline-exception notice; Rule 5.12 retains makeup deadlines. Rule 6.1.9/page 8 flex permission is Weekday 9.1-specific. Notice to Management for every agreed change is genuinely additional guidance, but the supplied standalone permission omits formal timing limits. Stopped before creation for wording review; a qualified/notification-only supplement may preserve formal authority. No existing scheduling review case was found, and no case was manufactured. All managed tables remain empty. Full findings are in the LMS-0721 report; production acceptance remains pending.

Gate 7 update (2026-09-06): owner-supplied NR candidate was rejected before creation by read-only existing-authority review. Active DUPR League Rules 4.5, page 3, already contains the coach recommendation, captain responsibility and fair-competition guidance. “Any division” is not “any team”; Rule 3.5 community restrictions still govern. No Draft/revision/event was created (all three managed tables remain empty). A different genuine missing-knowledge candidate is required; lifecycle/remaining role acceptance is pending. See the implementation report for exact passages. This is existing official knowledge, not an established new retrieval defect.

The owner approved the discovered contract clarification: formal Rules continue to govern grounded answers; bounded Authority Warning metadata lives in existing outcome diagnostics and is visible in the new Approved Answers tab. No fabricated conflict/unanswered occurrence, case, feedback vote or metric is created. Replacement/retirement clears current warning visibility without rewriting historical revisions or observations. The initial design-defect stop is resolved by that explicit authorization.

Implementation adds three restricted tables, audited Draft/Active/Retired revisions, independent official-source duplicate prevention, atomic activation with manifest/overlap checks, one same-vector managed retrieval, exact-revision citations/viewer/feedback provenance and manager Authority Warnings. PGlite tests exercise production-like default grants and migration replay. Desktop/mobile synthetic UI checks and measured synthetic embedding calibration are documented in the implementation report. The controlled migration and deployment are complete. The owner-authorized Rules update is reconciled to a 7-document / 20-version / 1,581-chunk baseline; LMS-0721 did not process or activate documents. Stage 3 RPC, Stage 7 capture/HMAC/metrics and deferred website/kitchen selection remain unchanged. Stage 7A/B and LMS-0720 remain accepted.

Website questions are **not managed-knowledge gaps or Approved Answer acceptance candidates**. Active DUPR League Rules page 2, Rule 1.1, directly supplies the main website. The owner's production trace for “What is the website for the club” places Rule 1 at rank 3 (~.464), excluded by Stage 4 applicability/material contribution, while rank-4 Players Guide is selected with only DUPR website/contact email. Record as **AI/Retrieval Review** (`ai_retrieval_selection`) for a future bounded Stage 4 diagnosis, not an LMS-0721 selection correction. Creation/activation duplicate checks must block a confirmed existing official answer and must not mistake the normal selector's exclusion for evidence of missing knowledge. A different retained “Lakewood ranch pb” insufficient-evidence trace is documented separately in the report.

Future AI Source Management requirement, separately approved implementation only: show actual activation time in the manager's local timezone, optionally Activated By, and retain superseded history. Read-only production schema/function/trigger inspection found no authoritative activation timestamp/actor or document activation audit. Upload/processing/update times cannot substitute. Recommend future transactional activation records; legacy versions must say “Activation time not recorded” unless independent authoritative records are verified. No activation schema/UI changes are part of this diagnosis.

## Purpose and durable memory

### Deferred AI/Retrieval Review — Rally Scoring scope/applicability

Recorded 2026-09-06 for the next appropriate bounded AI quality diagnosis, not an LMS-0721 correction. Owner-reported production question: `what are your rally scoring rules`. The answer begins `LWR DUPR League rally scoring rules are:` and includes `All regular games are to 15 using Rally Scoring, win by 1.` The owner identifies this provision as league/format-specific. The reported answer combines general mechanics with game format, point values, end switching, timeouts, Picklebreaker and game-to-25 scoring freeze behavior without adequately preserving scope. These are reported observations; no new production trace or source verification was performed for this recording task.

Classification: **AI/Retrieval Review — scope/applicability**, not Missing Knowledge / Approved Answer, unless later evidence inspection establishes a genuine absence of general Rally Scoring guidance. Do not create an Approved Answer for this case. Existing official knowledge should direct managers toward AI/Retrieval Review through the LMS-0721 duplicate/existing-authority workflow, not competing managed knowledge.

Future diagnosis must trace raw/effective question, detected scoring intent, Stage 3 candidates/ranks/scores, selected passages, each passage's league/division/format scope and governing-source classification. Explain why scoped provisions were admitted for an unspecified/general question; find any general Rally Scoring section/source and its retrieval rank. Inspect official evidence before deciding whether to answer universal mechanics only, explain that format varies by league, ask which league, or use a bounded combination.

Required principle: a broad Rally Scoring question must not silently inherit one league's format. Preserve passage scope through selection and generation. Determine from official evidence which provisions are universal and which are scoped, including target score, win-by requirement, end switching, timeout count, Picklebreaker and scoring freeze. Explicit league questions may use that league's applicable provisions.

Future regression controls:

- `What are your rally scoring rules?`
- `How does rally scoring work?`
- `What are the Saturday rally scoring rules?`
- `What are the Weekday rally scoring rules?`
- `What are the PrimeTime rally scoring rules?`
- `How does the scoring freeze work?`
- `How does rally scoring work in a Picklebreaker?`

Documentation only: no LMS-0721 retrieval/applicability or generation change, formal-source edit, corpus processing, Stage 7 semantic change, Approved Answer creation, deployment or version change. Separate authorization is required before implementing this correction.

This file is the durable project memory for the LWRPC League Management System. Future development should treat this as the first place to understand what exists, what still needs work, and what decisions have already been made.

## Workspace Layout

- `C:\lwrpc-league-system` is the overall project workspace and Git repository root.
- `C:\lwrpc-league-system\lwrpc-admin` is the Next.js admin/player/captain web application.
- `C:\lwrpc-league-system\import-members.js` is the original member import script.
- `C:\lwrpc-league-system\members.csv` is a MembershipWorks export used for member import history/testing.
- `C:\lwrpc-league-system\lwrpc-admin\supabase-scoring-schema-updates.sql` contains scoring-related database schema updates.

For future Codex sessions, open `C:\lwrpc-league-system` as the workspace so both the import/history files and the admin app are visible.

## Current Application

The web app is a Next.js app using:

- Next.js `16.2.4`
- React `19.2.4`
- Supabase client `@supabase/supabase-js`
- Resend package installed for email support

The app currently includes pages for:

- Admin dashboard
- Login and password reset
- Members and member detail
- Member CSV import
- Ratings
- Users and role management
- Leagues
- Divisions and division line configuration
- Locations
- Teams and team detail
- Scheduling setup
- Schedule editor
- Matches and match detail
- Score entry
- Live match view
- Standings
- Captain dashboard
- Player dashboard
- Scoring operations for league managers to review due matches, filter unverified scores, edit reminder email copy, and send score-entry reminders to captains
- Club Setup for deployment-level club branding and contact defaults, intended to make separate club deployments easier to configure
- Tournament public display rotation across court detail, court-only, and current standings views, with tournament team check-in/edit workflows and regular-season standing tie-break support
- A standalone Round Robin module lives under `/round-robin` with a default `/round-robin/rpro` group. It uses separate `round_robin_*` Supabase tables, a code-gated manager API, saved player/court lists with Edit/Delete actions and manager-side search filters, a paged searchable LMS member picker with manual player entry fallback, reusable invited-player groups with current-player popups and Edit/Delete actions, session planning/opening/editing through Add/Edit Session modals with a right-side searchable upcoming/past session panel, per-session Players popups grouped by Joined/Declined/Waitlist/Invited status with manager Join/Decline controls and a saved-player selector to manually add a Joined player, safe session delete/cancel actions, start-session court confirmation that generates the first game, per-session court names seeded from court defaults, host/co-host fields with phone-verified host controls, waitlist-aware player confirmation, phone-based player response links at `/round-robin/[group]/player` with saved-device sign-in, automatic phone formatting, Club Setup branding, player record/history cards with past session results, a minimal host/live-session screen with Exit back to the session list or player page, one-game-at-a-time generation, score entry, player swap/shuffle controls, result summaries, and SMS broadcasts with a persistent testing/off switch, editable templates, new-session texts, pending-signup reminders, and current-player update texts. The public, player, and manager views use a more dimensional court presentation with clearer contrast, richer session cards, larger date/time cues, strongly defined top buttons/tabs, and a cleaner operational color system. It is intentionally isolated from league match and tournament tables.
- Round Robin host/co-host player cards treat playing sessions as live-only controls: Join/Decline responses are hidden, Resume and Finish are exposed to assigned hosts, Finish confirms before completion, and result SMS uses an editable ranked-results template with joined-count and available-spots placeholders.
- Round Robin manager SMS supports joined, invited-only, session, and all-player recipient scopes plus per-template test sends to a single phone number; Saved Players include a Stats popup with current-session, month, year, and all-time filters.
- Round Robin live play keeps manager-code entry separate from host/co-host resume: the `/player` Manager button clears host shortcuts and asks for the manager code, while Resume Session opens the assigned live session directly. Live court score inputs sit near the center of each side, use editable score boxes plus a Save Score action, and player chips are visually distinct from score entry.
- Round Robin live-session stats re-rank only the players shown in the current game popup, and the manager-code field avoids password-manager injection to prevent extension-caused hydration mismatches.
- Round Robin host/co-host resume uses a live-session verification screen instead of the manager-code form, and Next Game, Finish, and Exit auto-save complete unsaved score entries from the current round before continuing; score-entry keyboard flow moves from the first score box to the second score box without tabbing through player chips.
- Round Robin player record stats use Current Month, Last Month, Current Year, and All filters without average-rank summaries; the manager-code field stays text-based for password-manager stability while visually masking entered characters.
- Round Robin past-session history is limited to players with saved results from that played session. Player and manager session-result popups show only played-player stats plus all saved rounds, courts, teams, scores, and byes; manager Past Sessions can duplicate a completed session into a prefilled Add Session modal with the date cleared.
- Round Robin SMS sending uses one template-driven send action: selecting a text type loads that template and drives the send button label, with Pending Reminder automatically targeting invited players who have not joined or declined while other text types use the Recipients selector.
- Round Robin past-session rankings are filtered from the actual saved match lineups, so manager and player history popups only rank players who appeared in that session's games/byes; player Past Sessions are hidden unless that player appeared in those saved lineups.
- Round Robin session setup texting defaults are state-aware: new sessions default to texting invited players when created if SMS is enabled, edited sessions default to no text and can optionally text all joined players when updated, and the checkbox is disabled whenever SMS sending is off.
- Round Robin page shells use the full browser width instead of centered narrow content wrappers, and completed sessions hide the manager-side Players action in favor of results/history actions.
- Round Robin SMS has an explicit Texting Session selector; recipients, placeholders, pending reminders, broadcast texts, and template tests are rendered/sent against the selected session. Round Robin routes opt out of the LMS sidebar padding with `full-screen-main` so they do not reserve empty left space.
- Round Robin session starts are blocked in every manager/host UI path unless at least 4 players are joined, matching the server-side guard. Next-game generation treats previous-round partner pairs as a hard exclusion so the same two players cannot be teammates in back-to-back rounds.
- Round Robin live-session Stats, Next Game, Finish, and Exit actions all auto-save every complete unsaved score pair across all displayed rounds before continuing.
- Round Robin scored rounds are visually marked as complete with an emerald-tinted round panel and a Round Complete badge once every court in that round has saved scores.

Double-elimination tournament standings show generated championship/finals games even while one or both sides are still waiting on bracket advancement. Winners Bracket rounds stay labeled as `Round X`; the generated championship game between the Winners Bracket winner and Elimination Bracket winner is labeled `Finals`, with a reset game labeled `Finals If Necessary`.

Tournament bracket home/away sides are balanced during bracket generation and as teams advance into later games. Championship reset logic uses bracket-loss history instead of assuming the Winners Bracket team is always the home team.

## Multi-Club Deployment Direction

For future clubs, prefer a separate Vercel deployment, Supabase project, and GitHub repo/branch per club instead of putting all clubs in one shared database. This keeps data isolation simple, reduces operational risk, and avoids every query needing club-level tenancy filters. The Club Setup page stores club-specific defaults such as club name, logo URL, website, main league email, support email, league site URL, and timezone. Locations support two member-backed Club Pro assignments; either Club Pro is upgraded to the `club_pro` role and sees teams at that home location in Captain Dashboard.

## Data Source

Supabase is the shared source of truth. The admin app currently reads Supabase values from:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

The parent project has service-role credentials for server-side/import tasks:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

Keep service-role credentials out of browser code. Use them only in trusted server scripts or route handlers.

## Roles

The app appears to use role-based navigation and access checks around:

- `player`
- `captain`
- `club_pro`
- `league_manager`
- `commissioner`

Role logic lives in `lwrpc-admin/app/lib/permissions.js` and auth/session logic lives in `lwrpc-admin/app/lib/auth.js`.

Role levels are hierarchical: `player < captain < club_pro < league_manager < commissioner`. Login routing sends players to the Player Dashboard, captains and club pros to the Captain Dashboard, and league managers/commissioners to the Admin Dashboard. Commissioner is the only role allowed to manage user roles, and the app now blocks changing the last remaining Commissioner to another role from both the User Roles page and Member Detail page.

Auth guards should resolve role access through the signed-in user's member email/member id before falling back to `user_roles.user_id`, because some role rows may exist before an auth user is linked. Captain navigation intentionally keeps captains focused on the Captain Dashboard instead of showing the broader sidebar League Operations groups.

Captain Dashboard operations use clickable status blocks for Pending Score Verification, Upcoming Matches, and Completed Matches. These blocks sit under My Teams, scope counts/results to the selected captain team, and the selected team card is highlighted. League Documents on captain team cards are collapsed by default. Password reset/account setup requests log whether Supabase accepted the `recovery` or `invite` email path so delivery issues can be diagnosed from server logs; Supabase Auth rate-limit errors are shown as a friendly "wait a minute" message.

Member Administration now uses the same password-reset/account-setup route as the login screen. If a member has no Supabase Auth user yet, the action sends a Supabase invite/setup email; if the auth user exists, it sends a recovery/reset email. Role capability help is shared in `app/components/RoleCapabilityModal.js` and used from Users, Members, and Member Detail.

Display dates/times should use `app/lib/dateTime.js` helpers so dates render as `mm/dd/yyyy` and stored SQL times such as `12:00:00` render as `12:00 PM`. Captain access to Team Roster Management should return captains to the Captain Dashboard, hide Season Ratings, and block locked-roster additions when the selected player is outside range or missing the required season rating.

League setup now supports a `leagues.rosters_locked` flag. When enabled, captains can view rosters but only `league_manager` and `commissioner` roles can add or remove roster players.

Scoring reminders use the existing notification route and Brevo helper stack. The scoring reminder email template can be stored in `notification_templates` when the schema update has been applied, with browser local storage as a fallback.

Notifications use server-only Brevo environment variables. `BREVO_API_KEY` is shared by email and SMS; SMS also requires a Brevo-approved `BREVO_SMS_SENDER` and may include `BREVO_SMS_ORGANIZATION_PREFIX` for US delivery. Email requires `BREVO_FROM_EMAIL`; `BREVO_FROM_NAME` defaults to Lakewood Ranch Pickleball Club. Keep actual secret values in Vercel/server environment settings, not committed files.

The Admin Dashboard includes a Notification Test panel that sends test email and/or SMS messages through `/api/notifications`, using the same Brevo environment variables as live score reminder workflows.

Print workflows should use the generic `/print` page so print previews open on an app URL and include the LWRPC copyright/page footer inside the document. Browser print dialogs may still show their own headers/footers unless disabled by the user.

Captain Dashboard's generated match score sheet lives in `lwrpc-admin/app/captain-dashboard/page.js` as printable HTML. Score sheet formats are managed from `/score-sheets` and stored in `score_sheet_templates`; Divisions can select a saved format through `divisions.score_sheet_template_id`. The default/fallback format matches the original Weekday DUPR League Score Sheet styling and uses placeholders such as `{{match_date}}`, `{{home_team}}`, `{{lineup_rows}}`, and `{{score_entry_table}}`. Score entry rows are generated from each Division's configured game lines and their `games_per_line` values; the score detail table intentionally uses a compact `Game N: Line Name` first column plus Line Type, Game Format, `Away Score:`, and `Home Score:` columns. The copyright/version/page footer is handled by the shared print page, outside the editable template body. The Supabase update is in `lwrpc-admin/supabase-score-sheet-templates.sql`.

Divisions now support `team_dupr_max`, a combined doubles-team rating cap based on the division rating type. This is not a full roster cap; captains are blocked from saving match setup lineups over the cap, duplicate players are blocked within a match setup, and saved lineups are checked when used in score entry.

Scoring operations can export verified scores for DUPR using the club DUPR CSV format. Verified, not-yet-exported matches are selected by default; already exported verified matches can be manually reselected for override re-export. The match list is sorted by scheduled date with the most current date first. The DUPR Export action includes only match lines marked to Post to DUPR, skips lines marked Not Posted to DUPR, and export tracking uses `matches.score_exported_at`. When League Managers or Commissioners open a match from Scoring Operations, the match detail route receives `?from=scoring`; only that scoped path unlocks editing verified scores and uses Submit/Verify Scores to save, auto-verify, rebuild standings, and notify all match captains that scores changed. Captain Dashboard score-entry behavior remains separate.

Captain Dashboard has a match setup workflow for upcoming matches. Captains can save doubles pairings per match line in `match_lineups`; completed setups show a status badge on match cards, opposing captains receive an email with the submitted lineup, and the match score-entry screen can then populate game players from those saved lineups.

Score entry is restricted to captains assigned to the match. Match score rows use Team 1/Team 2 style labels for line slots, and saved match setup choices are scoped to the matching team slot.

Teams & Rosters supports assigning captains/co-captains from outside the team's home community through an explicit all-communities toggle. Team lists are grouped by league/division and start collapsed as accordions.
Season rollover tools include copying a league's divisions into a target league, copying all teams from one division into another division with optional roster copy, and a Create Next Season workflow. Create Next Season copies leagues, divisions, configured game lines, teams, captain assignments, and optional rosters into fresh records while leaving standings, schedules, byes, matches, scores, ratings, and player results with the original season. Season inactivation archives the original season and teams without changing its historical standings or player history.
Player and Captain dashboards now fall back to Weekday League documents when no team/league is selected, while retaining the selected team's documents whenever one is available. The visible app version was bumped to LMS-0630.

Standings points are calculated from configured game-line scoring rules rather than only from match-level win/loss/tie points. Each division line defaults to `standings_points_mode = 'line_result'`, where the line winner receives configured `team_win_points`; `standings_points_mode = 'per_game'` awards configured `team_win_points` for each completed score-row win. Standings displays sort teams by the division's configured Standings Tiebreak Order. Divisions can set `playoff_team_count` to highlight the top ranked teams as Playoffs/Championship Day teams in standings displays. The visible app version is centralized in `lwrpc-admin/app/lib/version.js`, displayed near the copyright footer, and now uses the non-date `LMS-####` build counter format. Increment the build counter with each update.

Player Dashboard uses inline panels for play history, division standings, and upcoming matches. Team summaries appear above the dashboard action buttons and include captain/co-captain contact details. Play history includes top-level games/wins/losses totals plus individual game scores when entered.
Player Dashboard and Captain Dashboard My Teams show browser-style team tabs when more than one team is visible, with only one full team card expanded at a time. Player Dashboard My Teams cards mirror the Captain Dashboard team card rank/stat treatment: Rank is a raised blue action that opens League Standings for the team's league/division, and the header shows Players, Season Points, and W-L-T summary stats.
Player Dashboard Team Matches mirrors the Captain Dashboard completed-match card format, including green/red headers for selected-team wins/losses and the `Match Score Details` button. Completed match score-detail popups outline the winning home/away player card with a bold border.
The Player and Captain My Teams browser-style tabs use a small hover lift and shadow so tabs feel clickable without changing selection until clicked.
LoadingScreen renders default system settings for its initial server/client pass and then refreshes cached branding after mount, preventing hydration mismatches while preserving the fast cached-branding behavior.
Player Dashboard Play History Scope team filters must pass the logged-in member id into `filterHistoryRows()` so each match line resolves the player's actual home/away team before applying a `team:<id>` filter. Team filters match the resolved player-side team id first, then fall back to same-name team identity when roster/current team ids differ from historical match team ids.
Schedule Editor's Swap action uses an in-app confirmation modal instead of a browser prompt, showing the proposed new home team, a full-width red/green court availability banner, separate lines for Courts available, Current Courts Used, either Courts Used After Swap or Overbooked Courts by depending on availability, and current vs after-swap Home/Away counts for both teams. Schedule Editor match rows label the match-opening action as `Open Scores`, open that route with scoring override plus a Schedule Editor return target so Submit/Verify Scores rebuilds standings and returns correctly, and use a compact layout with date over time, location over status, a standalone Published/Not Published pill, and a two-row action grid.
Player Dashboard and Captain Dashboard member lookups should tolerate duplicate member rows for the signed-in email by choosing the first active member record, then the oldest record. Do not use exact-one Supabase calls for these email lookups, because users without a current roster/captain assignment should see the existing empty-state message instead of a "JSON object requested, multiple (or no) rows returned" error.

League Standings team names open the shared Division Team Schedules/Standings modal used by captain workflows, showing all teams in the selected division, division schedule/byes, standings summaries, and verified match details.
League Standings includes a prominent role-aware Back to Dashboard action above the standings filters, returning players to Player Dashboard, captains/club pros to Captain Dashboard, and league managers/commissioners to Admin Dashboard.

Phone numbers entered or imported through member workflows are normalized to `(999) 999-9999` formatting when enough digits are present, with extensions preserved as `x123`.

Member email addresses are normalized to lowercase and validated for basic email shape before manual save or import.

Password onboarding uses Supabase reset-password emails. The login page tells first-time users to use Forgot Password, Member Administration can send a reset/set-password email for a member, and Player Dashboard has a Change Password header action that opens the reset password screen directly for the logged-in player.

Member Administration loads each member's current team memberships for a Teams count button. Clicking it opens a modal with the member's active team list and league/division context.
Member Administration also has a Current Rosters Only filter. Team membership data is loaded in paged reads from `team_members` to avoid oversized Supabase `.in(...)` requests that can fail in the browser. The Deactivate placeholder action lives on the member edit screen and is visible only to `league_manager` / `commissioner` users until the members table has an active/status field.

The captain Division Team Schedules modal displays each division team's season match record from `team_standings` beside team names. The shared Division Team Schedules/Standings popup keeps captain/co-captain names in the selected-team header's top-right area as a truncated line to preserve screen space.

Season setup now lives on its own `/seasons` page under the League Setup sidebar. Teams also lives under League Setup with Seasons, Leagues, and Divisions. Locations, Email Options, Score Sheets, and Club Setup live under the System Setup sidebar. `/leagues` is focused on league information, roster lock status, and league document mappings, with create/edit on the left and the current league list on the right.

League setup supports five league-specific PDF document mappings stored on `leagues`: Code of Conduct, Captains Guide, League Rules, Score Sheet, and League Waiver. The app expects the columns from `lwrpc-admin/supabase-league-documents.sql`, uses `NEXT_PUBLIC_SUPABASE_LEAGUE_DOCUMENTS_BUCKET` or `league-documents` as the default bucket, and uses `NEXT_PUBLIC_SUPABASE_LEAGUE_DOCUMENTS_PREFIX` or `private` as the default Storage folder prefix for listing PDFs. Captain Dashboard team cards show configured league document buttons and open PDFs in an embedded viewer with download and print/open controls.
Player Dashboard team cards show player-facing league document buttons for Code of Conduct, League Rules, and League Waiver using the same embedded PDF viewer. Captains still see all configured league documents, including Captains Guide and Score Sheet.

Teams & Rosters displays roster counts on each team's Roster button.
Season Ratings has Current Rosters Only and Missing Doubles Rating filters, highlights players missing a doubles rating, and omits member email addresses from the ratings table.

## Verified Status

As of 2026-05-21:

- `npm run build` succeeds in `lwrpc-admin`.
- `npm run lint` succeeds with no warnings.

## Known Issues And Cleanup

1. Keep notification delivery configuration current.
   Brevo delivers both email and SMS. Keep its API key and sender configuration server-only.

2. Replace default Next.js README content.
   `lwrpc-admin/README.md` still contains mostly default create-next-app text. It should become project-specific setup and operations documentation.

3. Document Supabase schema.
   The code references many tables, but there is no single schema map. Add table descriptions, important relationships, and RLS assumptions.

4. Add tests or scripted smoke checks.
   High-risk areas include standings calculation, score verification, schedule generation, member import, and role-based access.

5. Review secret handling.
   Keep `.env` and `.env.local` local-only. Never commit live service-role keys or email/SMS provider keys.

## Roadmap

### Phase 1: Project Memory And Developer Setup

- Add root-level project documentation.
- Replace the default admin README with real setup steps.
- Document required environment variables.
- Document how to run, build, lint, and deploy.
- Document Supabase tables and major workflows.

### Phase 2: Reliability Pass

- Keep lint at zero warnings as new pages are added.
- Keep Brevo sender registrations and US SMS compliance current.
- Add smoke tests for critical pages or workflows.
- Add unit-level coverage for scoring and standings helpers where possible.
- Check all role-gated pages for consistent unauthorized handling.

### Phase 3: League Operations Polish

- Verify season, league, division, team, and roster workflows end to end.
- Validate schedule generation against real club rules.
- Confirm court availability and blackout behavior.
- Improve error messaging for failed Supabase operations.
- Add loading and empty states consistently where missing.

### Phase 4: Scoring And Standings Hardening

- Validate match score entry for all configured line formats.
- Confirm picklebreaker scoring behavior.
- Confirm captain score verification/dispute flow.
- Confirm standings recalculation behavior after edits.
- Add audit trail or history for score changes if not already present.

### Phase 5: Member And Rating Operations

- Validate MembershipWorks CSV import against current exports.
- Confirm inactive/missing member handling.
- Confirm DUPR, Primetime, and self-rating import/update flows.
- Add import result logging that can be reviewed later.

### Phase 6: Deployment And Admin Operations

- Document Vercel deployment setup.
- Document Supabase production configuration.
- Document admin account bootstrap process.
- Document backup/export strategy.
- Add a release checklist for league season launch.

## Suggested Next Tasks

1. Rewrite `C:\lwrpc-league-system\lwrpc-admin\README.md`.
2. Create `C:\lwrpc-league-system\docs\supabase-schema.md`.
3. Fix lint warnings in small batches.
4. Keep Brevo sender registrations and US SMS compliance current.
5. Add a release checklist for league season launch.

## Development Notes For Codex

- Before editing Next.js code, read the relevant local docs under `lwrpc-admin/node_modules/next/dist/docs/`.
- Prefer opening the parent workspace `C:\lwrpc-league-system`.
- Use existing app patterns before introducing new abstractions.
- Avoid service-role credentials in client components.
- Keep changes scoped and document system-level decisions here.
- User-facing delete actions now use the shared `app/lib/confirmDelete.js` helper, which requires typing `DELETE` and explains the affected records or risks before destructive actions proceed.
- Members now have a `notification_preference` (`email` or `text`) used by captain match setup notices, score-entry notifications, and scoring reminders. Schedule settings also support `actual_schedule_weeks`, and Schedule Editor highlights court overbooking / blackout issues in red with a home-away counts popup.
- Footer copyright rendering uses the centralized `COPYRIGHT_YEAR` from `app/lib/version.js` so client-rendered pages do not call `new Date().getFullYear()` during hydration.
- Schedule Editor supports team-name and exact-date filters. Swap court warning text reports proposed swapped-location usage instead of the original match location.
- Captain Dashboard and Player Dashboard only load `matches.is_published = true` for upcoming/visible match lists; draft schedules remain reviewable in Schedule Editor.
- Captain Dashboard bye weeks are only shown when a published match exists for the same division/week/date, so byes do not appear automatically from an unpublished draft schedule.
- Teams & Rosters has a team schedule popup with match status, match scores, configured game lines, and game scores. Captain Dashboard has a Division Team Schedules popup next to Manage Roster that lists every team in the division and shows published schedules/results.
- Captain Dashboard's Division Team Schedules popup uses compact schedule cards that hide configured game-line details and show only date/time, teams, week/location, status, and total score.
- Compact team schedule scores now label Home and Away separately and highlight the winner. Pending Score Verification cards now open the normal score review screen without Match Setup controls, and pending-verification score entry is read-only while Validate Scores and Dispute Scores remain available to captains.
- Division Team Schedules keeps compact score boxes pinned to the right on wider layouts. Schedule Editor now shows a visible problem-match count for court/blackout issues and groups Scheduled Matches into collapsible sections based on the selected sort option.
- Schedule Editor Scheduled Matches accordions now start closed. Player Dashboard play history shows the actual team matchup on the first line without the line-score suffix, and each game score chip includes the player names from that game line.
- Player Dashboard play history metadata now stops after season, league, and division. Game score chips include both the player's team/player names and the opponent team/player names.
- Schedule Editor has an administrative Reset Match action that clears match setup, score-entry players, game scores, winners, verification/dispute/export state, and rebuilds division standings while preserving schedule details. Captain Dashboard and Player Dashboard top sections were restyled with more mobile-friendly team cards, colored summary/action blocks, and larger touch targets.
- Admin Dashboard was redesigned into a cleaner operations hub with a dark command-center header, colored quick-status blocks, grouped tool sections for league setup, people/teams, and match operations, and mobile-friendly action cards.
- Member Administration now supports manually adding members from an Add Member modal with core profile fields, notification preference, DUPR ID, renewal date, optional home-community suggestions, email validation, phone formatting, and optional initial app role assignment.
- Admin Dashboard quick-status blocks now show live Supabase counts for member records, total matches, pending score verifications, and assigned user roles instead of placeholder labels.
- Admin Dashboard quick-status blocks now show member records, players on teams, total teams, and scheduled matches for the current Sunday-through-Saturday week.
- Admin Dashboard command-center header no longer includes the duplicate Open Schedule Editor button because Schedule Editor remains available in the Match Operations tool cards.
- Admin Dashboard now places League Structure below Match Operations and replaces the bottom build-status placeholders with live operational counts for assigned app users, saved communication templates, and verified matches waiting for DUPR export.
- Admin Dashboard System Snapshot now uses assigned app users, pending score verifications, and verified matches waiting for DUPR export; live signed-in user presence is not currently tracked by the app.
- Admin Dashboard active-season metrics now scope players-on-teams, teams, this-week matches, pending verifications, average roster count, and average team DUPR rating to leagues whose season start/end dates include the current date.
- Admin Dashboard now includes an Active Season / Not Active (Current Entries) scope toggle so preseason teams and rosters can be counted before the season date window opens.
- Members use `members.is_active_member` for active/inactive status. Member Administration hides inactive members by default with an Include Inactive toggle, member edit supports deactivate/reactivate for League Manager and Commissioner roles, MembershipWorks import reactivates imported members, and ratings/team selection workflows exclude inactive members.
- Member Administration now shows active/inactive status in the listing, includes a Data Tools action to mark all members inactive before a fresh MembershipWorks import, and the MembershipWorks import protects existing member fields by only overwriting email, phone, renewal date, active flag, and membership status while filling blank identifiers/profile fields. Existing DUPR IDs are retained; the import only supplies a DUPR ID when the member record currently has none.
- Season Ratings hides the Ratings Import panel behind a Data Tools toggle by default. Player Dashboard and Captain Dashboard division schedule modals received a visual cleanup with stronger colored headers, status blocks, and more polished match/result cards.
- Player Dashboard upcoming matches now use a purple visual treatment, player/captain team cards use stronger blue information panels, Captain Dashboard pending verification is red, and Season Ratings truncates entered DUPR doubles ratings to one decimal place before saving.
- Manual member creation now satisfies the non-null MembershipWorks account constraint with a generated `manual:` account id. Future MembershipWorks CSV imports can match those members by email and replace the placeholder with the real MembershipWorks account id. Captain Dashboard upcoming match cards were restyled with Week headers, and Player Dashboard upcoming matches now separate Current Scores from a Match Details popup with team records/ranks and a map link.
- Forgot Password and member-admin login email actions use a server route with `SUPABASE_SERVICE_ROLE_KEY` (or compatible service key env names) to verify active member records, find existing Supabase Auth users, send recovery emails, invite missing auth users, and link blank `user_roles.user_id` values. The route returns a setup/configuration error if the service key is missing so anonymous RLS cannot produce false member-not-found results.
- Login and password onboarding load member email matches as lists instead of singleton rows. Historical/imported member data can contain duplicate email rows, so these flows prefer active member records and use the highest role found across active duplicate rows.
- Login now highlights "League Management System" in the title. Member Administration uses fixed table columns and non-wrapping phone/action cells for consistent row heights. Scheduling pages format displayed dates through the shared date helper, Saved Schedule Settings are client-sorted by setting name with a generated/not-generated badge, and Schedule Editor date sorting breaks ties by home team.
- Root layout uses React hydration warning suppression for browser-extension injected attributes on form controls; avoid inline layout scripts because Next.js 16 dev mode reports them as client-rendered script tags.
- Captain Dashboard match setup now marks pending setup buttons red, blocks saving until every doubles team has two players and no lineup warnings, and reports whether the opponent captain notification actually sent. The Division Team Schedules modal sorts teams by current standings, shows record plus standings points in each team block, and lets captains expand match cards to view entered line players, ratings, and game scores.
- League setup includes `leagues.match_setup_reminder_days_before`; `/api/match-setup-reminders` is a server route intended for daily scheduler/Vercel Cron use and sends email/SMS reminders to captains whose published upcoming matches still have incomplete Match Setup teams. Season Ratings has NR filters for doubles and age-based ratings plus a Copy Name action. Score entry now enforces completed-game players/scores, points-to-win rules, and DUPR max blocks, while allowing forfeit/retired exceptions and captain edits until validation. Schedule Editor Reset Scores preserves saved Match Setup teams.
- Player Dashboard play history labels forfeited/retired game details, counts those rows under Other, shows player ratings in the team header and after play-history player names, and Team Roster Management shows each roster player's games played and record beside the player details.
- Teams & Rosters Schedule now opens the same division-wide compact schedule format as captain Division Team Schedules, scoped to the selected team's division and expandable to line/player/rating/score/result details. Game-line configuration blocks duplicate Game Numbers per division and the Divisions list shows configured line counts. Admin Dashboard can maintain player/captain login popup messages with per-user dismissal, dashboards include Contact League mail links, and Schedule Editor filter width/date accordion/swap court-unavailability messaging were tightened.
- Seasons and Teams use active/inactive flags instead of deleting historical records. Inactivating a season is a double-confirmed archive action that marks all teams in that season inactive and resets their standings rows to zero while preserving leagues, divisions, teams, rosters, matches, scores, and player history for reporting.
- Teams & Rosters can copy an existing team to another active-season league/division, optionally copying roster rows. Current roster/team selection displays now filter inactive teams out of member administration, member detail, ratings current-roster filters, match scheduling selectors, schedule-generation team lists, standings, and active dashboard counts.
- Player Dashboard and Captain Dashboard now show active teams by default with a Previous Seasons Teams toggle. Player Dashboard uses the selected My Teams team for both Division Standings and Team Matches; selected team cards are highlighted by card color instead of a separate selected-label button.
- Admin Dashboard login popup message saves go through `/api/notification-templates` with server-side Supabase access to avoid `notification_templates` RLS insert failures. Season Ratings includes a Copy Ratings data tool for copying member season ratings from one season to another, and CSV imports skip matched rows without numeric ratings instead of clearing values to NR. Division game lines support `uses_saved_match_lineups` so score entry can show saved Match Setup team dropdowns only on configured lines.
- Leagues and divisions now have active/inactive flags. Current setup dropdowns for seasons, leagues, and divisions should use active records, while administration lists keep inactive records visible for reactivation. Scheduling shows League Blackout Date weekdays and schedule generation advances through blackout weeks without counting them against Actual Weeks.
- User-facing timestamp displays should format stored Supabase UTC timestamps through the shared `formatDisplayTimestamp` helper, which renders them in `America/New_York`; date-only schedule fields continue to use the date-only display helpers.
- Team schedule popups and player/captain schedule views include `team_byes` alongside published matches, filtered to published division schedule weeks so draft byes stay hidden with draft matches.
- Season Ratings now separates raw `DUPR Doubles Rating` from cleaned numeric `Season DUPR Rating`. CSV imports write the raw doubles value, and Data Tools can clean selected-season ratings by truncating numeric raw DUPR to tenths or using the player's highest selected-season division max minus 0.5 for NR values.
- Season Ratings imports Age-Based ratings from a rating-export `Metrics` JSON field: it uses `subscores.doubles.over_65`, falls back to `over_50`, and leaves the value blank when neither is present. Imported, manually edited, and Clean Ratings Age-Based values are truncated to one decimal place.
- Team Roster Management now hard-blocks adding players whose current rating is outside the team's rating range. If a roster add succeeds for a player missing the required rating or carrying an `NR` DUPR Doubles value, the app emails `info@lwrpickleballclub.com` with the player, team, captain names, and captain emails for league follow-up.
- Team Roster Management shows roster-add player eligibility in the Available Players dropdown, includes the rating season in the compressed team summary block, and keeps DUPR IDs out of that dropdown. Login popup reads now go through the server notification-template route so captain/player messages are not blocked by template-table RLS.
- Score workflows track `matches.score_entered_by_member_id` along with existing entered/verified timestamps. Team schedule popups show non-empty score status with the relevant timestamp, while Schedule Editor and Scoring Operations show entered/verified timestamp plus member name for completed matches. Apply `lwrpc-admin/supabase-score-audit-updates.sql` before relying on entered-by tracking in production.
- Member Administration team popups show whether the member is a Player, Captain, or Co-Captain. Season Ratings has a Missing / Invalid DUPR ID filter for blank IDs or IDs not exactly 6 characters. Season inactivation now requires typing uppercase `INACTIVATE`.
- Admin Dashboard includes a Commissioner-only server-backed Master Reset All action with three confirmations. It deletes generated match/schedule/score/standing/roster data, clears team captain fields, marks teams inactive, and resets `captain` user roles to `player` while preserving saved schedule settings and higher administrative roles.
- Master Reset's database function uses explicit `WHERE true` predicates for its intentionally full-table deletes, so it remains compatible with the database safe-update guard that rejects unqualified DELETE statements.
- Deleting a division now removes its generated team-bye rows first, preventing the `team_byes_division_id_fkey` constraint from blocking the protected deletion. The visible app version was bumped to LMS-0626.
- Teams and Seasons now default to active records, with Include/Hide Inactive controls for archived records. The visible app version was bumped to LMS-0628.
- User Roles & Permissions shows Supabase Auth last-login timestamps through `/api/user-last-logins` using server-side service-role access. League roster-lock helper text clarifies captains cannot add/remove ineligible rated players while locked. Player and Captain dashboard section-button panels received stronger raised styling, Captain Dashboard completed match score status includes status timestamps, Season Ratings has an Edit Member shortcut beside Copy Name, and the visible app version was bumped to `2026.05.25.1200`.
- Sidebar navigation now groups Matches under Scheduling. Player Dashboard captain/co-captain contact blocks are mail links with tooltips, and Membership Info has a tooltip for the Manage Membership website. Captain Dashboard now labels standings points as Season Points, renames Division Schedules to Schedules/Standings, and the division schedule modal labels ranked teams as Teams sorted by Rank. The visible app version was bumped to `2026.05.25.1230`.
- Captain Dashboard defaults to Upcoming Matches unless any captain match is pending score verification, in which case it opens Pending Score Verification. Match Scheduler Scheduled Matches now has a search/filter box and compact match rows. The visible app version was bumped to `2026.05.25.1300`.
- Player, Captain, and Admin dashboards now include Players Guide, Captains Guide, and Admin Guide buttons. Admin Dashboard has a Dashboard Guides PDF selector using the same Supabase Storage bucket/folder/load-PDF flow as League Administration, storing guide selections in `notification_templates` as JSON guide config. The visible app version was bumped to `2026.05.25.1330`.
- Player and Captain guide buttons now render inside the header Welcome card below the signed-in user/role, and guide buttons use the same green styling as Contact League. Admin Guide uses the same green guide styling. The visible app version was bumped to `2026.05.25.1345`.
- Match score submission no longer rebuilds standings or Season Points; standings update only when scores are validated. Standings rebuilds now count only completed matches with `score_status = verified`, so Schedule Editor Reset Scores removes the reset match from standings. Resubmitting or disputing scores clears stale verification audit fields, Scoring Operations hides verified audit details unless the match is actually verified, and Filter Not Verified selects the visible not-verified matches. The visible app version was bumped to `2026.05.25.1400`.
- Player and Captain guide buttons now open guides in the embedded PDF preview modal with Download and Print controls, matching league document behavior. Team Roster Management now keeps All Locations selected after the user chooses it by separating uninitialized location state from the blank all-location filter. The visible app version was bumped to `2026.05.25.1415`.
- Admin Dashboard command-center metrics now include a Dashboard Scope selector for the default active/current scope, all seasons, a selected season, league, or division. Players On Teams, Teams, This Week, and the related operational snapshot counts recalculate from that selected scope. The visible app version was bumped to `2026.05.25.1430`.
- Admin Dashboard Players On Teams now has an All Players / Unique toggle so the card can count every roster assignment, including duplicate players across teams, or count each player only once within the selected dashboard scope. The visible app version was bumped to `2026.05.25.1445`.
- Teams now support a team-level Club Pro assignment via `teams.club_pro_member_id`. Assigned Club Pros are upgraded to the `club_pro` app role when needed, use the Captain Dashboard as their home dashboard, see assigned teams in Captain Dashboard My Teams, and are included in captain-style match setup, score notifications, reminders, team/member role displays, and contact lists. Apply `lwrpc-admin/supabase-team-club-pro-updates.sql` before deploying this change. The visible app version was bumped to `2026.05.25.1500`.
- Player Dashboard team cards now hide unassigned Captain, Co-Captain, and Club Pro contact blocks, and omit the whole contact row when a team has no assigned contacts. The visible app version was bumped to `2026.05.25.1515`.
- Captain Dashboard upcoming matches now include a Match Details popup aligned with the Player Dashboard view. Player and Captain match details include roster buttons for both teams, roster popups show each player with the correct season/age-based/self rating plus their verified season line record, and completed score details show player ratings plus line team ratings. The visible app version was bumped to `2026.05.25.1530`.
- Captain Dashboard League Documents blocks now use a stronger blue/cyan background treatment, doubles Team Rating displays now sum the two listed player ratings in Player and Captain match result views, and the sidebar club copyright line no longer adds a period after the club name. The visible app version was bumped to `2026.05.25.1545`.
- Login password-reset/setup success messages now use a centered popup instead of inline status text. Captain Dashboard action buttons are ordered Upcoming/Unverified, Completed, then Pending Score Verification, with the team Rank button styled as a raised button. User Roles & Permissions table headings sort the visible rows.
- Team Roster Management displays member membership status and renewal date in roster rows, below the season rating. Renewal dates due within 30 days or past due are highlighted in bold red. The shared loading screen uses the same full-screen centering treatment as login so it is not offset by sidebar layout assumptions while full-page loads are in progress.
- Captain Dashboard upcoming match cards include a compact Match Setup status/action area and put Match Details, Match Score Sheet, and score-entry actions below the match date/location/status details. Match Score Sheet generates a printable preview from saved home/away match setup lineups, player season ratings, match date, teams, and division. The visible app version was bumped to `2026.05.28.1731`.
- Team Roster Management, League Standings, and Division Team Schedules/Standings received mobile layout cleanup. Captain Dashboard Match Setup buttons now turn red while setup is pending and return to blue when complete.
- Round Robin live/session scheduling now uses a complete 8-player, 7-round partner rotation so every player partners with each other player exactly once before any repeats are allowed. The one-round-at-a-time generator shares this rule and falls back to strict no-repeat matching if manually edited prior rounds conflict with the fixed rotation.
- Round Robin Settings includes a typed-confirmation Master Reset that deletes all sessions, session players, generated games, scores, player-session result history, and session-linked logs for the selected Round Robin group while preserving Saved Players, Groups, and player group memberships.
- Player and Captain Dashboard Recent Results keep score blocks in the same home/away sequence as the displayed matchup, label the selected team's block Your Team and the other block Opponent, and retain Home/Away match context. Team names are not repeated inside score blocks; the selected-team block remains visually emphasized and the score uses a dedicated full-width row on mobile.
- Player and Captain mobile dashboard headers now include a hamburger menu that opens the complete dashboard navigation, restoring direct access to League Documents and My Play History. The bottom My Team/Team shortcut is now Play History on both dashboards; Captain links reuse the Player Dashboard's existing history view. The visible app version was bumped to `LMS-0530`.
- Captain Dashboard's prominent next-match Match Setup panel now looks up completion with the full match-and-team key, so saved complete lineups display Setup Complete instead of falsely remaining pending. Dashboard appearance preferences now resynchronize from browser storage on focus, page restore, cross-tab storage changes, and same-page dashboard changes so the Admin mobile drawer honors the saved light theme after resumed/mobile navigation. The visible app version was bumped to `LMS-0531`.
- Captain Dashboard no longer lists My Play History in its desktop or hamburger sidebars while retaining the requested mobile bottom Play History shortcut, and its Upcoming Match popup labels the email action Email Opposing Captains. Shared administration pages now apply the saved dashboard sidebar appearance to their desktop sidebar and mobile drawer. Player Match Lineup cards use stronger green/blue Team headers, and mobile profile footers across Player, Captain, Admin, and shared administration screens include a compact LWR PB Club System install/instructions control. The visible app version was bumped to `LMS-0532`.
- Captain Dashboard mobile bottom navigation now uses a Standings shortcut instead of Play History; tapping it opens the existing division standings popup. The visible app version was bumped to `LMS-0533`.
- Dashboard Appearance now offers a browser-saved collapsed desktop sidebar that expands on hover or keyboard focus without affecting mobile layout. Captain Match Setup shows individual rating range alongside the doubles-team maximum, uses one row for the two team-status pills with a full-width setup action, and Captain match-result audit details now appear as full-width Entered / Verified status lines that share a row when space allows. Captain standings summaries show the top six teams, and its Full View header displays the division's configured tiebreak sequence in place of the former Standings Points label. Captain team-roster dialogs now layer above both match-detail and match-results dialogs, and mobile dashboard navigation honors the sticky-header offset when scrolling to a section. The visible app version was bumped to `LMS-0538`.
- Collapsed desktop sidebars now close any expanded navigation group when the pointer leaves, preventing a retained submenu from creating blank space; submenu contents are hidden immediately while collapsed. The Player Division Standings dialog now matches Captain's header treatment by displaying the configured tiebreak sequence in the header and removing the redundant detail block. On touch devices in landscape, dashboards now retain the mobile navigation layout instead of rendering the collapsed desktop icon rail. Player Match Results now matches Captain's score audit summary with full-width Entered / Verified date, time, and member details. Player Recent Results now includes a Total Matches count badge, and My Team includes a Players count badge. Mobile Profile omits the desktop-only Collapse Sidebar preference through both responsive rendering and styling, Captain Match Tools now uses a compact two-pill Match Setup layout with a full-width action on small screens, and collapsed desktop sidebars now align the persistent system-footer copyright line to the icon rail. The Admin dashboard User Guide opens in the in-app viewer on desktop and now opens directly in the device's native PDF viewer on mobile, matching League Documents. Profile dialogs no longer include dashboard-switch shortcuts, while the Admin profile now includes the complete saved Dashboard Appearance controls. In Player and Captain Division Team Schedules, expanded match-detail game cards now use a subtle green or red result background matching their win/loss header, the winning team's roster panel now has a clear green outline, and the status chip now reads only Posted to DUPR or Not Posted to DUPR. Collapsed sidebars now remove the hidden submenu-chevron layout space so every navigation icon is centered consistently. The visible app version was bumped to `LMS-0553`.
- Admin Dashboard Club Operational Overview now labels completed match counts as verified matches and provides a three-way Average Team Rating toggle for DUPR Doubles, Season DUPR, and Age-Based team roster averages. The visible app version was bumped to `LMS-0555`.
- Captain Dashboard next-match and upcoming-match actions now read `Match Details` instead of `View Match`. The visible app version was bumped to `LMS-0556`.
- Admin Dashboard League Analytics now charts active teams by their home location instead of scheduled matches by location. The visible app version was bumped to `LMS-0557`.
- Player, Captain, and Admin standings bar-chart tooltips now consistently show the ranked team first, then its `Standings Points` record and rounded whole-number points. The visible app version was bumped to `LMS-0558`.
- Shared admin page banners omit their duplicate sidebar-group label for People & Teams, Match Operations, League Structure, and System Setup; the Welcome header retains the group context while each banner keeps its page title and description.
- The root-level `AppDialogProvider` provides branded, keyboard-accessible notice, confirmation, and typed-confirmation dialogs that preserve explicit acknowledgement while replacing browser dialogs. It also routes legacy `alert()` notices across the app to the branded Notice dialog; Admin Dashboard reminders, Dashboard Messages, guide saves, message-history deletion, Master Reset, and Season Reset use the explicit async dialog API. Migrate remaining legacy confirmation and prompt flows screen by screen.
- Team Roster Management now uses the branded typed `DELETE` confirmation for roster removals instead of a browser prompt. The visible app version was bumped to `LMS-0559`.
- Typed-confirmation dialogs suppress a duplicate body instruction when the same typed value is already shown as the bold input label.
- Admin Dashboard's Matches This Week helper now reads `Scheduled matches`. User Roles & Permissions restores Last Login sorting using the server-loaded Auth timestamp before pagination; Change Role remains a non-sortable action column. The visible app version was bumped to `LMS-0560`.
- User Roles & Permissions now gathers every matching 100-record directory page on the server before sorting by Last Login, so that sort applies to the complete filtered result while retaining the database function's pagination safeguard. The visible app version was bumped to `LMS-0561`.
- Captain and Admin dashboards now combine overdue unentered scores (scheduled before today and not cancelled) with pending score validations as `Pending Match Score Entry/Validations`. Captain action rows use the Upcoming Matches format and send overdue entries directly to score entry; Admin action rows open the corresponding match. The visible app version was bumped to `LMS-0562`.
- The Admin pending-score card now opens Scoring Operations with the exact counted matches selected. Initial overdue score entry is shown only on the home team's Captain dashboard, while pending validation remains visible to either captain. The visible app version was bumped to `LMS-0563`.
- The Captain dashboard's mobile-friendly Pending Match Score Entry/Validations count card is now tappable when it has items and scrolls to the pending-match list. The visible app version was bumped to `LMS-0564`.
- Player and Captain Division Standings dialogs now offer Tiebreak Details, showing the tied teams' cumulative verified-season metrics through the configured rule that separates them and explaining the resulting rank. The visible app version was bumped to `LMS-0565`.
- Division Standings shows the Tiebreak Details control only when two or more teams are tied on the division's primary ranking metric. The visible app version was bumped to `LMS-0566`.
- Documentation screenshots are maintained in `docs/screenshots/`. The current privacy-safe pack captures role-neutral page context only: it omits signed-in identity headers and all member, roster, score-entry, contact, and profile details.
- Team Roster Management labels players added without a required season rating as `Player Added (Temporary)` and tells captains that ineligible ratings will result in roster removal after verification.
- Season Ratings groups DUPR-focused filters in a collapsible `DUPR Filters` panel, including a selected-season check for players whose applicable Season DUPR, Age-Based, or Self Rating falls outside an active roster team's configured rating range. Blank range boundaries are ignored instead of being evaluated as zero. The visible app version was bumped to `LMS-0568`.
- Player Dashboard Next Match retains the match through its scheduled date, then advances to the next schedule date, including a bye. Captain Dashboard Next Match retains an unscored match through the day after it is played and advances immediately when scores are entered or on the second day after match day; it also displays its next scheduled bye. Both dashboards display `End of Regular Season - No More Matches` when no eligible match date remains. The visible app version was bumped to `LMS-0570`.
- Captain Dashboard Upcoming Matches highlights past matches whose scores are not verified with a red row and a `Scores Not Entered` or `Scores Not Verified` status label. The visible app version was bumped to `LMS-0571`.
- Flex League date/time changes on Captain Dashboard cannot select or save a time before the current local date/time, while retaining the existing one-week scheduling window. The visible app version was bumped to `LMS-0572`.
- Score Sheets now create a new copied format when an edited sheet's Name is changed, preserving the original format and its division assignments. A copied default sheet remains non-default so the original default is not changed. The visible app version was bumped to `LMS-0573`.
- Captain Dashboard Next Match treats a recorded team bye as a zero-day score grace period. It advances beyond the bye immediately after its date, and an obsolete scoreless match row for the same team-bye schedule slot cannot delay the following match. The visible app version was bumped to `LMS-0574`.
- Scheduling Administration now uses the branded, accessible in-app dialogs for every schedule-generation confirmation and typed `DELETE` safeguard, instead of browser-native confirmation or prompt windows. The visible app version was bumped to `LMS-0575`.
- Visual Schedule Editor now uses the branded, accessible in-app dialogs for publish/unpublish, home/away balancing, match deletion, and typed `RESET` score clearing confirmations. The visible app version was bumped to `LMS-0576`.

- League Communications adds a League Manager Match Operations tool for composing rich-text email, previewing the rendered message, selecting audience and the Admin Dashboard-equivalent scope, checking unique active recipient counts, sending through Brevo, and recording sender/content/count/status history. Apply lwrpc-admin/supabase-league-communications.sql before deploying. The visible app version was bumped to LMS-0577.

- Admin Dashboard System Setup now has a distinct gear icon rather than reusing League Structure's hierarchy icon. The visible app version was bumped to LMS-0578.
- Admin Dashboard System Setup now uses a conventional cog icon. LMS PWA registrations reload the active page once when a newly deployed service worker takes control, preventing an already-open installed app from continuing to display an older JavaScript bundle after deployment. The visible app version was bumped to `LMS-0580`.
- Player and Captain Match Details now use a clickable Location card to open its address map in a new tab, with a `Click for Map` cue; the former map and Done buttons were removed.
- Shared Admin-area headers now open the configured Admin User Guide PDF for League Managers and Commissioners instead of routing to an empty role Help Center. The shared sidebar now correctly maps League Structure to the hierarchy icon and System Setup to the settings cog. The visible app version was bumped to `LMS-0581`.
- Player Dashboard Recent Results now includes past, published team matches even when their scores have not been entered, with a clear `Scores Not Entered` status. Those overdue matches remain excluded from Upcoming Schedule. The visible app version was bumped to `LMS-0582`.
- League Communications Preview Body now mirrors the HTML that will actually be sent instead of automatically adding a logo, date, and repeated subject. A `Date` editor control inserts the current date at the sender's chosen position. The visible app version was bumped to `LMS-0583`.
- League Communications now uses the same compact Arial line spacing in its editor preview and delivered HTML. Its toolbar can insert the standard LWRPC League Management signature and attach one file up to 3 MB for Brevo delivery; Communication History records the attachment filename. The visible app version was bumped to `LMS-0584`.
- User Guides and League Documents now share an app-controlled PDF preview with text indexing, a compact `Find` prompt, total result count, previous/next result navigation, yellow match highlighting, and page navigation. Download, Print, and Close remain available. The visible app version was bumped to `LMS-0585`.
- PDF Find now distinguishes the selected occurrence with an orange outline/background, keeps other page matches yellow, automatically scrolls the active occurrence into view, and uses layout-neutral highlights so adjacent letters no longer shift or split visually. The visible app version was bumped to `LMS-0586`.
- PDF Find highlights now use translucent, text-transparent overlays so the PDF canvas remains the only visible lettering, eliminating doubled or clipped glyphs while preserving orange active and yellow secondary highlights. The visible app version was bumped to `LMS-0587`.
- Member Detail now provides compact copy-to-clipboard controls beside the visible Email and Phone values. Members Administration retains its active search, sort, current-roster/inactive filters, and page during the browser session when returning from a member detail. Its exit confirmation uses the branded app dialog. The visible app version was bumped to `LMS-0588`.
- Shared unsaved-changes navigation now uses the branded app dialog across every shared-helper caller. Confirmation dialogs default to the lower-risk Cancel/Keep Editing action; leaving without saving requires an explicit warning-action choice. The visible app version was bumped to `LMS-0589`.
- Player Dashboard Play History now provides a `More Player Details` control below its summary cards. It shows scope-specific match, game, point, recorded-rating, and last-five matchup statistics; estimated ratings clearly state their performance/strength basis. The visible app version was bumped to `LMS-0590`.
- Player Dashboard Play History now opens More Player Details in a separately scrollable popup from its header. The five detail areas use a consistent, proportional card layout. Matches with unposted results now show `Scores Not Entered`, and the summary tracks `Scores Pending` instead of a vague Other category. The visible app version was bumped to `LMS-0591`.
- On mobile, the My Play History popup keeps its `More Player Details` and `Close` actions together on one equal-width row below the scope selector. The visible app version was bumped to `LMS-0592`.
- Captain Dashboard now places Match Setup status and its action within the Next Match hero block, keeping match planning actions together. The visible app version was bumped to `LMS-0593`.
- Player and Captain Dashboard Next Match labels now use the same eyebrow-style treatment as dashboard section labels instead of a pill. Captain Match Setup omits the duplicate matchup line and separates each team name from its setup status with an em dash. The visible app version was bumped to `LMS-0594`.
- Captain Dashboard Match Setup now uses a blue gradient readiness heading and a soft blue panel background, giving the lineup status block stronger visual hierarchy. The visible app version was bumped to `LMS-0595`.
- Member Administration keeps its member count to the right of its compact action row. Season Ratings and Teams & Rosters align their count cards at the right edge, while User Roles & Permissions now has a full-sized member count card and a Clear Search action that restores the default member sort. The visible app version was bumped to `LMS-0596`.
- Season Ratings places Current Rosters Only, DUPR Filters, and Data Tools directly before the Players count card in its header. The visible app version was bumped to `LMS-0597`.
- Season Ratings and User Roles & Permissions desktop tables now use Member Administration's sticky column-header behavior during page scrolling. The visible app version was bumped to `LMS-0598`.
- Season Ratings mobile rows no longer repeat a column-header strip. User Roles & Permissions now uses mobile member cards with member, location, last-login, current-role, and role-change controls instead of a narrow table/header. The visible app version was bumped to `LMS-0599`.
- Member Administration now keeps its member count and action controls on one desktop header row, removing excess vertical space before the Search Members field while preserving the mobile two-column controls.
- Match score entry treats Picklebreaker lines as team-result-only: player selection and player-rating validation are not required, tied regular-line team points require a Picklebreaker score, and untied results clearly mark the Picklebreaker as not needed.
- The embedded Enter Match Scores window now confirms that saved entries have not yet been submitted when closed. Invalid game scores are identified immediately with a red game block and an explanation beside that game's Winner result; extended win-by-two scores must finish by exactly two points. The visible app version was bumped to `LMS-0600`.
- Captain Dashboard Match Setup now gives each lineup team a distinct bordered card with a labeled header, stronger color separation, and elevation. The visible app version was bumped to `LMS-0601`.
- The saved-but-unsubmitted score-entry close confirmation now clearly directs captains to press Submit for official score submission. The visible app version was bumped to `LMS-0602`.
- Picklebreaker score-entry labels now change from Optional to Required as soon as entered regular-line points are tied, while untied results remain Not needed. Score-validation messages identify the lineup game and ordinal played game separately. The visible app version was bumped to `LMS-0603`.
- Score entry now hydrates missing division-line settings for legacy matches by line number, so an unplayed Picklebreaker bonus is included in the live Match Score preview. The visible app version was bumped to `LMS-0604`.
- The Enter Match Scores header now shows a live Match Score. Its prior large score card remains on desktop but is hidden on narrow screens to avoid duplication. The visible app version was bumped to `LMS-0605`.
- Resubmitting already-submitted Match Scores now uses the branded in-app confirmation dialog instead of a browser-native popup. The visible app version was bumped to `LMS-0606`.
- Captain Dashboard Upcoming/Unverified Matches now clearly marks pending-verification cards as `Scores Not Verified` and past unentered cards as `Scores Not Entered`, while Action Needed remains limited to the selected team's required actions. The visible app version was bumped to `LMS-0607`.
- Captain Dashboard preview Upcoming Matches now flags same-day submitted-but-unverified scores immediately, rather than waiting until the next day. The visible app version was bumped to `LMS-0608`.
- Match Score Entry game headers now use a stronger soft-blue background to distinguish game information from the team-entry panels. The visible app version was bumped to `LMS-0609`.
- Match Score Entry game headers now use a deeper blue band for stronger contrast against individual game panels. The visible app version was bumped to `LMS-0610`.
- The desktop Enter Match Scores header now places its live Match Score beneath the action buttons, aligned with the match date and location; mobile retains the compact action-row score. The visible app version was bumped to `LMS-0611`.
- Admin Dashboard People & Teams now includes Division Standings and Division Schedules tools. Each opens a current-division selector popup; standings provides the complete division team table and schedules reuse the player-facing division schedule view. The visible app version was bumped to `LMS-0612`.
- Admin Dashboard's active design shell now dispatches Division Standings and Division Schedules to their respective popups instead of incorrectly treating them as Reset dialogs. The visible app version was bumped to `LMS-0613`.
- Division Standings no longer shows Matchups Tied, and its Rank and Team cells stay visible while the standings table scrolls horizontally on mobile. The visible app version was bumped to `LMS-0614`.
- Admin Division Schedules now loads the recorded home and away match-line players, so Match Details shows verified lineups rather than incorrectly reporting Players not entered. The visible app version was bumped to `LMS-0615`.
- Division Team Standings now correctly labels its game record column as Games Won-Lost. The visible app version was bumped to `LMS-0616`.
- Delete Generated Schedule now explains the scheduled-data impact and continues to a match count first; it requires typing `DELETE` only once, after the final count is known. The destructive-flow audit confirmed no other workflow requires `DELETE` more than once. The visible app version was bumped to `LMS-0617`.
- DUPR exports now exclude Picklebreaker lines and any line containing a forfeit-status game, preventing those results from being included in the generated CSV. The visible app version was bumped to `LMS-0618`.
- Division Standings Tiebreak Order now labels and evaluates `line_wins` as Line/Game Win % (line wins divided by total lines played) and `game_wins` as Match Win % (match wins divided by total matchups played). Rebuild ranking, live standings sorting, captain/player tiebreak explanations, and percentage displays share the same calculation. Stored rule keys remain compatible with existing divisions. The visible app version was bumped to `LMS-0619`.
- PBCC player-name corrections now synchronize the saved player record with every joined match roster entry, so corrected spellings appear in current matches, match history, and exports.
- PBCC refreshes the latest joined, declined, waitlist, and invited-player data before opening a match's player list in either Player or Admin view. The visible app version was bumped to `LMS-0620`.
- PBCC six-game scheduling now uses session-wide balanced tournament designs for two-court groups of eight or nine players. Eight-player sessions give every player three games per court, use six unique partners, and limit any opponent to three games; nine-player sessions rotate six distinct byes, keep partners unique, limit opponents to two games, and keep court assignments within two games. Generic schedules also avoid any prior partner while a unique pairing remains feasible. The visible app version was bumped to `LMS-0621`.
- PBCC final match-night results now always use SMS delivery for players with phone numbers, bypassing app-notification preferences for both automatic completion sends and manual result resends. Other PBCC messages retain their existing push-first behavior. Partner Comparison names now load the clicked player's complete regular-match history across all of that player's partners, with Current Month, Last Month, Current Year, and All Time filters for dates played, last played, record, win percentage, and point differential. The comparison header explains that player names are clickable. The visible app version was bumped to `LMS-0623`.
- Admin Dashboard sidebar now includes a League Documents accordion that shows the published Weekday League documents and opens them through the shared PDF viewer.
- PBCC now routes every completion, delete, reset, SMS, validation, and DUPR export prompt through the branded app dialog instead of browser-native Windows dialogs.
- League Administration and Division Administration edit forms now expose an Active status control, so inactive leagues and divisions can be reactivated directly. The visible app version was bumped to `LMS-0624`.
- Shared typed-delete confirmations now use the branded app dialog across League, Division, configured game line, Location, Season, Team, Score Sheet, and Scoring deletion workflows. The visible app version was bumped to `LMS-0625`.
- Member Detail deactivation now uses the shared branded in-app confirmation dialog instead of a browser-native confirmation window. The native-dialog audit still identifies other legacy confirmations on unrelated screens for separate migration.
- All remaining direct browser confirmation and prompt flows now use the shared branded in-app dialog service. This includes Member, Division, Rating, Scoring, Standings, League Communications, App Notifications, Match Dispute, and Tournament System workflows; copy fallbacks now provide a selectable in-app field instead of a browser prompt.
- Dashboard Guides and League Administration now list direct PDF objects from public `documents/league-documents`, save full Storage paths, normalize legacy `private/` values, and use Supabase public URLs when opening league or dashboard guides. The visible app version was bumped to `LMS-0632`.
- Member Administration now includes a protected, sortable Last Login column and a compact two-row Actions layout. The User Roles screen and sidebar entry are retired; roles remain editable on each member's detail page. Inactive sort pills are hidden across Member Administration and Season Ratings. The visible app version was bumped to `LMS-0633`.
- Member Administration's desktop table now fits within its content area: Phone, DUPR ID, and Role columns use narrower widths and padding while retaining full values, and the two-row Actions grid remains visible. The visible app version was bumped to `LMS-0634`.
- Member Administration gives the Actions column another 20px from the compact Role column, increasing action-button height and horizontal padding while keeping long role names visible. The visible app version was bumped to `LMS-0635`.
- Member Administration now explicitly reserves 220px for Actions, with full-width two-column buttons at a 40px height and larger labels. The Role column is reduced to a wrapping compact column to keep the table's total width unchanged. The visible app version was bumped to `LMS-0636`.
- Member Administration now offers compact History and Ratings actions for every member. History opens the shared play-history popup, whose More Player Details action now displays the selected member's match, game, point, recorded-rating, estimated-rating, and recent-matchup analysis. Ratings opens a season-rating summary with an Edit Ratings handoff to the existing focused ratings workflow.
- Captain Dashboard no longer redirects to League Standings when the signed-in user has no current captain-team assignment; it remains on the Captain Dashboard and displays its empty-team state.
- PBCC non-host Match Players now uses the host-style selectable Joined, Declined, Waitlist, and Invited count tabs. Its player roster remains read-only, with no host response-management controls. The visible app version was bumped to `LMS-0637`.
- Login email and password fields explicitly use dark text, keeping typed credentials readable when the device is set to dark mode while the sign-in card remains light. The visible app version was bumped to `LMS-0638`.
- The shared signed-in Profile dialog now places a `?` beside the member's name. It opens a member-information popup showing the signed-in player's phone, community, DUPR ID, and membership renewal date. The visible app version was bumped to `LMS-0639`.
- Profile member-information popups now include a bottom-left help note with a `contact us` mailto link that uses the configured support email. The visible app version was bumped to `LMS-0640`.
- Every My Play History `More Player Details` popup now uses shared animated Recharts analytics: a match-outcome donut, match/game win-loss comparison, recent point-differential trend, and recorded-rating context. The visible app version was bumped to `LMS-0641`.
- League Administration now labels the existing league document slot `League Important Dates` instead of `Blank Score Sheet`. The visible app version was bumped to `LMS-0642`.
- Player Dashboard League Documents now consistently excludes the Captains Guide, including when a captain views the Player Dashboard. The visible app version was bumped to `LMS-0643`.
- Player Dashboard League Documents includes League Important Dates while continuing to exclude the Captains Guide. The visible app version was bumped to `LMS-0644`.
- Member Detail now provides a private Notes field only while editing a member. The accompanying CSV import maps every source row by normalized email and records each unique `League / Division / Team` value on its own Notes line, preserving any existing Notes text. Apply `lwrpc-admin/supabase-member-notes.sql` before running `scripts/import-member-notes.mjs`. The visible app version was bumped to `LMS-0645`.
- Member Detail now also displays the member Notes value in its standard information panel, preserving line breaks. The visible app version was bumped to `LMS-0646`.
- Every authenticated LMS role, including Players, Captains, League Managers, and Commissioners, now has a four-hour inactivity timeout. A persistent branded warning appears with 15 minutes remaining and offers Stay Signed In; expiry signs out only the current browser session and returns the member to sign-in. The visible app version was bumped to `LMS-0647`.
- The inactivity sign-out notice now uses the dialog title `Signed Out`, while genuine sign-in errors continue to show `Sign In Problem`.
- Account Security now uses a neutral `Return to System` label and returns members to the screen that opened it. The visible app version was bumped to `LMS-0648`.
- Tournament Public Display standings now use a compact layout, keeping its ranking explanation alongside the title and active-division count. Every rotating display view can hide the sticky display header and restore it from that view's top information strip. The visible app version was bumped to `LMS-0649`.
- Member Administration Data Tools now explain Clean Members and Correct Roles before any data lookup or write, with their actions arranged in a compact two-column desktop grid. The broad Mark All Inactive action is no longer available there; inactive handling remains scoped to the MembershipWorks import workflow. Season Ratings Clean Ratings retains its Start of Season Clean path and adds an unchecked review mode that calculates without writing, lists only potential changes, and lets a League Manager select individual changes before applying them. The visible app version was bumped to `LMS-0650`, with a new LMS PWA static-cache revision to refresh installed clients after deployment.
- Inactivity sign-out now retains the PBCourtCommand context: expiring from PBCC returns the user to the PBCC player screen after re-authentication, while expiration from the main LMS continues to use the member's role-based dashboard. The post-sign-in target is restricted to the known PBCC route.
- The inactivity sign-out notice now uses the configured club logo in a branded dialog header, while other sign-in errors retain their warning indicator.
- PBCourtCommand's Add Player forms now check the complete in-group Saved Players directory and prior match-player records after a new name is entered, filling the phone field only when that exact name resolves to one phone number. In the mobile host flow, lookup begins shortly after a complete name is typed rather than waiting for the field to lose focus. The host-facing lookup is available only to the verified host or co-host assigned to that match.
- Season Ratings CSV import now resolves each member's existing season-rating record before deciding whether an age-based rating should be updated or cleared. This fixes the `Cannot access 'l' before initialization` import failure with DUPR member-list exports. The visible app version was bumped to `LMS-0651`.
- Season Ratings import now compares member and season IDs consistently when determining whether to update or insert a rating, preventing duplicate `member_id`/`season_id` inserts. The visible app version was bumped to `LMS-0652`.
- Season Ratings CSV import now uses the database's `member_id,season_id` conflict target to atomically insert new ratings or update existing ratings. It also coalesces duplicate matched-player rows before writing. This prevents unique-constraint failures if imported data has changed since the preview loaded. The visible app version was bumped to `LMS-0653`.
- Ratings Import now uses a two-dialog workflow: Continue/Cancel before choosing a CSV, then an Apply Ratings Import confirmation after the matched rows are previewed. The separate screen-level Apply button was removed. CSV imports only fill blank DUPR Doubles values, preserving existing ones; Delete Season Ratings remains the intentional way to clear a season and import anew. The visible app version was bumped to `LMS-0655`.
- Season Ratings Data Tools now groups Upload Ratings CSV, Clean Ratings, and Delete Season Ratings together. When a DUPR CSV has no usable 65+ age-based rating and imports its 50+ fallback instead, the player's DUPR Notes records that source. The visible app version was bumped to `LMS-0656`.
- Season Ratings and Membership Administration now show import, cleanup, and update outcomes in high-contrast status panels, with clear success, in-progress, and error treatments instead of low-contrast white result boxes. The visible app version was bumped to `LMS-0657`.
- Tournament Public Display Court Detail and Courts Only cards now place a larger, centered division-color pill beneath the court heading, while retaining the compact match-line indicator at the top. The visible app version was bumped to `LMS-0658`.
- Tournament Public Display court cards now keep their original compact height: the larger division-color pill is positioned in the top center without consuming layout space. The visible app version was bumped to `LMS-0659`.
- Tournament Public Display Current Standings now makes each rank a clickable celebration trigger. It opens a full-screen team presentation with tournament and division names, a large rank medal, team name, and player names; clicking anywhere returns to Current Standings. The visible app version was bumped to `LMS-0660`.
- Tournament Public Display rank celebrations now resolve and list both team members directly from the tournament team record, use a much larger division pill and placement label, and feature a large trophy with rank plaque. The return instruction line was removed while click-anywhere return remains available. The visible app version was bumped to `LMS-0661`.
- Tournament standings now aggregate every unique player name across same-division line records that share one team name. Rank celebrations therefore list the entire team, including multi-line teams such as three lines with six players. The visible app version was bumped to `LMS-0662`.
- Tournament rank celebrations now adapt team-name size to its length, reduce the trophy footprint for compact display layouts, and scale roster text for larger teams so names remain visible. The separate placement heading was removed, and player names are separated with vertical bars. The visible app version was bumped to `LMS-0663`.
- Tournament rank celebrations now present the division as a compact amber text label rather than a large pill, preserving more space for the team and roster. The visible app version was bumped to `LMS-0664`.
- Tournament rank celebrations now remove the redundant top heading and place the club's main logo beside the trophy at the same responsive size. The visible app version was bumped to `LMS-0665`.
- Tournament rank celebrations now use a larger compact division label for clearer distance viewing. The visible app version was bumped to `LMS-0666`.
- Password-reset recovery now explicitly uses Supabase's implicit browser flow and verifies the recovery session before enabling a password update. An unavailable or expired reset session now gives a clear request-a-new-link message rather than exposing the raw `Auth session missing!` error. The visible app version was bumped to `LMS-0667`.
- PBCourtCommand now keeps every Playing match visible to its host or co-host after re-login, including across the evening UTC date boundary, until Finish Match is used. Its round generator now searches all valid partner combinations before allowing a repeat, preventing repeated partners whenever a no-repeat matchup exists. The visible app version was bumped to `LMS-0668`.
- PBCourtCommand court cards now make saved scores read-only until the host chooses Edit Score. The host can then save corrected scores or use Delete Scores to clear both values and reopen that game for scoring. The visible app version was bumped to `LMS-0669`.
- PBCourtCommand's next-round planner now evaluates the complete available match history. It keeps past partners unavailable whenever a unique partner is possible, avoids exact repeat four-player courts, minimizes recurring player groups, and accounts for each player's court history when assigning games across multiple courts. The visible app version was bumped to `LMS-0670`.
- Tournament Public Display now detects when a header-hidden rotating view exceeds the viewport. It pauses briefly at the top, scrolls smoothly through the full display during its 30-second rotation window, then starts the next view at the top. The visible app version was bumped to `LMS-0671`.
- Tournament Division refresh from the Main System no longer requests a nonexistent `tournament_divisions.seed` column; it now loads only the division fields used by the sync. The visible app version was bumped to `LMS-0672`.
- PBCC next-round selection now treats every unchecked joined player as an explicit bye for that round. The planner saves those players in the round's bye list, includes the byes in standings/history, displays them in the live round, and labels them in the player-selection dialog. The visible app version was bumped to `LMS-0673`.
- Tournament Public Display expanded mode can now be exited with Escape or a pointer click anywhere in the display, restoring the display header and its controls while returning the display to the top. Court Detail and Courts Only now render the `vs` separator in contrasting blue text for better distance viewing. The visible app version was bumped to `LMS-0675`.
- Tournament Public Display expanded mode now waits for the next pointer interaction before restoring the header, so activating Expand Display does not immediately undo itself as its own click finishes bubbling. The visible app version was bumped to `LMS-0676`.
- Member Detail now offers a typed-confirmation **Delete Member** action only for inactive members. It calls a service-role-protected, atomic database function that removes member-linked records and match-line/game history, preserves shared team/location/match records without the deleted member's attribution, then removes eligible profile photos and login accounts. Apply `lwrpc-admin/supabase-delete-inactive-member.sql` before deploying. The visible app version was bumped to `LMS-0677`.
- Member Detail now places **Delete Member** beside **Show Player History** whenever the viewed member is inactive, making the permanent-action entry point easier to find while retaining its typed confirmation. The visible app version was bumped to `LMS-0678`.
- LMS, Tournament, and PBCC now share Brevo transactional SMS delivery through `sendSmsMessages`, reusing `BREVO_API_KEY` from email and requiring a Brevo-approved `BREVO_SMS_SENDER`; PBCC retains its push-first fallback behavior. Email Options now includes a Brevo SMS configuration check and the visible app version was bumped to `LMS-0679`.
- PBCC final match-night result texts now list each player by first name and last initial with only their win-loss record and bye count; points, point differential, rank, and ladder-position details are omitted.
- Season Ratings DUPR Filters now includes a **New Members** toggle that lists active members added in the last seven days, making their rating information easier to review. The visible app version was bumped to `LMS-0680`.
- PBCC New Match texts now send the saved template content without automatically appending the app-notification instruction. Managers can include that instruction in the template whenever it is wanted. The visible app version was bumped to `LMS-0681`.
- PBCC Text Template tests now force a Brevo SMS instead of using the app-notification path first, and report/log a provider failure or zero-delivery result rather than claiming that the test was sent. The visible app version was bumped to `LMS-0682`.
- Ask LWR Pickleball AI Stage 1 now has an applied Supabase schema for an official-document catalog, safe PDF version metadata, private server-only chunks/embeddings, per-chunk search exclusion, and atomic ready-version activation. Existing LMS PDF objects and links remain unchanged. The later user interface will use the global top-page AI drawer as its primary entry point while retaining Browse Guides & Rules access.
- Ask LWR Pickleball AI Stage 2 now provides League Manager/Commissioner-only document processing: secure managed PDF uploads, server-side PDF.js text extraction, logical page/rule-aware chunks, OpenAI embeddings, immutable replacement/reprocessing versions, explicit ready-version activation, and an extracted-content preview with per-chunk AI exclusion. Apply `lwrpc-admin/supabase-ai-assistant-stage2.sql` after Stage 1 before using the new management screen.
- Ask LWR Pickleball AI Stage 2 PDF extraction now uses PDF.js's Node-oriented legacy build with a minimal server-only `DOMMatrix` compatibility class before that module loads. This avoids its optional canvas package becoming a production prerequisite for text-only extraction; no browser DOM or rendering path is used. A regression test extracts a real generated PDF with browser DOM globals absent. The initial production failure record was verified as failed, inactive, and chunk-free.
- Ask LWR Pickleball AI Stage 2 now preloads PDF.js's bundled `WorkerMessageHandler` in the Node route before text extraction. PDF.js 5.3.93 otherwise dynamically imports `./pdf.worker.mjs` for its Node loopback worker, which Turbopack does not emit at the runtime `.next/server/chunks` path. The server bundle is checked for the preloaded handler, and a real-PDF regression poisons that old Vercel path to verify it is never resolved. The first two production processing versions remain failed, inactive, and chunk-free.
- Ask LWR Pickleball AI Stage 2 logical chunking now reconstructs PDF visual lines, strips safe repeated page furniture, separates rule/section-aware chunks, and retains table-of-contents chunks only for preview with `is_searchable = false`. Searchable chunks alone receive embeddings. `supabase-ai-assistant-stage2.1-logical-chunking.sql` keeps activation safe by requiring embeddings only for searchable chunks; it otherwise retains the existing immutable-version and activation behavior. The first ready DUPR League Rules version remains inactive pending a new reprocessed version review.
- Ask LWR Pickleball AI Stage 2.2 now normalizes text at extraction time, before logical chunking and embeddings. It converts only structurally identified bullet glyph runs, normalizes unusual spaces/zero-width characters, handles soft-hyphen line-break joins, and has a narrowly verified repair for the observed broken `ff` ligature mapping (`o` + U+01AF + `i`). Unresolved replacement, private-use, or unusual embedded-glyph artifacts become processing warnings for administrator review; existing active/ready versions are never modified in place.
- Ask LWR Pickleball AI Stage 3 adds a manager/commissioner-only Test AI Assistant console and a server-only, no-chat hybrid retrieval path. It uses one configured embedding query plus a protected service-role RPC, ranks only eligible active/current/ready/searchable chunks, and exposes semantic, keyword, exact-term, catalog-authority, and soft-context scores for review before any answer generation is approved.
- Ask LWR Pickleball AI Stage 3 catalog management now resets the separate Add Official PDF form only after a successful version activation. Authorized managers can edit validated parent-document metadata without changing a PDF, version, chunk, embedding, or active-version selection; scope references are checked server-side before retrieval-affecting catalog metadata is saved.
- Ask LWR Pickleball AI Stage 3 retrieval now derives bounded exact phrase and distinctive-term candidates from the user's query instead of relying on an evaluation-term list. Rule numbers and standalone acronyms remain especially strong; generic one-word club terms do not receive an exact-match boost.
- Ask LWR Pickleball AI Stage 3 retrieval now removes generic question framing before its PostgreSQL full-text query, so natural-language prompts such as `How does scoring freeze work?` search the substantive terms rather than requiring an unrelated intent word. The manager-only Test AI Assistant console now shows an exact-match reason and normalized FTS match diagnostics for each candidate. The visible app version was bumped to `LMS-0686`.
- Ask LWR Pickleball AI Stage 3 retrieval now treats ordinary one-word prose as non-exact evidence unless the document itself presents the term structurally as a heading, parenthetical official label, or inline `Label:` field. It removes conversational FTS noise and adds a bounded, generic interruption/continuation expansion to `cannot complete`, without injecting any LWR outcome term. This improves natural-language retrieval while retaining the existing active-version security filters, evidence threshold, and hybrid weights. The visible app version was bumped to `LMS-0687`.
- Ask LWR Pickleball AI Stage 3 retrieval now distinguishes deadline/requirement questions from procedural/how-to questions using generic query intent and direct evidence patterns. Concrete timing language in a directly matching numbered rule receives the strongest existing keyword-relevance signal; less-specific timing and direct procedural instructions receive bounded corroboration. The existing 47/24/19/6/4 weights and `.350` threshold remain unchanged, while the manager console shows detected intent and its evidence match for every candidate. The visible app version was bumped to `LMS-0688`.
- Ask LWR Pickleball AI Stage 3 retrieval now recognizes generic interpersonal-conduct questions and corroborates only chunks that contain both a behavioral standard and a concrete misconduct example or a prohibition directed at people. It never selects evidence by document title or authority rank, preserving relevance-first behavior for conduct, Rules, and guide content. The existing hybrid weights and evidence threshold remain unchanged. The visible app version was bumped to `LMS-0689`.
- Ask LWR Pickleball AI Stage 3 exact matching now excludes the generic relational term `another` from distinctive phrase and structural-label candidates. This prevents a conversational fragment such as `to another` from receiving a strong exact-match boost while retaining rule-number, acronym, meaningful phrase, and structural-label matching. The visible app version was bumped to `LMS-0690`.
- Ask LWR Pickleball AI Stage 4 now provides manager-only, evidence-gated answer generation from active official document chunks. The server skips OpenAI generation when Stage 3 evidence is insufficient, validates each selected chunk against its active ready document version, attaches trusted citations and time-limited official PDF links outside the model, and exposes answer, source, conflict, timing, token, and cost diagnostics in the Test AI Assistant console. A future LWR Pickleball AI Assistant Administration Guide should document catalog management, processing, chunk review, activation, source testing, answer testing, authority guidance, and troubleshooting. The visible app version was bumped to `LMS-0691`.
- Ask LWR Pickleball AI Stage 4 source validation now explicitly uses the `ai_document_versions_document_id_fkey` ownership relationship when embedding the catalog document. It also rechecks that every cited chunk belongs to that exact active ready version and remains searchable before signing its PDF URL. The visible app version was bumped to `LMS-0692`.
- Ask LWR Pickleball AI Stage 4 now extracts strict JSON Schema output from the native Responses API message/content array rather than relying only on a convenience `output_text` field. It distinguishes safe diagnostics for API failures, refusals, incomplete token-limited responses, missing structured output, schema failures, and parse failures without exposing prompts or raw model output. The visible app version was bumped to `LMS-0693`.
- Ask LWR Pickleball AI Stage 4 completed final production-tested cleanup. Evidence selection now keeps the smallest useful official set: a complete numbered requirement remains primary, same-rule continuation and explicitly requested procedure remain eligible support, and merely related near-score candidates are excluded. Selected Rules are labeled Primary / controlling while Guides remain supporting procedural evidence; the manager console displays each candidate's selection status and reason. Trusted citation labels remove duplicated rule metadata. Production evaluation passed incomplete-match/injury, NR, Match Balls, Match Setup deadline, and player-conduct questions using active validated official documents and signed PDF citations; insufficient-evidence and conflict protections remain enforced. Stage 5 has not begun. The future League Management guide remains required for catalog metadata, upload/process/review/exclude/activate, replacement/version history, retirement, and post-change retrieval testing. The visible app version was bumped to `LMS-0694`.
- Ask LWR Pickleball AI Stage 5 adds the authenticated player-facing experience without changing the Stage 3/4 retrieval or answer pipeline. The global header help entry is now an accessible AI icon that opens a responsive right-side drawer (near-full-screen on mobile); `/ask-lwr` reuses the same interface for direct links. It provides context-aware suggested questions, local-only current-session exchanges, trusted official citations and signed page-aware links, a polished loading/error/fallback presentation, and Browse Guides & Rules using existing public LMS guide configuration and role visibility. The player endpoint returns only an answer and trusted citation fields, keeps the OpenAI/service role/server retrieval path private, and treats supplied page context only as a relevance hint. It deliberately returns the existing insufficient-evidence fallback for personal/team/live operational questions until a future authorized deterministic LMS-data tool layer can route `policy/document question → RAG` separately from `personal/team/live LMS question → structured data`. Stage 6 conversation/feedback, Stage 7 analytics/review, and Stage 8 hardening were not started. The future League Management guide remains required for official-PDF catalog operation and post-change retrieval testing. The visible app version was bumped to `LMS-0695`.
- Ask LWR Pickleball Club AI Stage 5 corrective update keeps the validated Stage 3/4 pipeline shared by management and player routes, then removes the generic conversational word `kind` from the AND-based FTS query framing. This lets `What kind of balls are we using?` search the substantive term `balls`, preserving the existing hybrid weights and `.350` threshold. Page/module context remains only a soft boost; Member Administration is now accurately identified rather than being mislabeled from the user's role. Browse Guides & Rules now mirrors existing LMS entry-point roles: Player Guide for players, Player + Captain Guides for captain-capable roles (including Club Pro, which already uses the Captain Dashboard), and all three dashboard guides for League Managers/Commissioners; league-document visibility follows the same guide-access rules and remains separate from RAG eligibility. User-facing branding is now `Ask LWR Pickleball Club AI`. The composer is fixed above the local-only conversation history, which displays newest exchanges first with loading/errors attached to their own submitted question; Browse Guides & Rules remains below the transcript. Stage 6, Stage 7, and Stage 8 remain unstarted. The visible app version was bumped to `LMS-0696`.
- Ask LWR Pickleball Club AI Stage 5 citation-link correction keeps trusted Stage 4 citation validation intact. After a private official PDF receives its 15-minute signed Storage URL, a positive integer page number from that exact validated chunk is appended solely as the browser fragment `#page=N`; the signed path/query remains untouched. Invalid or absent pages add no fragment. The player-safe response and React citation link preserve the server-issued URL without rebuilding it. The visible app version was bumped to `LMS-0697`.
- Ask LWR Pickleball Club AI Stage 5 now opens citations in an LMS-controlled viewer rather than the native PDF viewer. Player links carry only an encrypted, user-bound, 15-minute opaque token. The viewer revalidates the active document/version/searchable chunk server-side and streams the PDF through an authenticated LMS route to the existing `react-pdf` controls, which explicitly select the cited chunk's real page and safely clamp invalid/out-of-range pages to Page 1 or the final available page. The viewer never exposes Storage paths, direct signed URLs, or catalog identifiers in player-visible URLs. Supporting copy now says `Get answers from official LWR Pickleball Club information`; the composer prompt is broader; the redundant New Conversation control was removed. Stage 6 remains unstarted. The visible app version was bumped to `LMS-0698`.
- Ask LWR Pickleball Club AI Stage 5 now uses one shared authenticated AI trigger in the standard `AppHeader`, active Player/Captain/Admin dashboard shells, and the captain-only mobile score-entry shell. The old dashboard `?` controls opened only role-specific guides and are replaced by the assistant, whose role-aware Browse Guides & Rules preserves those guide paths. The separate `?` inside the signed-in profile remains member-information help, not a header guide control. The controlled official-document viewer now includes a Close button: it closes its citation tab when permitted, otherwise returns through browser history or `/ask-lwr`. Stage 6 remains unstarted. The visible app version was bumped to `LMS-0699`.
- Ask LWR Pickleball Club AI Stage 5 retrieval correction adds a bounded, document-grounded Match Setup terminology bridge for official guidance about lineups, rosters, and player pairings. It activates only for a configuration action, timing, or procedural request; requires active official content to explicitly connect that configuration terminology to Match Setup; and refuses the expansion when a remaining distinctive query term is absent from the official corpus. Rule 5.4-style concrete Match Setup roster deadlines receive the existing keyword-relevance signal, while Match Setup procedural instructions can support how-to questions. The player route now distinguishes a personal live-lineup request from a lineup deadline/procedure question so the latter reaches the same evidence-gated Stage 3/4 pipeline. Bounded typo normalization is vocabulary-derived from retrieved official candidates, never changes acronyms, rule numbers, dates, scores, or numeric thresholds, and is visible separately in manager diagnostics. Existing hybrid weights, `.350` threshold, authority logic, active-version eligibility, and Stage 4 grounding remain unchanged. Stage 6 remains unstarted. The visible app version was bumped to `LMS-0700`.
- Ask LWR Pickleball Club AI Stage 5 retrieval now distinguishes team/season roster management from individual-match Match Setup. Team roster operations require explicit add/remove/delete/drop/update/lock/open/close or timing language and can promote active official roster-management evidence with dates; Match Setup requires match-specific wording and cannot activate from `roster` alone. Weekday, PrimeTime, and Saturday text in eligible evidence provides a soft relevance boost only. The active multi-league Important Dates catalog metadata remains unchanged. Stage 6 remains unstarted. The visible app version was bumped to `LMS-0701`.
- Ask LWR Pickleball Club AI Stage 5 now routes an official team-roster membership timing question—such as adding players to a Weekday team—through the evidence-gated retrieval path instead of misclassifying it as a personal live-team lookup. The production retrieval already ranked Weekday Important Dates first; match-specific player selection remains distinct. Stage 6 remains unstarted. The visible app version was bumped to `LMS-0702`.
- Ask LWR Pickleball Club AI Stage 5 now preserves evidence for each independently detected official concept in a compound roster-and-Match-Setup question. Stage 4 recognizes normal roster-management wording including plural `rosters`, update/updating variants, and abbreviated/full month labels, then retains the strongest direct roster/date evidence and direct Match Setup evidence while pruning candidates that support neither intent (such as score-entry material). Selected evidence carries its supported intent into manager diagnostics and the model-only evidence payload; trusted citations derive only from that final selected set. Stage 5 remains pending production acceptance and Stage 6 remains unstarted. The visible app version was bumped to `LMS-0703`.
- Ask LWR Pickleball Club AI Stage 5 now gives each conversation exchange a visibly distinct Question panel and Answer panel, including matching pending/error treatment, so older exchanges remain easy to scan in the drawer or standalone page. Stage 5 remains pending production acceptance and Stage 6 remains unstarted. The visible app version was bumped to `LMS-0704`.
- Ask LWR Pickleball Club AI Stage 5 now selects compound-question evidence by coherent local paragraph/list-item support for each detected intent rather than loose term co-occurrence across an entire chunk. It reserves the best direct team-roster/date and individual-match Match Setup evidence, then excludes less-specific or incidental material before model generation and citations. Manager diagnostics now identify direct intent support, its local rationale, and incidental/scattered-term rejection. Stage 3 ranking, scoring weights, and evidence gate are unchanged. Stage 5 remains pending production acceptance and Stage 6 remains unstarted. The visible app version was bumped to `LMS-0705`.
- Ask LWR Pickleball Club AI Stages 1–5 are formally complete. Stage 5 was production accepted at `LMS-0705`, including the controlled citation viewer, universally shared authenticated AI trigger, role-aware guide browsing, player-safe grounded answers, and compound-intent evidence selection. Stage 6 (conversation, follow-up questions, and feedback) is authorized to begin; Stages 7 and 8 remain unstarted.

- LMS-0706 implements the focused uploaded-USA-Pickleball governing-source enhancement. The additive migration is `lwrpc-admin/supabase-ai-assistant-lms-0706-usap-rulebook.sql`; it has NOT been applied to production in this task. `usap_rulebook` defaults to authority rank 2 and appears in Add Official PDF / Edit Document Details. Stage 4 checks direct local applicability per question issue before choosing LWR controlling rules over corresponding USAP evidence; guides cannot independently override USAP. The LMS-0705 LWR-only coherent-passage selector is preserved. The shared insufficient-evidence fallback now names official LWR and USA Pickleball materials. Stage 6 is PAUSED and unstarted under the latest user instruction, superseding the preceding authorization to begin. LMS-0706 is implemented locally, NOT production accepted: migration application, deployment, actual 2026 Official Rulebook upload/process/content review/activation, and production override/fallback questions remain required. See `docs/lms-0706-implementation-report.md` for migration SQL, validation, boundaries, deployment order, and operator steps.

- Read-only pre-activation audit of the actual 2026 USAP ready version `8e03c077-478b-4319-9c68-db7a13751915` inspected all 403 database chunks and the checksum-matched 96-page source PDF. Recommendation C: reprocessing required before activation. 402 chunks are searchable/embedded; only one TOC chunk is excluded. The audit found 84 distinct cover/folio/copyright-only/TOC noise chunks, 139 additional index-navigation chunks, no valid alphanumeric USAP rule IDs in stored rule metadata, and 48 rule spans across chunk boundaries (17 across PDF pages). A controlled local selector fixture retained an unfinished Rule 20.E.1.e fragment while dropping its supplied continuation. No processor/app code, database record, embedding, version, retrieval guard, or activation state was changed. The document remains inactive, Stage 6 remains paused, and LMS-0706 production acceptance remains pending. Full evidence, counts, proposed generalizable cleanup, and all-chunk inventory: `docs/usap-2026-ready-version-quality-audit.md` and `docs/usap-2026-chunk-audit.csv`.

- LMS-0707 implements the approved Recommendation C processor remediation locally, without changing Supabase data, activation, or Stage 6. A `usap_rulebook`-only chunking path now excludes cover/copyright/folio/TOC/index and Appendix A navigation from retrieval while retaining them for preview, recognizes bounded alphanumeric USAP IDs, and holds a named provision together across page boundaries. The checksum-matched 2026 source dry-run changed 403 old chunks (402 searchable) to 666 local chunks (586 searchable, 80 preview-only), with zero searchable structural-noise chunks, 547 valid provision IDs, no false rule IDs, and no dangling condition fragments under the bounded check. The original inactive version remains unchanged. No new migration is required; after LMS-0707 is deployed, use **Reprocess Selected** on the existing version to create a new inactive immutable version from its existing Storage PDF, review it, and only then consider explicit activation. LMS-0706 remains not production accepted and Stage 6 remains paused. See `docs/lms-0706-implementation-report.md`.

- LMS-0708 corrects the first production LWR-over-USAP authority acceptance failure without changing the active, production-accepted 2026 USA Pickleball Official Rulebook or its processing. The generic interruption/continuation matcher now recognizes singular `match` as well as `matches`, enabling the existing `cannot complete` expansion. Stage 4 reviews a bounded 12-candidate authority window before final selection while retaining the normal eight-item model handoff and four-item GPT-evidence cap. Direct applicability still precedes authority: an unrelated LWR rule cannot suppress applicable USAP fallback evidence. The focused production SQL update is `lwrpc-admin/supabase-ai-assistant-lms-0708-medical-authority-review.sql`; it must be applied after application deployment and before production retest. The manager console now exposes raw Stage 3 rank and authority-review membership beside applicability, governing class, and final evidence selection. LMS-0708 remains pending live-corpus acceptance, and Stage 6 remains paused. See `docs/lms-0708-implementation-report.md`.

- LMS-0709 implements the approved bounded USAP kitchen/NVZ terminology and direct-applicability correction locally. Stage 3 preserves player wording and adds a context-bounded parallel lexical signal for `kitchen`/`NVZ`/`non-volley zone`; it does not rewrite a query or select a rule. Stage 4 canonicalizes only harmless stored PDF line wrapping for matching and selects the operative USAP provision by question scope: general volley (11.A), post-volley momentum/contact (11.A.2), exit-before-volley (11.A.3), or definition (3.A.4.c). The active 666-chunk rulebook, its embeddings, and status remain unchanged. The player subtitle now names official LWR PC information and USAP Rules. Apply `lwrpc-admin/supabase-ai-assistant-lms-0708-medical-authority-review.sql` if needed, then `lwrpc-admin/supabase-ai-assistant-lms-0709-usap-nvz-terminology.sql` after app deployment, and complete live-corpus retests before acceptance. Stage 6 remains paused. See `docs/lms-0709-implementation-report.md`.

- LMS-0710 implements two bounded Stage 4 applicability corrections locally, with no retrieval-RPC, authority, processor, document, embedding, or activation change. Past-tense NVZ exit/re-establishment wording now recognizes the Rule 11.A.3 condition only when incomplete exit and a subsequent volley are both conveyed; a fully exited player does not satisfy that specific fault condition. A separate server-foot/court-contact scope recognizes the stored Rule 7.A.2 outside-court provision and keeps LWR Rule 5.8 available for genuine coaching/court-access questions. The answer prompt now asks the model to lead with the evidence-supported affirmative or negative result. LMS-0710 requires application deployment only and remains pending live acceptance. Stage 6 remains paused. See `docs/lms-0710-implementation-report.md`.

- LMS-0711 implements a bounded JavaScript-only correction for club-selected match equipment and USAP legal-ball questions. For a narrowly detected question about what LWR or its league matches use, Stage 3 preserves the player’s normal retrieval then separately probes active official LWR text for a structured Match Balls assignment without encoding any brand or model. The manager console identifies this evidence as a bounded probe and reports its original normal Stage 3 rank. A direct active Captain Guide match-equipment assignment can control that single issue as `lwr_selected_equipment`; Captain Guides remain supporting guidance for ordinary playing rules. A separate legal/approved-ball scope selects active USAP ball-specification evidence, while damaged-ball questions remain USAP-only. No weights, thresholds, normal candidate/review windows, model cap, authority ranks, SQL/RPC definition, documents, embeddings, metadata, processing, or activation changed. LMS-0711 requires application deployment only and remains pending production acceptance. Stage 6 remains paused. See `docs/lms-0711-implementation-report.md`.

- LMS-0712 starts Stage 6 with deterministic, one-turn conversation resolution and player feedback. Browser session history remains capped at eight visual exchanges; it sends only an opaque, user-bound, expiring receipt for the immediately relevant effective question or pending clarification, never raw history. Raw and effective questions both pass the live/personal-data guard before the unchanged Stage 3/4 pipeline. Missing color subjects receive a clarification rather than an unsupported answer; substantive answers remain grounded only in selected active official evidence. The new append-only `ai_answer_feedback_events` migration stores feedback snapshots without raw chunks, embeddings, signed URLs, credentials, or full conversation history; feedback writes stay server-side behind authenticated receipt validation and preserve changes as events. Stage 7 is not started. No document, version, chunk, embedding, metadata, processing, or activation change is included. See `docs/lms-0712-implementation-report.md`.

- LMS-0712 production acceptance exposed stale clarification receipts and standalone-question interception. The bounded local correction now separates authoritative session context from display history, preserves null through remounts, checks standalone precedence before pending clarification, consumes subject replies, retains actual raw/effective diagnostics, and applies both guards to player/manager answer paths. A color-subject-only Stage 4 framing correction selects the existing USAP ball-color evidence without changing global retrieval or hierarchy. The mobile Ask LWR drawer now portals above dashboard stacking contexts, fills the phone visual viewport with compact safe-area-aware header and its own scroll, and retains the desktop right drawer. Welcome text and four suggestions now demonstrate both LWR and USAP capabilities. Full tests (128), browser fixture checks, non-incremental TypeScript and isolated production build pass; the normal build encounters the known cache EPERM after compilation. Authorized live corpus/model replay confirms Ball → 3.C.3 → independent kitchen and Paddle → null → independent Captains Guide match-ball behavior. No SQL/schema, corpus, embedding, metadata, processing, activation or deployment change. LMS-0712 remains NOT production accepted; native phone keyboard and feedback-event acceptance remain pending, and Stage 7 stays paused. See `docs/lms-0712-implementation-report.md`.


### LMS-0712 bounded personal/live-data guard correction (2026-09-05)

Implemented locally for review: owned rating-value detection, completed-action precedence, personal opponent/schedule protection and general roster-troubleshooting eligibility. Existing contextual-fragment handling now clarifies absent as well as invalid/expired context. Added request-local manager conversation diagnostics without persistent logs. Valid kitchen follow-up/11.A.2 retrieval is unchanged; the original production context-loss cause remains unproven. No SQL, corpus or deployment changes. See `docs/lms-0712-implementation-report.md` for validation and exact production acceptance controls. LMS-0712 remains not production accepted; Stage 7 remains paused.


### LMS-0713 final Stage 6 production hardening (2026-09-05)

LMS-0712 is the prior Stage 6 revision. The user now confirms production PASS for personal rating/status/opponent guards, NR/Season DUPR official answers, kitchen 11.A and live contextual 11.A.2, LWR-selected Franklin Outdoor X-40 Optic, mobile full-screen layout and feedback rendering. This supersedes the earlier pending observations without rewriting their historical entries. Feedback database-event acceptance remains pending.

LMS-0713 is prepared locally to correct roster troubleshooting confidence and a reproduced tangential Player Guide/upcoming-match selection. It shares the existing bounded troubleshooting detector, requires materially applicable evidence before GPT/citations, and adds a troubleshooting-only distinction between documented checks and unknown live causes. No SQL/corpus or deployment changes. Tests: 172 passing; four real answer-model replays use only selected official evidence. See `docs/lms-0713-implementation-report.md` for the exact trace, answers, validation and deployment/acceptance sequence.

Stage 7 remains documentation-only and paused: Helpful/Not Helpful management reporting; automatic capture/review of insufficient-evidence/no-applicable-source questions; recurring unanswered-question identification; rule/guide clarification and update workflow; statuses New / Reviewing / Rule or Guide Update Needed / Resolved / Dismissed where appropriate.

Future permission-aware live LMS intelligence/navigation is also documentation-only: personal DUPR, another player's rating where permitted, Team X roster/standing, next opponent/match, Friday lineup, Take me to Match Setup, Open my roster and Open the standings. Implement later through existing role/permission rules and deterministic authorized server-side data services, never by pretending official-document RAG inspected live state.


### LMS-0714 bounded Stage 6 feedback lifecycle correction (2026-09-05)

The selected feedback button stayed disabled under LMS-0713 even after successful pending cleanup, and `disabled:cursor-wait` made selection appear permanently busy. LMS-0714 separates selected from pending state, keeps selected controls enabled, suppresses repeat votes, adds a synchronous per-answer request lock and `finally` cleanup, retains confirmed state on errors, and resets abandoned pending state on session restore. Server route and feedback database semantics are unchanged. Version LMS-0714/package 0.1.536; 182 tests and component browser verification pass. Required lint/type/PDF checks and isolated production build pass; normal build hits the known cache EPERM after compilation. No deployment or SQL/corpus change. See `docs/lms-0714-implementation-report.md`. Stage 6 awaits final same-answer four-click/two-event production acceptance; Stage 7 remains paused.

Pending post-Stage-6 manager UI cleanup (documentation only): System Setup highlights AI Assistant Management and Test AI Assistant simultaneously when the latter is active; the blue Test AI Assistant header has a blank white upper-right control/box. Neither is fixed in LMS-0714. Previously documented Stage 7 reporting/review and future permission-aware live LMS intelligence remain future work.


### LMS-0715 post-Stage-6 manager UI cleanup (2026-09-05)

Stage 6 / LMS-0714 is production accepted, including read-only verification of exactly two same-answer feedback events Helpful → Not Helpful. This current acceptance supersedes historical pending statements without rewriting the LMS-0714 report.

LMS-0715 (package 0.1.537) fixes the two previously documented manager UI findings: AI Assistant Management uses exact route matching so Test AI Assistant does not double-highlight; the existing AI Source Management header action has explicit dark text on its white background, retaining its label/navigation with mobile touch/focus styling. Both manager tools remain separate and named unchanged. No player Ask LWR, AI pipeline, feedback, SQL/corpus or deployment changes. Tests: 185 pass; desktop/phone fixture browser checks pass. See `docs/lms-0715-implementation-report.md` for files, validation and deployment acceptance steps.

### Stage 7 agreed requirements — documentation only, not started

- Manager Helpful / Not Helpful reporting, with drill-down into question, answer, Official Sources, selected evidence and useful diagnostics.
- Automatically capture genuine `insufficient_evidence` / no-applicable-source questions without requiring a Not Helpful click. Surface recurring unanswered/problem questions so League Management can identify missing or unclear rules/guides.
- Review workflow/statuses: `New`, `Reviewing`, `Rule/Guide Update Needed`, `Resolved`, `Dismissed`.
- Filters for date, Helpful/Not Helpful, source type (LWR/USAP), and topic/question where practical.
- Keep protected live/personal-data requests distinct from genuine missing-source cases. In particular, `What is my DUPR?` is not a missing-rule problem merely because live LMS intelligence is unavailable.
- Preserve later permission-aware live LMS intelligence/navigation: ratings, rosters, standings, opponents, schedules, Match Setup and navigation, using existing role/permission rules and deterministic authorized server-side data services.

These requirements are recorded only. LMS-0715 implements no Stage 7 reporting, automatic capture, review workflow or database schema.

### LMS-0716 Stage 7A local implementation (2026-09-05)

LMS-0716 / 0.1.538 adds the six protected quality tables and atomic capture RPC, shared feedback identity, metadata-only grounded/protected/clarification outcomes, automatic genuine no-source/conflict occurrences, conservative versioned fingerprint groups and automatic case/audit seeds. Approved normalized text correction: retain full normalized value with a 32 KiB byte constraint; effective questions remain limited to 2,400 characters. Existing append-only feedback history, player payload/UI, Stage 3/4 behavior and active corpus remain unchanged.

Validation: 208 tests passed; lint passed with six pre-existing warnings; non-incremental type check and PDF verification passed. Normal compilation succeeded but hit the known tsbuildinfo cache lock; isolated production build and its PDF verification passed. The SQL was executed only in isolated PGlite tests, not production. Deployment order is additive migration → schema/security verification → server-only grouping-key configuration → application deployment → controlled acceptance. See `docs/lms-0716-implementation-report.md` for exact files and production gates. LMS-0717 manager reporting/workflow remains unimplemented.

### Pending post-LMS-0716 AI quality investigations — owner production findings (2026-09-05)

**Gate:** After LMS-0716 / 0.1.538 is production accepted, perform a diagnosis-only pass on the three questions below before proposing any retrieval/selection changes. These are owner-reported production observations, not independently replayed findings or confirmed root causes. They do not expand LMS-0716: Stage 7A remains observational/capture-only. No Stage 3, Stage 4, terminology expansion, clarification behavior, document, corpus or active-version change is authorized by this note. Preserve the accepted LMS-0701/LMS-0705 controls. This follow-up does not authorize LMS-0717 implementation or Live LMS Intelligence.

1. **Weekend roster timing**

   Exact question: `When can I start adding players to my roster for the weekend league`

   Reported behavior: the answer mixes team/season roster management with individual-match Match Setup and cites tangential evidence, including `LWR Pickleball Club DUPR Captains Guide — ENTER/VERIFY SCORES — Page 9`.

   Later diagnosis must determine whether `weekend league` should resolve to LWR Saturday League context; whether the applicable Important Dates/Saturday roster-opening evidence is available; and whether Stage 4 is admitting Match Setup/score-entry evidence into a season-roster-management question. Check whether team roster timing and match lineup timing should both be discussed only when the question actually asks about both. Compare with the earlier LMS-0701/LMS-0705 evidence-selection issue and preserve their accepted regression controls. Do not assume the alias, evidence availability or cause before inspecting the actual retrieval/selection trace.

2. **Underspecified adding-players question**

   Exact question: `When can I start adding players`

   Reported behavior: insufficient evidence.

   Later diagnosis must determine whether that remains appropriate or whether a bounded clarification should ask whether the player means adding players to a team roster or entering players into a match lineup/Match Setup. Do not guess the missing object. No clarification change is part of LMS-0716.

3. **Roster-entry answer and forfeiture provision**

   Exact question: `How do I enter players on my roster`

   Reported behavior: a generally useful Manage Roster answer also states: `Retroactive additions are allowed without penalty only if the player was a valid LWR PC member with an eligible DUPR account and rating; otherwise the match is forfeited and not posted to DUPR.`

   Later diagnosis must verify that the selected official evidence directly supports this statement and that the forfeiture provision is materially applicable to a general how-to question rather than tangential enforcement information. Treat the quoted answer as a reported claim to verify, not an established club rule.

Expected diagnosis deliverable: for each exact question, identify original/effective question and resolved scope, applicable official evidence availability, actual selected/cited evidence and material relevance, then recommend whether a correction is warranted. Return findings for review before implementing any fix; do not alter or activate sources to manufacture a result. Until the production-acceptance gate is met, these items remain pending and LMS-0716 implementation/validation remains as already approved.

### LMS-0716 production preflight hold (2026-09-05)

Controlled production validation/deployment was subsequently authorized, but preflight stopped before any production mutation. Production default table ACLs give service_role broad privileges; the reviewed migration's limited GRANT statements do not remove them. The six new tables are still absent. Required bounded correction: revoke inherited service_role grants on only those six new tables before regranting the approved minimum permissions, and test with Supabase-like default ACLs. Do not change global defaults or existing feedback/LMS privileges. See the production preflight stop in `docs/lms-0716-implementation-report.md`. No migration, HMAC configuration, deployment or synthetic data creation has occurred; production acceptance remains pending.

## LMS-0716 reimbursement diagnosis — review checkpoint

Diagnosis only completed in `docs/lms-0716-reimbursement-diagnosis.md`. No active searchable reimbursement policy was found. The original question exposes a pre-existing generic Stage 4 LWR applicability/status gap; accepted LMS-0715 chooses identical evidence and model prompt and reproduces kind=answer despite missing-policy prose. The Stage 7 observer was neutral in identical-input and local persistence-failure comparisons. Shorter reimbursement controls return insufficient_evidence after existing LWR PC → lwrpc query normalization; explicit waiver controls use waiver evidence within its limits. No fix or production mutation occurred. A separate tested no-source question, `What is the chemical symbol for tungsten?`, yields .0898 and skips the model. Recommend resuming Stage 7A capture acceptance unchanged with that alternative after owner review, while tracking the reimbursement quality correction separately. Stage 7A remains not production accepted; LMS-0717 and deferred roster diagnosis remain unstarted.

### Owner-accepted generic quality follow-up and future UI requirement

The preceding diagnosis hold is historical: the owner accepted it and authorized Stage 7A testing to resume unchanged. The resumed functional capture results and remaining operational verification limits are recorded at the top of this roadmap and in the implementation report.

After Stage 7A production acceptance, obtain separate diagnosis/implementation authorization for the generic applicability issue: **retrieval rank/authority alone must not make tangential evidence directly applicable to the user's issue.** Also investigate generation/status: when selected evidence does not establish the requested fact/policy, do not infer a policy or incorrectly classify the response as a grounded answer. Do not hardcode reimbursement, sunglasses, lost property or waiver-specific answers. This is a post-LMS-0716 generic AI-quality issue, not a demonstrated capture regression. The three roster investigations above remain deferred and their accepted controls must be preserved.

Approved future player-UI requirement (documentation only): **`Ask LWR PC AI may make mistakes. Check Official Sources for important information.`** Use small muted text, centered immediately above the `Ask a question` composer on desktop and mobile, with unobtrusive familiar AI-disclaimer treatment. Do not change answer/source behavior. Do not implement this in LMS-0716. LMS-0717 and Live LMS Intelligence remain unstarted.

## LMS-0717 / 0.1.539 — approved AI quality hardening implementation

Implements the approved diagnosis in `docs/lms-0717-ai-quality-hardening-diagnosis.md`. Direct operative evidence is required before rank/authority can prioritize a passage. Explicit independent issues must each have coverage; unsupported questions skip the model with the existing insufficient-evidence result. Saturday/weekend roster timing uses compatible Important Dates evidence; general roster procedures exclude unrequested enforcement; the missing player-entry object uses the existing sealed clarification lifecycle. The approved single muted disclaimer is immediately above the composer. See the implementation report for fixtures, validation and production acceptance instructions. No SQL, Stage 3, corpus, processing, HMAC, capture or feedback change.

**Stage 7B reservation: LMS-0718 / 0.1.540.** Manager AI Feedback & Review reporting, drill-down, recurring unanswered grouping views, review status/category workflows and filters remain deferred to that separately authorized implementation. Protected/live data remains distinct from genuine source gaps; future permission-aware Live LMS Intelligence remains a later stage. No Stage 7B implementation or production deployment is authorized by this entry.


### LMS-0717 production correction (2026-09-05)

The owner approved correcting the unaccepted LMS-0717 release without a version increment. Actual production-format evidence now exercises separate list parents/bullets, Rule 5.7 conditional children, Season DUPR establishment/truncation and NR qualifications, Rule 5.5 permission/eligibility, missing league after roster clarification, and natural cracked/broken/damaged-ball questions. The cracked-ball failure had applicable USAP 10.G at Stage 3 rank 1 (.5366): broad equipment intent diverted Stage 4, while verb forms cracks/breaks missed damage recognition. The correction stays in bounded JavaScript applicability/conversation routing; no Stage 3 RPC or corpus change is needed. All 257 tests pass, preserving all 227 previous tests. Production acceptance is pending.

Preserve legitimate LMS-0717 defect telemetry as history. Future LMS-0718 review must allow AI/Retrieval Review Needed classification and subsequent Resolved status; do not delete these outcomes/occurrences/groups to hide pre-correction behavior. Existing post-0716 quality findings remain documented and this correction does not authorize Stage 7B implementation.


### Final bounded LMS-0717 correction (2026-09-05)

Owner approved the final diagnosis without a version increment. The Season DUPR failure involved valid context being superseded and a separate scoped date-bullet applicability rejection; both are corrected locally. Community Rule 3.5 was already searchable/embedded and inside the production candidate window but failed literal applicability; its complete permission/exception is now selected. Seven live community/team eligibility lookups now stop at the guard without database lookup. The new fixture uses verbatim previously captured production evidence; 26 added tests preserve all 257 previous controls. Cracked-ball selection and accepted UI layout are unchanged. Current production remains deployed but NOT accepted pending the final report checklist. Keep the historical failed timing/community quality records for future LMS-0718 review; no Stage 7B implementation or Live LMS Intelligence has started.
# Approved future player UI follow-up — New Question

Deferred beyond LMS-0718: add a secondary **New Question** control on the right of the composer, similar footprint to Ask and above Ask on desktop where space permits. It must clear the visible current-session exchanges, signed follow-up/context receipt and pending clarification, start a guaranteed fresh subject and focus the empty composer. It must not log out the user or delete stored Stage 7 telemetry/feedback. Closing/reopening Ask LWR continues to preserve context unless New Question is explicitly pressed. Do not implement during Stage 7B acceptance.

## Future Ask LWR quality/UI enhancement — most-specific supported rule identity

Owner-requested future enhancement only, for the next appropriate Ask LWR quality/UI version. No version assigned or implementation authorized here. LMS-0718 / 0.1.540 remains production accepted; this item does not reopen or extend its historical-viewer correction.

Reported production example: `My community has a team in my division, but they don't have any room for additional players. Can I play for another community's team?` The answer is substantively correct and based on Rule 3.5, but Official Sources displays `LWR Pickleball Club DUPR League Rules — Rule 3 — PLAYER REQUIREMENTS — Page 2`. Preferred future presentation: `LWR Pickleball Club DUPR League Rules — Rule 3.5 — PLAYER REQUIREMENTS — Page 2`.

Diagnosis should trace whether the passage-level selector already identifies a specific subrule while the stored parent chunk has a broader `rule_id`. Prefer deterministic display identity derived from trusted selected passage metadata/text, preserving exact document/version/chunk/page binding and the complete governing qualifications. Apply consistently to LWR and USAP. Examples of supported specificity include 3.5 rather than 3, 5.7 rather than 5, 11.A.2 rather than its parent, and 7.A.2.a where that provision governs; these are examples, not hardcoded mappings. When identity is ambiguous or unsupported, retain the reliable broader identity rather than infer or invent a subrule.

For direct rule questions, the grounded answer may naturally mention the controlling rule when useful, only if that rule identity is supplied in trusted selected evidence. Do not force rule-number wording into every answer or ask the model to infer citation identity. Illustrative desired answer for the reported case: `Yes. Under Rule 3.5, you may play for another community's team if your own community does not have roster availability for additional players in your division.` This is a presentation example, not a hardcoded answer.

Future regression coverage should include production-format parent/subrule passages for both LWR and USAP, nested rule identities, ambiguous/multiple controlling provisions, broader-identity fallback, preservation of source/page binding and qualifications, and prevention of model-generated unsupported rule numbers. Do not change corpus processing merely for display convenience; consider it only if diagnosis establishes that current stored evidence cannot reliably preserve the specific identity. This entry makes no code, selection, generation, corpus, source-viewer or deployment changes.


### LMS-0719 implementation checkpoint — 2026-09-05 local

Owner approved the preceding diagnosis and the bounded two-feature implementation. New Question now clears both session keys, shared receipt/revision and visible history without server activity. A shared operation counter blocks reset during Ask/feedback even after remount; generation checks reject obsolete completions/history writes. Shared history also lets a pending completion reach a reopened assistant. Storage refusal preserves immediate memory reset but cannot guarantee persistence after reload. Desktop uses the secondary action above Ask; phones retain the 639px full-screen panel and use a full-width textarea over an action row.

Citation diagnosis confirmed Rule 3.5 is present in the trusted selected passage despite broad stored Rule 3. The new runtime utility verifies selected text against revalidated source content (including existing structural passage assembly), validates anchored IDs against the stored parent, retains parent families and bounded independent identities, and ignores prose cross-references. Rule identity changes only after selection. Existing string serializers retain the trusted specific/combined identity; historical snapshots are not rewritten. No SQL or corpus processing is required. The PDF viewer uses the same source resolver without answer passages; its exact source authorization remains intact.

346 local tests pass (317 baseline plus 29 new), including captured production-format LWR/USAP passages, reset races/storage, model metadata and Stage 7 snapshots. Real-component isolated browser checks pass at desktop/390/320 widths with local mocked responses. No production request, feedback event, telemetry write, model call or deployment was made for this implementation. Deployment/acceptance requires the report's separate controlled sequence; LMS-0718 remains production accepted.


### LMS-0719 final production acceptance — 2026-09-06 UTC / September 5 local

PRODUCTION ACCEPTED. New Question cleared three visible exchanges and a composer draft, restored focus, survived close/reopen and prevented a subsequent short question from inheriting context. Between 01:13:25.777739 and 01:14:01.007379 UTC, full-row counts/hashes for outcomes (63), occurrences (14), groups (12), cases (12) and feedback (13) were identical across reset and close/reopen. The subsequent short question returned the full-question clarification. The established kitchen conversation also survived close/reopen and its immediate momentum follow-up remained grounded.

Production citations passed: medical parent family 5.7 (page 5); qualified roster rule 5.5 (page 5); Season DUPR 4.1, 4.2 plus independently selected 4.5.1, 4.5.2 (page 3); USAP 11.A.2 (page 30) and 7.A.2.a (page 21). Saturday Important Dates correctly returned Monday September 28, page 1, without a rule number. Previously owner-confirmed Rule 3.5 and roster-availability, Franklin ball, context/reset and PrimeTime clarification passes are retained.

One authorized Helpful vote on the Season DUPR answer preserved exact specific identities in both feedback and review source/evidence snapshots, with valid document/version/chunk/page joins. All 13 historical feedback rows retained their pre-pass hash 389f8eb5b008ed3770e890780d63d87a, also matching the pre-0719 acceptance checkpoint. Desktop 640px drawer/116px action column and mobile 390px/320px full-screen panel/action row passed; reduced-height focus test passed. Physical phone keyboard/safe-area hardware was not available. Raw browser receipts were not extracted; clearing was confirmed through behavior, code/local coverage and server resolver results. No direct live HTTP interception was available; reset telemetry checks and the previously verified no-fetch reset implementation support no-request behavior.

Ten submitted acceptance questions produced ten LMS-0719 outcomes (eight answers, one expected post-reset clarification, one additional medical follow-up insufficient-evidence outcome) and exactly one feedback event. No code, SQL, configuration/HMAC, corpus or deployment changes were made in this acceptance pass. Only documentation is updated; no commit/push/deployment is performed.

### Deferred quality enhancement: bounded additive typo interpretation

Owner accepted the diagnosis that typo robustness predates LMS-0719. Future work should preserve original wording and use only high-confidence, context-bounded additive interpretation, with live-data protection applied to both original and accepted interpretation. Examples: usin, useing, volly, comunity, roser, linep, seson, damged, craked and misspelled league names. Do not introduce broad autocorrect, weaken precision-sensitive guards or rewrite official evidence. No version is started or correction implemented by this record.

### Additional quality observation during acceptance: medical score follow-up

After `Medical issue during match`, closing/reopening retained the answer. `What if one team has 7 points?` correctly resolved to `Regarding Medical issue during match, What if one team has 7 points?`, with priorContextAvailable=true and classification=follow_up, but returned stage4_no_applicable_evidence. This demonstrates an upstream selection gap rather than a reset/context or citation regression: it stops before LMS-0719 source/citation decoration, and the conversation/applicability/governing modules are unchanged from accepted LMS-0718. The separate established kitchen close/reopen/follow-up test passed. Retain the automatically captured occurrence for later diagnosis; no fix or new version is authorized here. This observation is separate from the accepted typo diagnosis.


### LMS-0720 local implementation checkpoint

LMS-0720 / 0.1.542 implements the approved bounded interpretation and validated medical score-follow-up correction. Production remains accepted LMS-0719; LMS-0720 has not been deployed or production accepted. All 386 tests pass, lint has the six existing warnings, explicit type/PDF/diff checks pass, and the isolated clean production build passes after the known normal-build cache-write lock. See `docs/lms-0720-implementation-report.md` for exact files and the controlled acceptance sequence.

Original raw/effective wording remains authoritative for receipts, feedback and Stage 7 grouping. Corpus typo suggestions are diagnostics only; they no longer rewrite/retry the normal SQL query. Accepted interpretations drive deterministic matching and the existing equipment probe. Medical score semantics require valid immediate context and operative score-dependent evidence; rule-label digits are not score evidence. No SQL/RPC, corpus, HMAC, Stage 7 or player-layout changes occurred. Live LMS Intelligence remains deferred.


### LMS-0720 production acceptance stopped — 2026-09-06 UTC

Deployment e0479bd reached READY and the live footer showed LMS-0720. The first request, `What kind of balls will we be usin`, returned insufficient evidence despite Stage 3 sufficiency and a successful equipment probe. Outcome 676bb995-b582-42dc-a6c5-b92473bdaab9 records zero selected evidence and skipped model generation. Testing stopped immediately; remaining production gates are pending, not passed.

Read-only code/local replay identifies a probe handoff gap when evidence is deduplicated against normal ranks 13–32 but omitted from the Stage 4 top-12 window. No correction made. Original/effective/grouping typo wording remained intact; public schema/function/security and corpus/embedding fingerprints plus all existing feedback rows remained unchanged. HMAC key versions remain [1]; no environment action occurred. Production currently runs LMS-0720 but LMS-0719 remains the last accepted baseline. See the implementation report acceptance-stop section.


### LMS-0720 equipment correction deployed; acceptance stopped on volley typo

Commit 8b2e0cd corrects probe deduplication against the actual eligible top-12 downstream handoff. A fresh pre-edit production replay confirmed Captains Guide chunk 68591ceb-77db-464b-b590-412a886dd372 at normal rank 32, score .3564, probe rank 1. All 411 automated tests and the required checks/isolated build passed. Production usin/useing/playng and correct using each returned the grounded Franklin answer with one source. One Helpful snapshot preserves the typo and answer/outcome correlation.

The next production question, `Can I volly in the kitchen?`, failed Stage 3 at .1826 below .350 (outcome e34d7d14-0c38-4c3d-ba82-0f467b558988). Testing stopped before the remaining gates. No additional code/query/threshold/corpus change or rollback. Original failure history, prior feedback, schema/functions/security, corpus/embeddings and HMAC version remain unchanged. LMS-0720 is still not accepted; retain LMS-0719 as the last accepted baseline and await authorization for the distinct volley diagnosis.


### LMS-0720 same-embedding retrieval correction (2026-09-06)

Owner approved one additive lexical retry for accepted interpretations when original Stage 3 fails or deterministic Stage 4 selects zero evidence. Implemented at 0.1.542 with original wording/vector preservation, bounded provenance and unchanged 32/8/12/4 limits, .350 gate, equipment handoff, medical context and Stage 7. Actual retrieval sequence fixtures recover volly, roser, linep, comunity and medcal; already-successful typos incur no retry. 430 tests and explicit type/PDF/lint checks pass; isolated clean build passed; deployment/production acceptance remain in progress. LMS-0720 is not yet production accepted. No next version or Live LMS Intelligence started.


### LMS-0720 final controlled acceptance (2026-09-06)

Production accepted at commit 9263f83 / deployment dpl_3EzLNxGmr12RDGuJJqVZnwfquxUB. Exact volly, comunity, medcal, roster/lineup, cracked-ball, Saturday, protected typo, signed medical 7-point follow-up, New Question reset, Franklin/correct kitchen, actual manager diagnostics, Stage 7B page, feedback original-wording correlation and integrity checks passed. 430 tests plus full validation passed (known normal cache lock; clean build passed). No SQL/corpus/HMAC/player layout change. Final documentation remains local to avoid a redundant deploy.

Separately retained unanswered production observations, not part of the controlled test sequence: 'Is the kitchen the same as the NVZ' and 'What is the website for Lakewood ranch pb'. Diagnosis/baseline comparison is pending separate authorization; do not label as a demonstrated regression or change aliases/corpus automatically.

### LMS-0721 controlled deployment preflight stopped — 2026-09-06

Owner deployment authorization was received, but its gate-1 stop condition triggered before mutation: production has 20 document versions / 1,581 chunks versus the accepted LMS-0720 record's 19 / 1,507 (seven documents in both). The live application remains accepted LMS-0720 commit 9263f83; HMAC key version remains [1]; no proposed Approved Answer object collisions were found. This is an unexplained intervening corpus change, not a demonstrated defect or LMS-0721 regression. Confirm its provenance/authorization before adopting the new corpus baseline and resuming. See the implementation report for the read-only checkpoint and pending gates. No migration, deployment, acceptance data, corpus processing or environment change occurred. LMS-0721 is not deployed or production accepted.

### LMS-0721 preflight baseline reconciled — 2026-09-06

Owner confirmed the intentional DUPR League Rules upload. Read-only checks account for exactly one added Rules version and 74 added chunks; excluding it leaves 19/1507. The active Rules page 2 contains Rule 3.5's roster-availability condition and the predecessor remains superseded. The predecessor's extracted clause also contains that phrase; do not attribute its first introduction to this latest upload. Current authorized baseline: 7 documents, 20 versions, 1,581 chunks. No reprocessing/reactivation. The corpus preflight stop is cleared and controlled LMS-0721 deployment resumes; production acceptance remains pending.

### LMS-0721 production migration verified — 2026-09-06

The authorized migration is applied; do not reapply it. Three restricted managed-knowledge tables remain empty. Effective grants/RLS/functions/indexes/constraints and permitted reads passed. Existing corpus, history, feedback, HMAC and object ACLs are unchanged; only the approved case-category compatibility function changed. Application deployment and genuine owner-policy acceptance remain pending. See the implementation report.

Latest public-contact correction checkpoint (2026-09-06): shared exact email exception implemented locally, not deployed. Full suite 470/471 passes; exact scheduling-policy snapshot test exposes existing Stage 7 name regex treating “Rule's” as a personal possessive name. Stopped before changing that filter per owner instruction. No production action or policy rewrite. TypeScript/PDF/lint pass (six lint warnings); build compiled then known cache lock. See report's shared public-contact regression-stop section. LMS-0721 remains not production accepted.

Public-contact/name correction validated: 473 tests pass; exact and curly scheduling text survive privacy handling. Shared reviewed email and contextual structural-possessive classifier implemented; no schema/history changes. Full local checks and isolated 74-page build pass. Normal pipeline deployment and remaining scheduling lifecycle authorized; production acceptance remains pending.

Future AI quality diagnosis: Saturday mixed-doubles additional-player question, manager-confirmed Rule 6.2.2 (Roster & Courts, page 9), remains AI/Retrieval Review. Yes classification does not repair retrieval; diagnose/correct separately, then Retest and explicitly Resolve. Approved LMS-0721 confirmation copy now explains this; no retrieval/Stage 7 semantic change.

CURRENT production status: 941fd96 READY; exact scheduling Draft and activation/canonical/viewer/Helpful/public-email preservation pass. Active natural variant “Can we move our match to another day?” fails insufficient evidence (Rule 5.11 chunk rank 1 .4154; no selected evidence). Stopped before retrieval correction/revision/retirement. Revision 1 remains Active. LMS-0721 not production accepted. New explicit post-Yes explanation is implemented locally, not deployed after this stop; future Saturday 6.2.2 diagnosis recorded. See report current checkpoint.

Natural-variant diagnosis complete (read-only/local): managed search returns Active revision 1 rank 1; failing move-day cosine .6147 is rejected by .65 while canonical .8176 passes. Other positive scores .5612–.7410 expose recall gap; reschedule wording passes score but unrelated formal chunk prevents supplement. Representation already includes title/question/answer. Two local alternatives do not solve all positives. Recommend separately approved independent scheduling applicability + exact bound formal-source qualification, then conditional recall calibration; do not broadly lower threshold. Full traces/matrices/recommendation: docs/lms-0721-natural-variant-diagnosis.md. No implementation/deployment/Active revision change. Pending UX copy preserved.

### LMS-0722 source-verification stop — 2026-09-06/07 UTC
Owner-activated Rules v20260906234736-a8d90205 is ready/current; prior v20260906111607-c0604ad8 is superseded with PDF/chunks retained. Current Rules 6.3.3/6.3.6 say Picklebreaker 11 by 2 Rally, but original PDF page 13 PrimeTime summary still says 15 by 2 Rally. Verification fails the owner's explicit gate; local implementation is paused and incomplete, nothing deployed. Preserve generic equal-authority conflict tests. See [verification](lms-0722-owner-source-correction-verification.md).

### LMS-0722 source gate passed — 2026-09-07 UTC
**PrimeTime Picklebreaker source conflict RESOLVED BY OWNER SOURCE CORRECTION.** Active Rules v20260907001227-e4d9bf77 consistently specify 15 by 2 Rally in 6.3.3, 6.3.6 and summary. Both prior versions remain superseded/retained and excluded from current retrieval. Resume approved implementation; keep generic conflict regression, no score hardcoding. [Final verification](lms-0722-final-source-verification.md).

LMS-0724 owner entry-point clarification: Members → Member Detail → View As User is the only Phase 1 entry. Real Commissioner/League Manager authorization and selected-target validity are rechecked server-side before confirmation and again at context creation. No global entry or nested target entry. Implemented locally; production review remains pending.
### LMS-0726 / 0.1.548 — local implementation in progress

Owner approved the final design for local implementation only. Eight of fourteen database functions and deterministic foundation controls are implemented; the six shared read projections, application read/write cutover, Phase 2, real shared UI parity and mini-LMS removal are still pending. This is not final implementation acceptance. Current details and remaining work: [local implementation checkpoint](lms-0726-local-implementation-checkpoint.md). Production remains unchanged; no OpenAI calls.


### LMS-0726 production checkpoint / Player target visibility — 2026-09-09

Corrected migration applied once and reviewed application READY. Normal Captain/Player owner checks PASS; Commissioner read/control smoke PASS; all 19 business fingerprints unchanged through Player/Captain View-As checks. Both effective contexts exited, credentials cleared and audits retained. See docs/lms-0726-corrected-production-review.md for completed and remaining coverage; NOT PRODUCTION ACCEPTED yet.

Owner-reported Marilyn Niedzwiecki missing View As User is explained by active member with no user_roles assignment, whereas Members displays Player as its fallback. Accepted target validation returns null and the button is hidden. Mark Abbott with an explicit Player assignment shows the button and opens shared Player UI correctly. No permission/account changes or target-guard relaxation. Record any future display-label clarification separately; do not auto-provision access. Deferred security hardening remains HIGH PRIORITY / OPEN.

### LMS-0726 owner correction — active-only Division Team Schedules

Captain/Player popup queries now locally match the existing Commissioner is_active=true filter. Same shared loader applies in View-As; no permissions/SQL/business data changes. Nine focused tests pass; lint zero errors. See lms-0726-active-schedule-teams.md and build logs. Local correction is not deployed, and prior production source hashes must be refreshed before any corrected deployment. Current production acceptance remains open.

### Owner acceptance clarification — no-role members

Marilyn's missing durable role is an accepted separate existing semantics issue, not an LMS-0726 failure. Continue acceptance using legitimate assigned-role targets, including Mark Abbott. Preserve durable-role authorization; no account changes, role backfill or presentation-default grants. Mandatory post-release review: MEMBER ROLE SEMANTICS — DEFAULT “PLAYER” DISPLAY VS ACTUAL ROLE ASSIGNMENT, detailed in post-lms-0726-member-role-semantics.md. Neither automatic Player provisioning nor UI-only resolution is selected now.

2026-09-10: Season Ratings CSV owner matching decision: **DUPR ID ONLY**, no masked-email/name matching or member identity writes. [Read-only diagnosis and remaining implementation gates](season-ratings-csv-import-diagnosis.md). Missing action traced to zero-ready early return and removed persistent Apply button; doublesRe alias absent. Preserve recorded blank-only raw Doubles policy. Age-source availability, inclusive cleanup versus strict RF <29 boundary, mixed-field upsert preservation, and atomic identity revalidation require resolution before implementation. No production mutations or OpenAI API calls; application unchanged.

2026-09-10: [Season Ratings final design gate](season-ratings-final-design-gate.md), diagnosis/design only. Actual export has 879 rows and full doublesReliability header (corrects prior assumed doublesRe-header cause); Metrics selects 295 over_65, 394 over_50, 190 missing. Read-only identity counts: 695 unique, 178 not found, 6 ambiguous; 36 unique matches inactive. Existing RF/raw NR feed eligibility, and age imports share PrimeTime Season DUPR storage; no explicit rating-lock metadata found in inspected tables. Recommend additive source-only refresh; retain existing season values/blank-only protection. Cleanup inclusive threshold discrepancy inventoried, not changed. Implementation, reset and production import remain unapproved. STOP FOR REVIEW.

OWNER HOLD — Season Ratings CSV RF=29: [OWNER POLICY DECISION REQUIRED](season-ratings-rf-owner-hold.md). Supersedes prior instruction/approval to change Clean Ratings <=29 to <29. Preserve Clean Ratings, Ask LWR, eligibility and Rule interpretation independently; no automatic cleanup. Source refresh must not reclassify existing season inputs/outcomes. Added four passing local baseline regression tests; future importer requires isolated integration proof. No application-source change, production mutation, import, deployment or OpenAI calls. Workspace still records design-only implementation gate.


## 2026-09-10 — Season Ratings source importer, local review candidate

Owner RF hold resolved by active Rules version `6ae10e5f-fdde-41be-a941-d1b7ed360d1a`, Rule 4.1.1: RF of 29 or below is NR. Subsequent owner clarification: no hardcoded cutoff; Clean Ratings continues to prompt, while deterministic eligibility reads the threshold from verified Rules. Import preview shows raw source RF and never invokes Clean Ratings. Eleven existing Clean/Delete/Copy functions are unchanged from HEAD.

Local implementation adds strict DUPR-ID-only CSV preview, explicit signed confirmation, and separately stored source ratings with transactional revalidation/audit/idempotency. No business-table writes or season reset. Local additive migration only; production SQL/import/deployment remain prohibited pending separate review. See [21-part review report](season-ratings-implementation-review.md), which supersedes earlier hold/implementation-status notes. Existing unrelated workspace changes are retained; no bulk staging or commit.


## LMS-0733 / 0.1.555 — local immutable candidate review

Season Ratings importer and bounded RF synchronization isolated against the 318-file hash-verified LMS-0732 accepted source. See [release identity](lms-0733-release-identity.md). No production SQL/deployment/import; controlled review paused for exact commit review. Original mixed workspace is not the deployable candidate.

## LMS-0733 controlled production review — STOP, 2026-09-10

Supersedes the prior local-only status: owner-authorized exact migration applied once (production history 20260910144835); immutable commit e24d27f64953a6128383bb6393bcb383ba67f5dc deployed READY as dpl_3bUs78Vz63nxpVwguqPcHn35WziL. Normal representative LMS, View-As and exact RF29/30 checks passed within documented empty-data coverage limits. The original 879-row CSV PREVIEW failed with “CSV row 2 has the wrong number of fields.” Read-only local reproduction confirms an empty trailing header makes 11 header columns versus 10 in every data row; the accepted parser rejects this layout. No live preview counts/confirmation produced. No import, CSV edit, retry, production correction or new release. All 19 operational fingerprints and existing security remain unchanged; sources/batches both zero. LMS-0733 is deployed but NOT PRODUCTION ACCEPTED; LMS-0732 remains the accepted rollback baseline. STOP FOR OWNER REVIEW. See [production checkpoint](lms-0733-production-preview-checkpoint.md).

## LMS-0733 empty trailing header correction — LOCAL REVIEW READY

Owner-authorized parser-only correction is committed on the isolated `codex/lms0733-trailing-header-correction` branch, remaining LMS-0733 / 0.1.555. See [correction review](lms-0733-correction-review.md) and [immutable identity](lms-0733-correction-identity.json). Removes only whitespace-empty trailing header cells; rejects interior unnamed headers and strict width mismatches without trimming data rows. Original hash-verified 879-row file reaches the local preview service with all 8,790 fields independently verified. Accepted synthetic membership fixture yields 659 ready / 36 inactive / 178 not found / 6 ambiguous; these are not fresh production counts. All 1,165 tests and 69 focused controls pass; lint has six unchanged warnings; TypeScript, completed isolated build, rebuilt PDF bundle and diff checks pass. Migration unchanged. No production SQL, deployment, CSV retry, import or model calls. STOP for correction review before controlled production continuation; no LMS-0734 started.

## LMS-0733 corrected production preview — OWNER IMPORT GATE, 2026-09-10

Supersedes the preceding local-only status: approved correction a9a515da7287f50408fcd98b6779f3d7ad6806bb is deployed READY as dpl_2HH3eaFjrPesqRASyy6SacX4TQUT, same LMS-0733 / 0.1.555. Application-only; migration not reapplied. Normal lightweight smoke passed. Actual original CSV preview succeeds: 879 total, 696 unique / 660 active-ready, 36 inactive, 177 not found, 6 ambiguous, zero missing-ID/invalid/no-change. Ready-row ages 238 over65 / 315 over50 / 107 missing; whole-file ages remain 295/394/190. Final confirmation inspected and CANCELLED. Sources/batches remain zero. All 19 fingerprints checked: 18 unchanged; members changed during owner-confirmed concurrent member/DUPR-ID editing. One NOT FOUND sample gained a live match after the preview; refresh before any later authorized import. Existing security unchanged; zero new model calls; rollback0732 READY. Production acceptance remains in progress. STOP BEFORE IMPORT. See [actual production checkpoint](lms-0733-correction-production-preview.md).

## LMS-0733 first live source import — COMPLETE, 2026-09-10

Owner authorized one batch subject to fresh-preview gates. During preparation, ZN5DGM and 67POVE became ambiguous; stale previews were discarded without a transaction. Owner paused member edits; final fresh mappings were independently verified stable. The exact original 879-row CSV then committed ONCE as batch 2c0fcb21-e3b3-4dc0-ab52-9b49114995af at 15:47:26.791625 UTC: 659 source records, revision1; 36 inactive / 176 not found / 8 ambiguous skipped; zero missing-ID/invalid/no-change. Stored ages:236 over65,315 over50,108 absent (no fabricated values). All659 source/payload/unique-active identity checks pass; all19 operational fingerprints, including1012 legacy Season Ratings rows, match immediate pre-commit state. Imported data is in the separate source store; legacy season-grid blanks remain unchanged. Post-import normal smoke and representative View-As pass. No Clean/Copy/Delete, migration, second batch or model calls. No rollback needed; source before-images retained. Current LMS0733 first live import is completed and verified. STOP; no further release/workflow. See [final report](lms-0733-first-live-import-report.md).

## LMS-0733 source-review UX — STOP at new-data-access gate

Owner requested local post-import history/source comparison UX, with an explicit stop if SQL or new data access is required. Inspection confirms existing snapshot returns source/protected values but no durable last-successful-import timestamp/count, and no selected-season history read exists. Private batch metadata cannot be read directly through existing application grants. No code/SQL/production changes; no tests or mobile checks run. Proposed narrow read-function/authorized review-contract extension requires owner review before implementation. See [UX data-access gate](lms-0733-source-review-ux-gate.md). Clean Ratings remains untouched and unrun.


## 2026-09-10 — LMS-0733 source review and initialization diagnosis

Local read-only source review implemented in the isolated codex/lms0733-source-review candidate; deployment held. See docs/lms-0733-source-review-local-review.md for metadata ACL/SQL footprint, raw source/protected comparison, local acceptance, and production sequence. No production mutation, Clean Ratings, or initialization. See docs/lms-0733-season-initialization-diagnosis.md: Clean Ratings reads legacy inputs, not the new source store; the old importer filled legacy Doubles/RF and truncated age-derived PrimeTime but not regular Season DUPR. A separately reviewed blank-only initializer is missing. Current active Rules version 6ae10e5f-fdde-41be-a941-d1b7ed360d1a governs RF; PrimeTime 50+ fallback versus pre-season under-65 NR, NR division mapping, and eligibility RF/raw-NR inputs need resolution before future implementation. Source review remains policy-neutral. Existing 320px season badge produces 11px overflow independently of the new review; deferred outside scope.

## 2026-09-10 — LMS-0733 source-review production checkpoint

Exact approved c6ee7f0 deployed READY as dpl_2tk9Ei2VnKsPvVd22JdhuRpZ7Xns; exact hash-verified metadata migration applied ONCE, recorded 20260910175201. All 324 deployed source files match approved Git-derived bytes. Normal lightweight smoke and View-As pass; actual Fall 659-row history and five raw-source samples match storage, protected blanks preserved. All 19 business fingerprints, 659 sources, audit batch, and existing security remain unchanged apart from the intended private read-function definition. SOURCE REVIEW UX ACCEPTANCE PENDING: approved commit omits absent-preview status text required by latest acceptance, and live mobile viewport overrides did not change actual dimensions, so fresh 390/320 production verification is incomplete. Prior local responsive results remain valid but are not substituted for live checks. See docs/lms-0733-source-review-production-checkpoint.md. No new CSV/import, Clean Ratings, initialization, business writes or follow-up release. Initialization accepted DIAGNOSIS ONLY. STOP FOR REVIEW.

## 2026-09-10 — LMS-0733 Source Review UX PRODUCTION ACCEPTED (desktop)

Supersedes the preceding pending source-review checkpoint. Owner clarified Season Ratings administrative/import/source-review/confirmation workflows are desktop-only: 390px/320px production verification is NOT APPLICABLE, not an acceptance limitation. Existing responsive behavior is preserved; global LMS mobile requirements for Player, Captain, View-As, Ask LWR and other user-facing screens remain unchanged. Small one-runtime-file empty-state correction de36572acdbcd5b5e6f669ed451dc6db93c7778a deployed READY as dpl_3BXEyQk6kbxFA793Jg649oAZq77X, same LMS-0733/0.1.555. Desktop verified separate CURRENT CSV PREVIEW absent state plus actual 659-row successful Fall import history, raw source values and missing-age em dash. Normal LMS/View-As smoke, lint/build and four existing source-review tests pass. All 324 deployed source files match the exact candidate; all 19 business fingerprints, 659 source rows, audit batch and security catalog unchanged. No SQL applied/reapplied, new CSV/import, Clean Ratings or initialization. SOURCE REVIEW UX PRODUCTION ACCEPTED; initialization remains DIAGNOSIS ONLY. STOP. See docs/lms-0733-preview-empty-production-acceptance.md.

## 2026-09-10 — Fall initialization OWNER POLICY REVIEW ONLY

Fresh active Rules remain 6ae10e5f-fdde-41be-a941-d1b7ed360d1a: RF <=29 NR; regular rated tenth truncation; NR applicable division max minus 0.5/highest across rostered divisions; 65+ and explicit 50+ fallback, with under-65-at-start NR precedence requiring owner resolution. Fresh 659-source counts: 538 high-RF numeric candidates, 121 low-RF NR (41 literal NR); 236 65+/315 50+/108 missing-age. All active and both protected fields blank; 1,159 active members not in source excluded. No team_members rows; reviewed schema has no dedicated DOB/age-verification fields. Final PrimeTime eligibility/proposal counts not inferable. Eight decision groups cover age-NR/fallback precedence, no-age fallback, cross-rating NR mapping, absent roster context, verified-age/scope, partial inputs, frozen season RF/NR eligibility provenance, and effective date/phasing. See docs/lms-0733-initialization-policy-review.md and its evidence JSON. No code/SQL implementation, deployment, Clean Ratings, initialization, production mutation, new import or OpenAI API calls. STOP FOR OWNER POLICY REVIEW.

## 2026-09-10 — LMS-0733 Phase 1 initialization local review

Owner initialization decisions are resolved in docs/lms-0733-phase1-local-review.md. Phase 1 regular rated NULL-only initializer implemented/tested locally in the isolated candidate; NO production SQL, deployment, initialization, Clean Ratings or source import. Fresh production-derived preview: 1,974 members; 538 rated candidates, zero protected, 121 NR deferred, 1,159 active missing source, 156 inactive; zero other review categories. Required communicated establishment date/source suitability and specific batch authorization remain execution gates. Rules RF threshold is extracted, not hardcoded. Private frozen provenance, atomic rollback, stale-basis checks and default-disabled writes implemented. Full regression 1,176 passed; focused six passed; lint zero errors/six existing warnings; build passed using synthetic settings. Desktop synthetic confirmation and repeat protection passed. Additive SQL remains review-only. NR and PrimeTime remain future phases; verified age proof and actual division bases are required. Do not deploy the mixed main checkout. See local review for exact files, migration SHA-256, all rows and controlled sequence.

2026-09-10 LMS-0733 Phase 1 production checkpoint: exact reviewed infrastructure applied once (database version 20260910193949), zero initialization/audit/provenance writes. Candidate 2916f887b1018205523e98c806e3867abed2db17 frozen; deployment rejected by automatic approval review pending direct-chat authorization. Accepted deployment remains live. See docs/lms-0733-phase1-production-checkpoint.md. Do not reapply migration; do not initialize ratings.

## 2026-09-10 — CURRENT OWNER WORKFLOW: Upload inputs; explicit Clean recalculates

Supersedes the separate Phase 1 initialization design and blank-only Clean assumptions. Upload matches only unique active DUPR IDs and fills blank working inputs; explicit Delete → Upload is refresh, subject to safe Delete scope review. Clean Ratings is the authorized explicit establishment/recalculation operation and may CREATE or UPDATE existing Season DUPR at any time using current applicable Rules and roster/division context. No automatic Clean. Removal from a higher NR division and subsequent downward recalculation remains OWNER DECISION REQUIRED. Source/input values are protected from routine Upload; final Season DUPR is not permanently protected against explicit Clean.

Separate initializer remains disabled/dormant; no production initialization happened. Candidate 2916f887b1018205523e98c806e3867abed2db17 deployed READY as dpl_8G1bMtnDwA8nqezetwpfuvi2Qecp with write flag false. Its preview timed out; no retry/commit. Do not optimize or use that retired workflow. Additive infrastructure applied once remains empty; no automatic object cleanup. Future reviewed app correction should remove the obsolete entry point.

See docs/lms-0733-clean-ratings-reconciliation.md and full read-only preview HTML/JSON. Actual Fall working fields empty: current Clean zero changes. Conditional safe input mapping yields 538 regular CREATE, 121 NR deferred for actual division, 1,159 missing inputs; PrimeTime unsupported pending proper age/context handling. 551 age metrics, 108 missing; 62 age metrics belong to NR players. Current age field is final season_primetime_rating, not separate input. Current Delete removes whole selected-season rating rows and leaves source store intact. Current source Upload refreshes rather than fills blanks. These need reviewed local reconciliation, not production execution. No Clean/Delete/Upload/rating writes, model calls or SQL/app changes in this diagnostic review. Next gate: owner review of reconciled local scope and remaining policy decisions before any production mutation.

## 2026-09-10 — FINAL OWNER DECISIONS: current-state NR and integrated input workflow

NR decrease is RESOLVED: explicit Clean uses only CURRENT applicable rostered divisions and may decrease after removal from the higher division; no historical high-water mark. No current division means DEFER. This supersedes earlier OWNER DECISION REQUIRED entries on downward recalculation.

See docs/lms-0733-final-workflow-design.md (22 requested design items) and docs/lms-0733-final-workflow-counts.json. Fresh 20:02:34 UTC read-only transfer counts: 659 source members active/unique; 659 Doubles + 659 RF fills, 551 potential working-age fills after independent age field approval, 108 missing age, zero existing Doubles/RF protected and zero identity/inactivity/fractional-RF review. 1,869 field fills across 659 players, not executed. Conditional regular Clean: 538 CREATE, 121 no-division NR DEFER; no PrimeTime writes. Proposed future Upload is one transaction for source audit plus blank working inputs; explicit Clear touches only inputs/selection references, never final ratings/notes/row history. Existing destructive Delete is not repurposed; disposition A retained separately pending review. Separate initializer remains abandoned/dormant, no enablement or cleanup. Proposed age/lineage and narrow transactions require local implementation and later exact SQL review. No production writes or application changes in this design turn. STOP for owner review, then explicit local implementation authorization; deployment/input transfer/Clean remain separate later gates.


### LMS-0733 final working-ratings implementation — LOCAL ONLY (2026-09-10)

Owner settled the NR lifecycle: admission never writes Season DUPR; the next explicit Clean calculates from current regular roster/divisions. If all rosters are removed, retain an established rating as NO CHANGE; a blank NR rating remains DEFER. Candidate evaluation uses target division NR rules without a provisional numeric write. The local candidate adds separate Working Age-Based storage, transactional integrated Upload/blank transfer/Clear/regular Clean, and bounded read-only Rules metadata for existing roster users. PrimeTime and the abandoned initializer remain disabled/separate. See docs/lms-0733-final-workflow-local-review.md and the updated governing design. No production deployment, SQL, import, transfer, Clean, Clear, Delete, admission or rating mutation was performed.


### LMS-0733 final workflow production review — release guard STOP (2026-09-10)

Read-only preflight confirmed 659 source rows, unchanged original successful batch, blank Fall working/final ratings and unapplied final migration. Migration hash matches. Staging the 23 accepted files exposed a blank-line-at-EOF failure in the previously untracked local browser-fixture script. Per owner stop-on-guard instruction, no automatic correction, commit, migration or deployment occurred. See docs/lms-0733-final-workflow-production-review.md.

LMS-0733 EOF-only correction committed as 12df254965c552c8d21f2b96b81d6843ace7bc84; exact 23-file/whitespace/hash guards pass. Read-only production check still finds final workflow migration absent. Production unchanged pending reconciliation of the SQL restriction; see docs/lms-0733-final-workflow-production-review.md.

LMS-0733 exact final-workflow migration applied ONCE as 20260910211313; security and 19-table integrity checks pass. DO NOT REAPPLY. Deployment of 12df254 halted at raw export blob identity guard (.gitattributes mismatch); prior production app remains live, zero rating writes. See final-workflow production review.

LMS-0733 final workflow reached owner preview checkpoint: exact 12df254 deployed READY dpl_9gnFJX1C8rK8VHKrnz5dmE6LWKJ7 after byte-exact export correction (core.autocrlf archive behavior; no source edit). 659/659/551 fills,108 missing age; read-only post-transfer projection538 CREATE/121 NR DEFER. Business integrity unchanged; writes disabled; no migration reapply. See docs/lms-0733-final-workflow-owner-preview-checkpoint.md. Await owner before any rating writes.

LMS-0733 first source-to-working transfer authorized including551 Working Age-Based values; NOT executed. Automatic approval review rejected enabling the shared workflow write gate because it also permits Clean/Clear. Needs explicit temporary shared-gate authorization or separately reviewed narrower implementation. Production remains writes-disabled; no data changes.


### LMS-0733 owner workflow simplification — 2026-09-10

Owner cancelled the pending Transfer before final confirmation. Restored production to writes-disabled deployment `dpl_9gnFJX1C8rK8VHKrnz5dmE6LWKJ7`; browser gate confirmed disabled, workflow audit zero runs, business/source/final integrity unchanged. No transfer, Clean, Clear, Delete, Upload or initializer ran; migration 20260910211313 was not reapplied.

Local candidate now groups Upload Ratings CSV, Clean Ratings and existing Delete as Data Tools buttons; Transfer/Clear are hidden and Copy remains separate. Existing backend Upload already atomically records source history and fills NULL working fields; no SQL/API/rule change. Delete removes whole selected-season rating rows but leaves source/history/private provenance. Copy omits Working Age-Based and is not an import substitute. Owner must review reset semantics and separately authorize the one-time 659/659/551/108 reconciliation. See `lms-0733-simple-workflow-local-review.md`. No corrected application deployed; stop for review.

Simplified workflow validation complete: 1,184 tests passed; lint zero errors/six existing warnings; build/TypeScript, PDF guard and diff check passed. Desktop synthetic preview recorded zero commits and unchanged source/ratings. SQL hash and Delete/Copy handlers unchanged.


### Member Administration fast-search race — local only
Confirmed stale directory responses can overwrite newer search results. Isolated local fix in `.local-validation/member-search-race` ignores obsolete responses/errors and invalidates pending results on search input/unmount. Five targeted tests pass; no matching/SQL/production change. Season Ratings is synchronous and did not reproduce with Smith; awaiting owner example rather than applying speculative changes. See `member-search-race-local-review.md`.

Member search race validation complete: five targeted tests, lint (zero errors/six existing warnings), build/TypeScript and diff check passed. Not deployed.


### LMS-0733 simplified workflow deployed; one reconciliation complete — 2026-09-10

Owner-authorized commit `50ae3343e1a5bc9a763ca71db7e75b945b12516a` deployed READY (`dpl_7qjLYYTsZn5t98PNHoQLRqUvKzSX`). Normal Commissioner/Captain/Ratings/Ask LWR opening/View-As smoke passed. Transfer/Clear hidden. Application write gate remained disabled throughout. One internal reviewed transaction successfully populated 659 Doubles, 659 RF, 551 Age-Based; 108 age inputs remain blank. Final regular/PrimeTime writes zero; source and all operational fingerprints unchanged; all 1,012 original rating rows unchanged. New 659 working-input rows and their audit/provenance are verified.

Actual read-only Clean preview: 538 CREATE, 0 UPDATE, 0 NO CHANGE, 121 NR DEFER, 0 REVIEW, 1,162 other active missing-input deferrals, 159 inactive skips. STOP BEFORE CLEAN; owner authorization remains required. No migration reapplied; no Clean/Clear/Delete/Copy/Upload/initializer. See `lms-0733-simple-workflow-production-checkpoint.md` and evidence JSON.

Member-search clarification: owner confirms the reported timing issue may have been limited to Member Administration and considers that local correction complete. It remains separate and was not included in the LMS-0733 deployment. No speculative Season Ratings search change is needed.


### LMS-0733 Upload UX production checkpoint

Deployed reviewed Upload-only commit `515c5d5525aa7d927a30c1011c4cf309764765d4`; READY `dpl_46G3yMrKFp8vx5JsfKJkxvTwxThp`. Normal LMS and permitted read-only UI checks passed. No SQL or production ratings actions. Preview-summary/Import confirmation interaction remains local-fixture evidence because production CSV submission and write enablement were prohibited. See [production checkpoint](lms-0733-upload-ux-production-checkpoint.md). Combined regular + PrimeTime Clean remains the next separate gate.


### LMS-0733 combined Clean selected RF cutoff — local only

Owner-selected 0–100 whole-number cutoff now defaults from active Rules and drives both regular/PrimeTime calculations, receipt/fingerprint and audit. Changing cutoff invalidates/recalculates preview; stale/legacy confirmations fail closed. See [local reconciliation](lms-0733-combined-clean-cutoff-local-review.md). New migration remains local and requires separate review; no production Clean or rating writes.

### LMS-0733 combined Clean — controlled production preview checkpoint

Supersedes the local-only status above: owner-authorized migration `20260911010000_combined_clean_selected_cutoff.sql` applied once as production version `20260911011803`; recorded SQL SHA-256 exactly matches reviewed `622d319d7871cbd928f997da4b859634fe989b7e19a3fdfffb7670be1b2d72ed`. Exact application commit `6d86e113ba1d40cc623f435bfd68753ea7535d5b` deployed READY as `dpl_E89PiGwFsrfqGPKdofS5CutKFMws`, with both ratings write gates false. Fresh Fall cutoff29 preview: regular538 CREATE, PrimeTime489 CREATE, zero UPDATE/NO CHANGE/REVIEW, 121 NR deferrals each, 49 Rated missing-age PrimeTime deferrals, 1162 missing-input members. Production 29→30→29 recalculation and old-preview removal passed. All19 business fingerprints unchanged; working659/659/551; final ratings and Clean runs remain0. No Clean or other rating operation executed. See [owner checkpoint and confirmation summary](lms-0733-combined-clean-production-checkpoint.md). Stop pending owner review before any rating writes.

### LMS-0733 first combined Clean — completed under explicit owner authorization

September10 9:32:26PM EDT: exactly one successful combined Clean at cutoff29, audit `08d3c0c5-d5fe-4e8e-8824-4a1577ffd340`. Created538 regular and489 PrimeTime finals (538 distinct players), zero updates;121 NR deferred,49 Rated missing-age PrimeTime blanks. All proposals matched final values; all18 operational fingerprints and every nonfinal rating field/input/source hash unchanged. Temporary workflow gate restored disabled immediately after observed success; enabled deployment safely removed; production remains exact commit6d86e113 on disabled deployment E89PiGwFsrfqGPKdofS5CutKFMws. Migration not reapplied. No second Clean. See [final result](lms-0733-first-combined-clean-production-result.md).

Separate UX finding: Player/View-As dashboard without a selected season/team can show the existing “NR” fallback despite a valid stored Fall rating (Alan Fox3.6 /4.2 verified). View-As renders read-only and exits normally. Do not rerun Clean or change classification to address this display issue; separate review needed.

### LMS-0733 Delete Season Ratings — notes preservation correction (local)

Owner requires DUPR Notes to survive Delete Season Ratings. Local handler now clears only the five rating fields instead of deleting whole rows; notes and other seasons remain intact in storage and displayed state. Confirmation explicitly promises notes preservation. Actual-handler regressions cover multiline/blank/null notes, cancellation and failed updates. No SQL migration or production action. See [local review](lms-0733-delete-preserve-notes-local-review.md).

## September 10, 2026 — permanent FAST FIX lane and Season DUPR date retrieval
Owner authorized docs/lms-fast-fix-workflow.md: localized application-only fixes may proceed through focused tests, lint/build, exact deployment identity, and targeted production replay without intermediate review. SQL, policy, security, corpus, business-data mutation, and broad shared-router changes stop for review. Current Season DUPR date failures were unresolved intent variants; active Important Dates has September 27, 2026 for all three leagues. See docs/ask-lwr-season-dupr-date-fast-fix.md. Unrelated Delete/DUPR Notes local correction remains separate.
FAST FIX — PRODUCTION ACCEPTED: commit 9109c716f232f10e9ca1fe612bc5203717accf78 / deployment dpl_4j1HcDHg8aj8GeZKkPoAGFogGLKp. Four targeted production replays pass; 151 focused tests, lint and build pass. No SQL/corpus/ratings/operational mutation. See docs/ask-lwr-season-dupr-date-fast-fix-production-acceptance.md.

### Season Ratings execute-action diagnosis — 2026-09-11

FAST FIX stopped at authorization-policy boundary. Recorded deployed source 6d86e113 suppresses signed receipts and disables confirmation whenever the shared workflow gate is false; controls are below the preview table. Reselecting Upload explicitly clears file/preview. No existing per-action permission path; proposed upload/clean-only receipt policy needs review under the owner's stop condition. No runtime/SQL/deployment/gate or business-data changes. Fresh browser session expired; production replay not completed. See [diagnosis](season-ratings-execute-action-diagnosis.md).

### Season Ratings normal action correction — 2026-09-11

Approved local implementation complete in .local-validation/season-ratings-normal-actions, based on accepted9109c716. Upload/Clean use bounded receipts with maintenance flag off; Transfer/Clear stay gated. Focused30 pass, local browser cancel flows commit0, build/lint/types/PDF pass. Deployment held for ten failures reproduced on unchanged accepted baseline. No SQL/deployment/production business-data writes. See [local review](season-ratings-normal-actions-local-review.md).

Local candidate committed as 1cf9fb25fc94288c92b035678d6fe8b4fabae000 (codex/season-ratings-normal-actions). Final full suite1217:1207 pass/10 fail; failure names exactly match unchanged baseline. Focused30 pass. All other required checks pass. Deployment/production acceptance remains held; no production business-data writes or gate changes.


### September 11, 2026 — Season Ratings baseline-differential release

Owner-authorized exception for exact `1cf9fb25fc94288c92b035678d6fe8b4fabae000`: baseline `9109c716f232f10e9ca1fe612bc5203717accf78` has 1,197 pass/10 fail; candidate 1,207 pass/the same 10 fail; zero candidate-only failures. All 30 focused tests and required static/build checks pass. Deployed/promoted `dpl_6tkBP82qYfhA6amAQB8zqq5ZHFfU` with both maintenance flags false. Upload production confirmation/cancel passes, file and preview retained. Clean cutoff 29 shows zero changes and 1,821 missing working RF inputs, as before deployment; Apply is above table but disabled. **DEPLOYED — PRODUCTION ACCEPTANCE PENDING**, not production accepted. All 19 business-table hashes and source snapshots unchanged. Prior accepted deployment retained for recovery. Ten baseline failures remain separate **MUST-FIX OPEN** debt (seven stale date expectations; three line-ending-sensitive fixtures/identity guards). See `docs/season-ratings-baseline-differential-release.md` and `docs/season-ratings-baseline-test-debt.md`. Permanent release policy unchanged.


### September 11, 2026 — Owner clarification and acceptance handoff

Owner confirms intentional Delete Season Ratings after prior Clean cleared working inputs and final Season/PrimeTime ratings; retained source/audit history is expected. No Working RF regression. Missing-RF investigation canceled and concern closed. The zero-change Clean preview is correct. Exact deployed commit `1cf9fb25fc94288c92b035678d6fe8b4fabae000` proceeds to **OWNER WORKFLOW ACCEPTANCE IN PROGRESS**. Owner will personally execute Upload/Preview/Import, then Clean/cutoff/Preview/Apply. Codex must not perform these production rating actions. Final execution acceptance awaits owner results; existing baseline test debt stays separate. Prior zero-write evidence applies only to the recorded Codex acceptance window.


### September 11, 2026 — DUPR header/path correction production preview accepted

Deployed `d8b55475a5377b6c6c05ead4e72a63236f681dab` / `dpl_7TPb3ZXyPMpPrgMy35CEpDH4cvSd`, app-only current RF alias precedence and invalid-age fallback plus explicit raw-to-working source labels. 96 affected tests and lint/build/types/PDF/diff pass. All 886 actual CSV parsed results match old parser; 3.604/30 are Singles values, not the deployed Doubles/RF mapping. All 667 current Fall working rows match the authoritative CSV. Production preview shows Terry 4.0/100/4.3; ID 1R9LNE matches two active members and both stay excluded as ambiguous. Four additional distinct rows verified. All rating/source hashes and 18 of 19 business-table hashes unchanged; sole Locations difference is the owner-confirmed concurrent Esplanade at Artisan Lakes edit at 15:46:59 UTC. No Import/Clean/Delete/Copy or SQL by this task. Both maintenance flags false. **HEADER/PATH CORRECTION — PRODUCTION PREVIEW ACCEPTED**. No re-import needed to correct existing inputs. Any Terry duplicate-ID resolution is separate identity work. See docs/season-ratings-header-mapping-review.md. Existing ten baseline test failures remain separate open debt.

### September 11, 2026 — read-only DUPR duplicate inventory

Read-only review identifies Terry Adelman `43e1e363-82f1-47d7-a869-befed4c967b8` as the MembershipWorks-backed Commissioner identity. Manual `Terry Captain` (`a6cc5885-f6da-41f2-8bdc-88596b176d96`) has a separate Player Auth account and tournament contact; both carry `1R9LNE`. Clearing only the incorrect DUPR ID is the narrow proposed remedy, pending owner authorization. No changes performed. Deactivation alone would not resolve importer ambiguity because duplicate counting includes inactive members. Nine duplicate-ID groups among active members (eight excluding Terry), two active records each. See `docs/terry-dupr-duplicate-read-only-review.md` for member IDs, masked emails, linked history and inventory. Do not infer authority to merge, deactivate, unlink Auth, or repair data from this review.


### 2026-09-11 — Terry DUPR correction and local assignment validation

Owner-authorized production correction cleared only Terry Captain a6cc5885-f6da-41f2-8bdc-88596b176d96.dupr_id. Terry Adelman 43e1e363-82f1-47d7-a869-befed4c967b8 retains 1R9LNE, now exactly one member match. Production CSV preview is READY TO IMPORT with Doubles 4.0, RF 100, Age-Based 4.3; no Import/Clean was run. Protected hashes and the other eight duplicate groups are unchanged. Local branch codex/member-dupr-assignment-validation adds shared assignment checks to Create/Edit Member, desktop/mobile Ratings inline ID, and MembershipWorks import. No deployment or SQL migration. 66 targeted tests pass, lint has zero errors/six existing warnings, build passes. Application preflight is not cross-client atomic; defer database unique enforcement until all legacy duplicates are individually reviewed. Propose a small manager-only duplicate filter/count. See docs/terry-dupr-correction-and-prevention.md and its evidence JSON.


### 2026-09-11 — DUPR-ID prevention PRODUCTION ACCEPTED

Owner-approved exact commit 7c0e5c7c1fff3803f6b3c2593175dd5ef11c9037 deployed as dpl_8YTFfmCy1VF4thuJoRMd2X9R49T1 to league.lwrpickleballclub.com. Production-source archive verified byte-for-byte (1,165 files); 26 published-validator cases passed against a read-only 1,980-member snapshot with zero writes, and all four deployed handlers validate before writing. Live member/forms/ratings/MembershipWorks/Teams pages passed focused read-only checks. CSV preview preserves 11 ambiguous rows; Terry remains unique at 4.0/100/4.3. Member/ratings/role/Auth/roster/import and eight-duplicate-group hashes unchanged; ratings private history unchanged after preview. Owner confirmed the concurrent Dill With It (Windward) team edit was intentional. No SQL, uniqueness constraint, Ratings Import, Clean, or production business-data mutation by this task. Rollback deployment dpl_7TPb3ZXyPMpPrgMy35CEpDH4cvSd retained. See docs/dupr-prevention-production-acceptance.md. Application-only concurrency limitation and individual duplicate review remain documented.


### 2026-09-11 — Full DUPR input precision: STOP FOR SQL REVIEW

Owner supersedes earlier Upload truncation expectation: visible DUPR Doubles and internal age retain full precision; only Clean truncates final Season/PrimeTime. No new age grid column. Live working columns already support precision, but ratings_workflow_private.plan truncates Upload/Transfer fills in SQL; app-only correction cannot fix commit storage. Preview formatter also truncates. Read-only current Fall impact: 620 Doubles and 556 age fields across 625 members, with full source/provenance available; Saturday blank, no repair needed. Terry currently stored 4.0/100/4.3, source 4.077/100/4.311, finals 4.00/4.30. Eight local diagnostic checks pass, including actual Clean preview/commit boundaries and preservation of all inputs. Proposed scoped function mapping + Upload/Transfer fingerprint version + preview formatting correction requires review; no app/SQL deployment or business mutation. Separate audited row-level repair would need owner approval. See docs/ratings-full-precision-sql-review.md.


### Full-precision ratings SQL and repair — local review only

Owner-authorized local planner correction preserves source Doubles/Age precision, versions Upload/Transfer receipts, and corrects administrative preview formatting. Clean semantics and fill-blank-only uploads remain unchanged. Production-derived evidence identifies 625 affected members; 623 are repairable (618 Doubles + 554 Age fields), with Barry Wolf and Chris Oleson excluded for source-snapshot conflicts and zero detected later manual edits. Terry proposes 4.077 / 100 / 4.311. At RF cutoff 29, all 625 retain both current final results. Local repair and guarded rollback, 14 focused tests, static/build/PDF checks pass; full-suite candidate has the same ten established baseline failures, zero candidate-only failures. No production migration, repair, Upload, Clean or deployment. See [local review](ratings-full-precision-local-review.md) for migration SHA, security, conflict limitations and controlled sequences. STOP FOR REVIEW.


### Full-precision production migration/application — acceptance pending

Exact approved SQL SHA c3b4cfb6b433087c6b419898ffacfca282c4f0da2f7ed41b77f81394b7c4e811 applied once as migration 20260912001226. Application 6397e53 is READY on production deployment dpl_5GBYhis9FKxzmjMu5gBrqDxVdwLf. Security and scoped zero-business-write checks passed; live preview remains 625 affected / 623 repairable / 2 source conflicts / 0 detected later manual edits. No repair or other ratings action executed. Commissioner session expired; signed-in smoke and CSV Preview Ratings remain prerequisites for the authorized single guarded repair. See docs/ratings-full-precision-production-acceptance.md and production-progress JSON. Resume gates only; do not repeat migration or deployment.


### Full-precision ratings — PRODUCTION ACCEPTED

Completed the owner-authorized single guarded repair on production commit 6397e53, deployment dpl_5GBYhis9FKxzmjMu5gBrqDxVdwLf. Run 251adf4f-3be7-4918-83e7-0789c756d3af restored 618 Doubles + 554 internal Age fields across 623 members; RF/finals unchanged, zero failures. Barry Wolf and Chris Oleson remain excluded source conflicts. Terry now stores/displays 4.077 / RF 100 / internal 4.311, with finals 4.0 / 4.3 unchanged. Protected rating fields, source batches, existing audit and scoped operational hashes remain unchanged. Read-only Clean comparison at RF 29 changes zero results across 1,986 members. Signed-in dashboard/Ask LWR/View-As and actual CSV preview smoke passed. No Import or Clean ran; normal full-precision fill-blank Upload is safe. See ratings-full-precision-production-acceptance.md and production-progress JSON. Repair completed once; do not repeat migration, deployment or repair.

### September 12, 2026 — Ask LWR community Rule 3.5 FAST FIX — PRODUCTION ACCEPTED

Application f0d6849a814798b79d7396a48a6b05f3d47425b8 deployed as dpl_4jw8S8P8gmHgDj9jjpTsyeqeW993. Current Rule 3.5 permits other-community teams and recommends local play in the same league/division with roster space; historical-prohibition-only selection caused both reported failures. Three scoped application files corrected; no intent expansion, policy/corpus/SQL or business mutation. 50 affected tests, lint/build/types/PDF pass. Exact and own-community production replays cite Rule 3.5 Page 2 as OFFICIAL RULES; self-team control remains LIVE LMS DATA. Normal Teams and scoped integrity hashes pass. Two generation calls: 3490 input/133 output tokens, estimated $0.02144 excluding embeddings/cache/billing reconciliation. Previous accepted dpl_5GBYhis9FKxzmjMu5gBrqDxVdwLf retained for application rollback. See docs/community-rule-fast-fix-production-acceptance.md.

### September 12, 2026 — Season Ratings post-success refresh — local verified, sign-in preflight pending

Owner-authorized application-only FAST FIX d5db069335cf85587cae26741b6434c4e9879cbf corrects stale uncontrolled grid inputs after Import/Clean, expands actual committed-result summaries, clears consumed preview/receipt/file only after success, and separates transaction from refresh failure. Nondefault season preserved. Actual React browser fixture shows Import 4.077/100 and Clean finals 4.0/4.3 with persistent summaries; 56 focused tests, lint/build/types/PDF pass. No SQL/calculation/matching/security/receipt-validation/Delete/Copy changes. No production Import/Clean. Deployment is held for normal signed-in preflight because the browser session expired. Prior accepted dpl_4jw8S8P8gmHgDj9jjpTsyeqeW993 is the recovery target. See docs/ratings-post-success-fast-fix.md; continue from this candidate after sign-in, without redoing implementation or production writes.

## September 12, 2026 — Season Ratings post-success FAST FIX accepted

FAST FIX — PRODUCTION ACCEPTED. Application d5db069335cf85587cae26741b6434c4e9879cbf, production deployment dpl_CiR76cjRdyW9Sn6GTn5wRRnYB1aF (lwrpc-admin-q8jvt0rik-terry-lwrpc.vercel.app). Import/Clean now show actual committed totals, clear stale preview/receipt, refresh server ratings and visible grid inputs, and retain selected season and result. Successful Import resets CSV; cancel/failure preserve input. Root cause was uncontrolled inputs retaining stale defaultValue after an existing refetch. No SQL/calculation/security/transaction or Delete/Copy change. 56 focused tests, lint/build/TypeScript/PDF bundle pass; safe real-page synthetic success evidence and signed-in read-only production checks pass. Ratings, teams, rosters and workflow-run fingerprints unchanged. No production Import/Clean performed. Recovery: dpl_4jw8S8P8gmHgDj9jjpTsyeqeW993; no DB rollback. See docs/ratings-post-success-production-acceptance.md. Upload ambiguous/invalid counts remain combined REVIEW as returned by server.

## September 12, 2026 — Ask LWR pair eligibility diagnosis only

STOP FOR REVIEW; no application or production changes. Pair questions fall through both classifiers, while "Are Terry and Tim eligible for DUPR8?" is misclassified as policy-only. Accepted eligibility lookup is SELF-only; other-member numerical rating reads omit NR inputs, and ordinary player-role other-member lookup is denied in deployed SQL. Full pair support requires reviewed private projection/authorization and two-subject context composition. Active Rules version 478a87bb-1b05-4da9-ac09-7ddecec64f69 differs from deployed eligibility binding 6ae10e5f-fdde-41be-a941-d1b7ed360d1a; preserve fail-closed behavior pending source review. See docs/ask-lwr-pair-eligibility-diagnosis.md for the twelve requested findings and proposed tests. No member searches, business writes, migrations, deployment or model usage.

## September 12, 2026 — Member search response race FAST FIX accepted

FAST FIX — PRODUCTION ACCEPTED. Application d399272a4e3cfcd1026c04c67db77856543260ec, deployment dpl_B5vYMadFVmMA1GLG9DLxyvgKLK3p (lwrpc-admin-50jhr949a-terry-lwrpc.vercel.app). MembersPage ignored response ordering: slow old broad searches could overwrite the final Steve results. One-page request-generation guard fixes this without SQL, matching, authorization or business-data changes. Nine focused/neighboring controls, lint/build/TypeScript compilation and bundle checks pass. Production fast steve and Steve with Last Login DESC returned stable 23 matching rows; Clear Search and terry controls passed; browser/runtime errors zero. Members, teams, rosters and ratings fingerprints unchanged. Recovery target dpl_CiR76cjRdyW9Sn6GTn5wRRnYB1aF; no DB rollback. See docs/member-search-production-acceptance.md. No production mutations or model usage.

## September 12, 2026 — Pair eligibility continuation: minimum approval scope

Fresh diagnosis against production d399272 confirms both Terry-with-Tim and Tim-with-Terry miss eligibility/Live routing. Deployed SQL still provides SELF-only eligibility and denies ordinary-player other-member lookup. No pair feature was previously implemented. STOP FOR REVIEW remains required. See docs/ask-lwr-pair-eligibility-minimum-approval.md for a concrete single private ELIGIBILITY_PAIR read operation, minimal server-only NR/placement projection, audit allowance, current Rules binding review, and deterministic two-subject coordinator. Recommended initial access preserves existing authorized member populations; broader ordinary-player partner access requires a separate decision. No application/SQL/production mutation or Approved Answer created.


## September 12, 2026 - Ask LWR pair eligibility local review

Owner approved the minimum ELIGIBILITY_PAIR private operation and explicitly required STOP FOR REVIEW before production. Implemented on isolated `codex/eligibility-pair-local`: independent target authorization, bounded retained clarification, deterministic Rated/NR pair calculation, current active Rules binding, private audit and tested rollback. Ordinary Player partner access remains denied; View-As denied. No production SQL/deployment/business mutation. See `docs/ask-lwr-pair-local-review.md` for the 22-item review and validation evidence. Broader Player partner access remains a separate owner/security decision.


## September 12, 2026 - ELIGIBILITY_PAIR production accepted

Exact commit `74df01d`, deployment `dpl_5hYSN6eZN7SVJUQi7Uch7s5D2Nwd`, reviewed migration applied once (`20260912192743` history record). Both exact name orders resolve Terry Adelman + Tim Carnahan and return identical Weekday MDUPR8 pair rating compliance: 4.0 + 3.8 = 7.8 <= 8.1 under current Rules `478a87bb-1b05-4da9-ac09-7ddecec64f69`. Ordinary Player and View-As denied. Existing route controls pass; no release business writes. Owner concurrent team/role changes recorded. Packaging/deployment error was contained by removing the accidental separate project and verifying exact blobs before correct deployment. See [23-item acceptance and limitations](ask-lwr-pair-production-acceptance.md). No access expansion started.

## September 12, 2026 — Captain prospective pair access review
STOP FOR REVIEW: existing ELIGIBILITY_PAIR permits managed-roster players but excludes prospective players. The roster picker uses broad browser reads/client filters, not a bounded prospective lookup. Required minimum: reviewed private SQL team-scoped candidate/facts authorization plus retained team context through clarification/read/audit; preserve Player/View-As denial. Local pair suite: 33/33 passed. No app/SQL/production changes. See docs/ask-lwr-pair-captain-access-review.md.


## September 12, 2026 — Captain prospective pair local implementation

Owner-approved minimum private authorization extension is implemented in the isolated codex/eligibility-pair-local checkout. Specific managed team is required before named prospective lookup; its exact division/season survives player clarification and is rechecked through final audit. Captain/co-Captain, rostered/prospective/mixed, NR blank, Player/View-As denial and Commissioner controls pass. No directory/RLS/table-grant expansion. Local focused 40/40, native PostgreSQL 17 replay/security/rollback, lint (0 errors, 6 existing warnings) and build pass. Full baseline 1331/1312/19; candidate 1340/1321/19, same failures, no new failures. STOP BEFORE PRODUCTION: no SQL application, deployment or business mutation. See docs/ask-lwr-pair-captain-local-review.md for all 18 review items, hashes and proposed controlled production sequence.

## September 12, 2026 — Captain prospective pair deployed; acceptance pending

Owner-approved exact migration applied once (history 20260912210918), exact application 9e02e61 deployed READY as dpl_EesjhBpXCrugcoUAihC4Fy7wqzhB. Actual Terry Captain managed-team flows pass both exact name orders with identical 7.8 <= 8.1 result; non-rostered Terry Player resolves and safely reports missing Rated/NR status. Team context, privacy, audit, Player/View-As/unmanaged-team denials and business hashes pass. Existing Ask LWR controls pass. Final calculated prospective/NR case and remaining legitimate-session normal LMS/Commissioner UI checks pending; not marked accepted. No business writes or View-As session. See docs/ask-lwr-pair-captain-production-review.md and production evidence JSON. Do not reapply migration or redeploy the same package.

Owner accepted the prospective NR production-data coverage limitation: authorized scope has 143 active unrostered players (0 RF <=29 NR, 29 RF >29 Rated, 114 missing RF that cannot be assumed NR). Retain accepted local/PostgreSQL prospective NR + blank Season DUPR evidence; explicitly label the branch not independently exercised with live production data. This limitation is not a release failure. Final acceptance still awaits remaining real-session UI checks and satisfied security/regression/integrity gates. Only the Captain browser is currently connected; no View-As or manufactured business data.

## September 12, 2026 — Captain prospective pair production accepted

CAPTAIN ELIGIBILITY_PAIR PROSPECTIVE ACCESS — PRODUCTION ACCEPTED. Real Terry Adelman Commissioner UI passed dashboard, Teams, Kitchen Killers roster, Season Ratings, own Player-dashboard rendering and exact pair regression (7.8 <=8.1). Earlier real Captain managed-team/context/privacy/audit evidence retained. Final READY alias is exact 9e02e61; reviewed helper hash/security intact; all 14 business snapshots unchanged. No View-As session or business mutation. Owner-approved coverage exception: 143 authorized active unrostered prospects (0 confirmed NR, 29 Rated, 114 missing RF); prospective NR + blank Season DUPR relies on accepted local/PostgreSQL evidence, not an independently exercised live NR case. Full 19-item review and final evidence in docs/ask-lwr-pair-captain-production-review.md. Stop after acceptance; do not reapply/redeploy.

## September 13, 2026 — Season DUPR rounding FAST FIX accepted

FAST FIX — PRODUCTION ACCEPTED. Two existing intent/applicability matchers now recognize Season DUPR rounding/decimal/tenth questions and retrieve active Rule 4.2 page 3. Exact production fallback reproduced before correction; exact question and variant now explain truncation (3.496 ->3.4), and Saturday recording-date control remains September 27. 111 affected tests, lint (0 errors/6 existing warnings), build/TypeScript and scoped diff pass. Exact c88447fde5c31e87b02137bcef4fd94618d43495 deployed READY as dpl_8qBAzd6FrjawH9graP7Hjqe47Zrs. Fresh 14-table business hashes unchanged; no SQL/security/corpus/Approved Answer or business writes. Three acceptance generations: 7,564 input/146 output tokens, estimated $0.0422 excluding embedding/cache adjustments. Previous application retained for rollback. Full evidence: docs/season-dupr-rounding-production-acceptance.md.

2026-09-13: FAST FIX — PRODUCTION ACCEPTED: Lifetime-ball retrieval wording. Isolated application d1e2871 / dpl_EPJYR5Nz4DZmmkr8eXDpyf5Vg987. Exact+variant+USAP neighbor verified; 127 tests, lint/build pass, 14 business hashes unchanged. See docs/lifetime-ball-production-acceptance.md.

2026-09-14: FAST FIX — PRODUCTION ACCEPTED: Match Score Sheet access, isolated application92dd94d / dpl_39SWo7yb9HnM4vMnbEGjATj26E4a. Exact+variant+score-entry control verified,165 tests and lint/build pass,14 business hashes unchanged. See docs/match-score-sheet-production-acceptance.md.

2026-09-14: Owner-authorized member-location label correction:160 members with exact club_location 'Azario - Esplanade' changed to 'Esplanade at Azario' (no period). All already referenced location_id15d76227-fe8c-46d3-8796-7801ffd7924c whose name was correct. Transaction asserted160-row scope and unchanged hash of every other member field. Recovery IDs/old values retained locally in .local-validation/azario-member-label-before-20260914.json. Member grid displays denormalized club_location, explaining why location rename did not update the visible member labels. No app/schema/security changes.

## Saturday DUPR posting FAST FIX — PRODUCTION ACCEPTED (2026-09-14)
Isolated branch codex/saturday-dupr-posting-fast-fix, application13ef008; READY dpl_5LzEeFQHZt9FoEuhe9X2ASH1VKWD. Exact/variant production questions now cite6.2.3.6 p10 and correctly exclude mixed doubles/Picklebreaker. Neighbor scoring control passes;175 focused tests, lint/build pass;14 protected table hashes unchanged. See docs/saturday-dupr-posting-production-acceptance.md. Primary checkout application was not deployed or broadly committed.

## DUPR entered-into wording FAST FIX — PRODUCTION ACCEPTED (2026-09-14)
Isolated branch application e35fbc4 / READY dpl_8a29p1t37aMyw1f4M2JPdJeXj5Jo. Both screenshot questions production-pass with6.2.3.6 p10; original posting and Saturday scoring controls pass.180 tests, lint/build pass;14 protected table hashes unchanged. See docs/saturday-dupr-entry-fast-fix.md. Primary checkout application not deployed.

## PBCC Not played — LOCAL IMPLEMENTATION READY FOR REVIEW (2026-09-14)
Isolated codex/pbcc-not-played branch. Existing not_played schema reused, no SQL. Court Not played/Reopen controls, blank scores, skip-only completion and history labels.9 tests, schedule-balance verification, lint/build pass; no production changes. Scoring-change release review required by FAST FIX workflow. See docs/pbcc-not-played-review.md and docs/pbcc-not-played-preview.html.

## PBCC GitHub Desktop handoff correction
Owner's main deployment d4a47c8 / READY dpl_SimiHDLtsevTYezL9MfzuxbrNy4C did not include isolated c93cf43 Not played changes. Transferred the scoped PBCC code/test patch into primary working tree for the owner's next commit/push.9 tests, lint (11 existing warnings), build pass. No agent deployment or business writes. See docs/pbcc-not-played-review.md.

## Password reset invalid-link UI — locally verified
Primary working tree now rejects invalid/expired recovery URLs before enabling password or passkey actions and rechecks the session before submission. Nine focused controls, lint (11 existing warnings), build, and local built-page browser checks pass. No production credential changes or deployment. Does not repair expired email links themselves. See docs/password-reset-ui-fix.md.

## Password visibility controls — PRODUCTION ACCEPTED (2026-09-15)
Account Security now provides independent accessible Show/Hide eye buttons for New Password and Confirm New Password. Application `fc05970` / READY deployment `dpl_3g8znJ2WgtjoHiGTL1PaD1jWLiZW`; 12 focused tests, lint, local/Vercel builds, normal sign-in smoke checks, feature UI checks, and runtime error scan passed. No database, security-policy, credential, or business-data changes. See docs/password-visibility-production-acceptance.md.

## Kitchen-fault claim retrieval FAST FIX — PRODUCTION ACCEPTED (2026-09-15)
Exact opponent kitchen-fault claims now retrieve the complete active USAP call procedure: Rule 11.A.1, Rule 9.B.3, and Rule 9.B.3.b. Application `b97f4ff` / READY deployment `dpl_Dj43Zji3G9BwXyx9ZfReNnrjAibu`; 42 focused tests, lint/build, exact/variant/neighbor production-model replays, normal sign-in smoke checks, and runtime error scan passed. Three validation generations used 6,078 input/266 output tokens, estimated $0.03837 excluding embedding/cache adjustments. No database, corpus, Approved Answer, security, or business-data changes. See docs/kitchen-fault-claim-fast-fix.md.

## Picklebreaker same-partner FAST FIX — PRODUCTION ACCEPTED (2026-09-16)
Application f58de6f / READY dpl_USE6qPsje5xz5mzp1XyWXG6EBjz8. Exact plural/mix-team wording and natural partner-change variant now answer No using active Rule 6.2.3.5 page 9. General Picklebreaker control passes. 69 focused tests, lint/build pass; normal Commissioner Teams unchanged; all 14 protected-table fingerprints equal. No SQL, corpus, Approved Answer, authorization or business-data changes. See docs/picklebreaker-partners-fast-fix.md.

## Saturday DUPR posting restoration — PRODUCTION ACCEPTED (2026-09-16)
Restored posting-only changes lost when isolated deployments returned to main, plus a narrow standalone posting exception for “that.” Application e88946c / READY dpl_Ej7wcMdKbMMTNSrHrs6PjHu8S5aF is now on production main. All three reported questions cite Rule 6.2.3.6 page 10 with correct mixed/Picklebreaker exclusions; partner continuity control retained. 69 focused tests, lint/build pass; normal Teams and 14-table fingerprints unchanged. See docs/saturday-dupr-posting-restoration.md.

## Generalized Ask LWR retrieval — LMS-0734 / 0.1.556 — LOCAL, LIVE MODEL ACCEPTANCE PENDING (2026-09-16)

Owner-authorized architectural correction, outside FAST FIX. Exact PrimeTime schedule question already retrieved the correct Important Dates chunk at rank 1 (0.7566 versus threshold 0.35); applicability lost the evidence. Added bounded model query interpretation, expanded/rescue hybrid searches using unchanged RPC scope/context, soft ranking affinities, constrained semantic relevance for unknown terminology, and admin diagnostics. Existing recognized policy/concept safeguards, authority, active/searchable source validation and business data remain intact. Full 1,224 tests and final 42 affected checks pass; lint has 11 existing warnings; build passes. Recorded-source replay selects the exact schedule date with a mocked planner. Automatic approval review blocked live planner metadata disclosure to OpenAI; live model acceptance remains pending explicit approval. No SQL, corpus edits, production writes or deployment. Source carried an older version label; release history already used 0733, so the distinct new application identity is 0734 / 0.1.556. See [diagnosis, scores and validation](generalized-retrieval-lms-0734.md).

## LMS-0734 / 0.1.556 — OPENAI-BACKED VALIDATION FAIL, NOT DEPLOYED (2026-09-16)

Owner approved read-only official-document/model validation, superseding the prior external-disclosure approval blocker. Sixteen final cases: 14 PASS, 2 FAIL. Exact PrimeTime schedules question and three variants pass; other calendar paraphrases and known Rules/Captains Guide/LMS controls pass. Blocking new behavior: Saturday end-of-season answer uses February 2026 instead of chronological 2027. Scoring-sheet fallback remains (also fails unchanged baseline e88946c); semantic assessor ID output rejected safely. Rescue diagnostics sometimes claim execution despite zero rescue queries. Contact-bearing payloads were blocked initially and omitted for replays. No application edits, production data/config/security/document-status changes or deployment during this validation. See [all questions, scores, selected evidence, answers and baseline comparison](lms-0734-openai-validation.md).

## LMS-0735 / 0.1.557 — LOCAL VALIDATION PASS, NOT DEPLOYED (2026-09-16)

Corrected the three LMS-0734 blockers while preserving generalized retrieval. Revalidated event chronology now supplies traceable per-event years (including explicit-year precedence and cross-year rollover) to both date selectors and generation. Semantic evidence IDs are constrained to the current candidate allowlist; server rejection remains intact with one bounded repair attempt and precise diagnostics. Rescue considered/triggered/executed/returned/selected states now reflect actual calls/results. A nullable-heading source-validation edge case was caught and corrected. Final full suite: 1,236/1,236 pass, zero skips; lint zero errors with 11 existing warnings; production build pass. Final-code OpenAI replay: 16/16 pass, including all PrimeTime variants, Saturday February 2027, scoring-sheet access, Rules, Captains Guide ball, LMS Match Setup and safe unsupported-topic refusal. Final replay completed in two batches after the first local process ended after eight completed cases; all sixteen final cases and rescue/ID invariants are recorded. No production data, SQL/RLS, activation/configuration changes or deployment. Recommend controlled application deployment review only when owner authorizes. See [full evidence and before/after report](lms-0735-validation.md) and [local source identity](lms-0735-local-identity.json).

## LMS-0735 / 0.1.557 — DEPLOYED, ACCEPTANCE STOPPED / NOT ACCEPTED (2026-09-16)
Owner-approved commit acf4341949546eb5070f1012e47efb6f44678e34 deployed through main to READY dpl_EjCSVvvYtLrxvoASkRPwZsVxN8yy. Real Commissioner preflight and post-deploy Teams smoke passed. Seven production questions passed (all four PrimeTime variants, Saturday 2027, scoring sheet, Rules posting). Eighth question, “Are we going to use the Lifetime ball”, returned document fallback; acceptance stopped immediately. General USAP ball passages dominated evidence; semantic assessor validly returned no IDs, generation skipped, NO_APPLICABLE_EVIDENCE_AFTER_RESCUE. Remaining Match Setup and unrelated date controls were not run. Rescue UI states verified across not-needed, considered-only, failed-query and successful-selected paths. No fixes or rollback performed after stop. Twenty-six of twenty-eight integrity fingerprints unchanged; Teams hash and user_roles count (206 to 208) changed during window, attribution unconfirmed. No business writes were issued; documents, security/RLS, migrations and configuration hashes unchanged. Release remains deployed but NOT PRODUCTION ACCEPTED. See docs/lms-0735-production-acceptance.md and docs/lms-0735-production-integrity.json. Prior e88946c READY deployment remains recovery target.


## LMS-0736 / 0.1.558 — proposed-value verification, local only (2026-09-16)

Owner directed LMS-0735 to remain deployed but NOT accepted, with no rollback. Exact Lifetime-ball fallback reproduced against baseline: the page-10 Captains Guide Match Balls assignment was present at 0.3776 but displaced by generic USAP candidates before applicability; the proposed brand remained a required search entity. Local generalized verification separates subject/value/scope, uses value-independent subject queries, preserves club candidate diversity and same-version scope, and requires an exact affirmative source quote/value before matches/contradicts. No brand-specific production constants or Approved Answer were added. Known policy gates, evidence-ID validation, chronology and rescue execution semantics remain.

Final local checks: 1,257/1,257 automated tests; 27/27 real OpenAI read-only official-document cases (the previous 16 plus 11 verification cases); lint 0 errors/11 existing warnings; build passed. Exact failure now cites Franklin Outdoor X-40 optic yellow; all PrimeTime schedule answers retain Oct. 7, 2026, Saturday end retains Feb. 20/27, 2027, and score-sheet navigation passes. Missing-assignment controls do not infer No. Full before/after: docs/lms-0736-validation.md and .json.

Prior production deltas attributed read-only: Wild Blue Crush updated 18:01:10.409 UTC; Captain roles for current captain/co-captain followed at 18:01:12.853566/18:01:13.037650 UTC. Sequence matches normal team-save role assignment, actor not proven because no general team actor/history record was found. No evidence of Ask LWR writes; no reversal. See docs/lms-0736-data-delta-attribution.md. No production business/security/document/config changes or deployment were made. Hold LMS-0736 for owner review and separate deployment authorization; no production acceptance claimed.

## LMS-0736 / 0.1.558 — PRODUCTION ACCEPTED (2026-09-16)

Owner-authorized c52f74f863f5beb27b51307e915549de65a78def deployed to READY dpl_3HPPGWmv4v9ixRHa3KJguQRSexD3 via controlled GitHub main deployment. Real Commissioner preflight and post-deploy Dashboard/Teams/Season Ratings passed; no View-As. Production questions 18/18 PASS: original Lifetime ball now correctly rejects the proposed brand using Captains Guide Franklin Outdoor X-40 assignment; matching/conflicting/unknown controls pass. All twelve required LMS-0735 regression controls pass, including four PrimeTime October 7, 2026 questions, Saturday February 2027, score-sheet navigation, posting, Rules, ball, Match Setup and two unrelated dates. Rescue counters match executed retrieval paths in all cases. All 28 protected integrity fingerprints unchanged; no concurrent business-data changes observed. No code edits during acceptance, no business/security/document/config changes; prior LMS-0735 ordinary-team-save attribution retained. LMS-0735 dpl_EjCSVvvYtLrxvoASkRPwZsVxN8yy retained for rollback. See docs/lms-0736-production-acceptance.md and .local-validation/lms0736-production-integrity.json. No remaining acceptance blockers.

## 2026-09-16 — LMS-0737 / 0.1.559 FAST FIX accepted

Deployed 4d3c4a9c18ee1dda8d8c77d119defcf3b9044e4c through GitHub/Vercel. Explicit current-topic questions now bypass the mistaken short-pronoun follow-up classification; Season DUPR rating-date wording selects existing official calendar evidence. Exact owner question and variant answer September 27, 2026 in the production drawer, with Important Dates citations; ball contrast passed. 122 focused tests, lint and build passed; three local OpenAI and three production cases passed. No business data/security/corpus/config changes by the agent. Owner confirmed concurrent deletion of 1,759 Season Ratings; 27 other fingerprints unchanged. Prior blank-panel cause remains unconfirmed. See docs/lms-0737-production-acceptance.md and docs/ask-lwr-rating-date-blank-investigation.md. Known-good application recovery remains LMS-0736.

## 2026-09-16 — LMS-0738 / 0.1.560 accepted

DUPR CSV parser automatically tolerates omitted empty trailing header fields; all named-field, nonempty-column and quoting validations retained. Exact unmodified 934-row owner export passed production preview with zero invalid ratings and no commit. 38 focused tests (7 new), lint and build passed. 28 integrity fingerprints unchanged. Commit 7c72f89973aba08e3de2856cfe5de8c6bcf77836 / deployment dpl_Hxpxk6erDFT8szveYas98Fk9kST1. No import-policy/calculation/security/SQL changes. Details: docs/lms-0738-csv-header-compatibility.md.

## 2026-09-16 — LMS-0739 / 0.1.561 local Upload restoration candidate

Owner-approved local implementation reconnects Upload to the existing protected workflow transaction; unchanged source evidence now fills deleted/missing selected-season working inputs. No new SQL or authorization expansion. Preview separates CSV/current/fills; committed summary, file/receipt clearing and selected-season refetch verified. 1,287/1,287 full automated tests, 46/46 final focused controls, lint 0 errors/11 existing warnings, build and synthetic local browser checks pass. Clean/Delete/Copy unchanged; legacy Clean Age-Based handling is outside this Upload scope. NOT DEPLOYED; no production business writes. Review: docs/lms-0739-upload-restoration-local-review.md.

## 2026-09-16 — LMS-0740 / 0.1.562 local Age-Based Clean correction

Owner approved local correction/testing after read-only evidence showed 8EPKQE Fall input 4.659 but final Age-Based rating blank. Clean now fetches the separate input and uses it to propose 4.6; absent inputs preserve legacy final truncation. No Upload/Delete/Copy, RF/NR policy, SQL or security changes. Successful Clean advances the refreshed grid key. Eight new synthetic planner/write-flow tests pass alongside four existing RF controls; full suite 1,295/1,295 passed, lint 0 errors/11 existing warnings. Local build encountered EPERM on existing .next cache and is being retried with local filesystem permission. Candidate remains LOCAL ONLY, not deployed; no production writes. Details: docs/lms-0740-age-based-clean-local-review.md.

LMS-0740 final verification: local production build retry PASSED; the initial failure was cache-file permission only. All local gates above passed. Remains not deployed.

## 2026-09-16 — LMS-0741 / 0.1.563 PBCC night balancing (local)

Owner-approved priorities: unique nightly partners, balanced courts/byes, no avoidable consecutive triples/quartets, and spaced pair recurrence. Local two-court 8/9-player planner replaces the fixed-preset bypass with bounded whole-night search, exact court-orientation exploration, deterministic spacing improvement and verified-prefix continuation. Retains history through automatic/manual byes and omitted participants; deduplicates history and ignores explicitly not-played exposures; fails closed instead of silently repeating partners. Larger-roster/one-court/ladder behavior retained. Synthetic six/seven-game batch/next-game cases have unique partners, closest court splits and zero consecutive triples/quartets. Six-to-seven extensions also pass. Pair-only overlap remains (up to 4/6 or 5/7 in the eight-player cases; 4 in the nine-player cases), and global optimality is not claimed. Full suite 1,306/1,306 passed. Local-only candidate; no live matches, scores, notifications, SQL/security/config changes or deployment. Review: docs/lms-0741-pbcc-night-balancing-local-review.md.

LMS-0741 final local verification complete: full suite 1,306/1,306; focused scheduler 11/11; complete-night replay 8/8; lint exit 0 with 11 existing warnings; build and diff checks pass. No production acceptance or deployment performed.

## 2026-09-17 — Captain roster player-check notifications (local)

The Captain Dashboard's Manage Roster flow now leaves out-of-range players visible as Not Eligible but disables their dropdown options. Rating Needed and DUPR ID Needed players remain selectable and show an immediate explanation that adding them will notify League Management plus the player and assigned Captain/Co-Captain(s). The existing League Management alert is preserved; a separately editable Email Options template, Roster Player Check Alert-To Player, supplies the new player/captain email. Both sends are attempted independently after the roster insert, recipient addresses are trimmed/deduplicated, and delivery failures identify the affected audience without reversing the legitimate roster add. Six focused checks and the full 1,312-test suite pass; lint has zero errors and 11 existing warnings; production build passes after clearing only the generated `.next/cache` directory following the known Windows EPERM condition. Local only: no production emails, roster/template writes, SQL, schema, deployment, or business-data mutation.

## 2026-09-17 — Email Options link placement FAST FIX (pushed)

The Email Options rich-text editor now preserves its exact editor selection while the Add Link prompt has focus, then restores that range before applying the link. Highlighted text becomes linked in place; an empty caret inserts the entered URL at that caret; a missing or stale range falls back to the end of the message instead of the top. Three focused range checks and the full 1,315-test suite pass; lint and production build pass. The owner pushed commit `ed0b1ea` through GitHub. The application version remained `0.1.563`; required carry-forward: increment the application version with the next release and include this link-placement fix in that release's notes. No email was sent and no saved template or production business data was changed during implementation.

## 2026-09-17 — Missing roster information warning cards / 0.1.564 (local)

Team Roster Management now gives rostered players with a missing league-specific season rating or DUPR ID a pale-red card and an explicit Rating Needed or DUPR ID Needed badge. Captain Dashboard View Team uses the same classification and warning presentation; its existing roster read now includes the already-authorized DUPR ID field needed for that display. Missing DUPR ID takes precedence when both values are absent, and valid rated players retain the normal presentation. Application version advanced to 0.1.564, fulfilling the version carry-forward from the pushed Email Options link-placement fix. Eight focused checks and the full 1,317-test suite pass; lint has zero errors and 11 existing warnings; production build passes. Local only: no roster, rating, email, SQL, schema, authorization, or production business-data change was made and no deployment was performed.

## 2026-09-17 — Teams & Rosters CSV export / 0.1.564 (local)

Teams & Rosters now has an Export Teams button beside Copy Division Teams. It downloads one Excel-compatible CSV row for every loaded team, including inactive teams, with team identity/status, season/league/division and rating type, leadership names/emails/IDs, roster count, notes and timestamps. The existing Location relation now also reads and exports the linked street, city, state, ZIP, combined full address, court count and court notes. CSV quoting covers commas, quotes and multiline notes; formula-like cells are neutralized. Three focused export checks and the full 1,320-test suite pass; lint has zero errors and 11 existing warnings; production build passes. This remains part of the unpushed 0.1.564 candidate. No database, roster, location, email, SQL, schema, authorization, or production business-data changes and no deployment.

## 2026-09-17 — MembershipWorks Location Review CSV / 0.1.564 (local)

Members > Data Tools now includes a Location Review CSV button. It reads the latest MembershipWorks import audit batch and exports only rows that still meet the import's existing Location-review rules. The report identifies the member and source import row, shows MembershipWorks Location text, saved member Location text, linked canonical Location/ID, and gives a specific reason such as unmatched name, duplicate active exact match, missing catalog link, or possible alias/conflict. It is read-only, paginates large imports/member lists, ignores skipped import rows, and neutralizes spreadsheet-formula cells. The detailed review classifier now shares the same logic as the original import count while preserving the import helper's existing public result. Four new focused checks pass; full suite 1,324/1,324 passes; lint has zero errors and 11 existing warnings; production build passes. No version increment beyond the existing unpushed 0.1.564 candidate, no database/schema/security/business-data changes, and no deployment.

## 2026-09-17 — Clean Members safe Location-text correction / 0.1.564 (local)

Clean Members now retains its phone cleanup and also corrects the denormalized `members.club_location` text only when the latest non-skipped MembershipWorks import value and the member's existing linked Location name agree. It does not change `location_id`, infer aliases, act on conflicting/duplicate MembershipWorks values, or touch unmatched/unlinked records. Each Location-text write is guarded by the previously read text and Location ID so a concurrent member edit wins and is reported as skipped. The confirmation explains the expanded scope and the completion message reports separate phone and Location counts. Three additional focused controls and full suite 1,327/1,327 pass; lint has zero errors and 11 existing warnings; production build passes. Local implementation only: Clean Members was not run, no production member/business data changed, and no deployment was performed.

## 2026-09-17 — Rule 5.15.1 compensatory points / LMS-0742 / 0.1.565

League Standings now provides League Managers and Commissioners a controlled Rule 5.15.1 workflow per Division/Pool. It first captures an immutable, audited snapshot of the published starting schedule, counting distinct scheduled match dates per team. At season end it previews earned points, verified played dates, the maximum starting date count, missing dates, average earned points per played date, the raw adjustment, the single half-up whole-number adjustment, and final points. Finalization is blocked while a starting match is unresolved or standings contain matches outside the frozen baseline; verified forfeits count and explicitly cancelled matches do not. Finalized awards are persisted with their inputs, actor, timestamp and calculation version, then incorporated into the normal standings rebuild/ranking; displayed points identify the earned-plus-compensatory split.

The additive migration `20260918012928_rule_5_15_1_compensatory_points.sql` creates the baseline and award audit tables, adds earned/compensatory fields to `team_standings`, enables RLS, revokes browser access to baseline details, grants award reads only to authenticated users, and reserves all writes for the service role. An isolated PostgreSQL test verifies schema execution, RLS, effective permissions, constraints, defaults and cleanup. Six focused calculation/database controls and the full 1,333-test suite pass; lint has zero errors and 11 existing warnings; production build passes. The release advances the application identity to `LMS-0742 / 0.1.565`, satisfying the carried-forward version requirement. Migration must precede application deployment; no Rule 5.15.1 baseline or award business rows are created by the migration or application deployment.

## 2026-09-18 — End of Season Points placement and rule label / LMS-0743 / 0.1.566

The compensatory-points workflow is now discoverable under Match Operations as **End of Season Points**. Its action and dialog use the same label, and the interface identifies the governing reference as **DUPR Rules, Rule 6.3.9**. The workflow remains on League Standings so its existing League/Division selection, preview, baseline capture, validation, and finalization behavior stay unchanged. This is an application-only labeling/navigation correction: no calculation, authorization, SQL, schema, or production business-data changes.

## 2026-09-18 — End-of-season-only standings compensation / LMS-0744 / 0.1.567 (production rollout)

Match Operations now labels the destination **League Standings**, with **End of Season Points** retained as the action inside that screen and identified as DUPR Rules, Rule 6.3.9. The workflow no longer requires a starting-schedule capture. At season end it compares each team with the highest distinct verified match-date count in its Division/Pool, multiplies the team's earned-points average per played date by its missing-date count, and rounds the total adjustment once using half-up whole-number rounding. Same-date doubleheaders count as one date while every verified match still participates in the standings consistency check. Finalization is blocked for unfinished non-cancelled matches, verified matches without a scheduled date, participant/division mismatches, or standings that do not agree with the verified match count.

The additive migration `20260918114101_end_of_season_played_date_points.sql` makes an award's historical baseline optional and records the new calculation basis and played-date audit values; existing baseline tables and historical awards remain intact for rollback compatibility. It creates no award, standings, match, schedule, roster, or other business rows. Focused calculation/database controls pass, the complete suite passes 1,334/1,334, lint has zero errors and 11 existing warnings, the production build passes, and the diff check passes.

Production rollout used the current September 18 physical backup and preserved LMS-0743 commit `7c6a6eb` as the application rollback target. Migration `20260918120746 end_of_season_played_date_points` applied with all 19 protected fingerprints unchanged and zero award rows. Normal-first acceptance then caught a PostgREST team/division relationship ambiguity before feature acceptance. Compatibility migration `20260918121548 end_of_season_points_relationship_compatibility` removes only the two new cross-link foreign keys; the application pins Captain Dashboard to the established `teams_division_id_fkey`. No business row changed. The corrected candidate again passes 1,334/1,334 tests, lint with zero errors/11 existing warnings, and the production build. Corrective deployment and final production acceptance remain pending at this checkpoint.

## 2026-09-18 — Scheduling Special Requests / LMS-0745 / 0.1.568

Scheduling now opens with a clearly visible Special Requests section for League Managers and Commissioners. It provides add, edit, delete, date-aware sorting, member search, Division-to-Team filtering, and Location/Division/Team/Date list filters. Location, Division and Team are optional; Date, Member Requesting and Request are validated in the administrative form. The interface and database explicitly identify the records as informational administrative tracking only, and the existing schedule generator does not read or enforce them.

The additive `20260918150701_scheduling_special_requests.sql` migration creates only `public.scheduling_special_requests`, its indexes, timestamp/creator trigger, safe `ON DELETE SET NULL` relationships and four least-privilege admin RLS policies. Authenticated access is limited through the existing `private.current_user_is_lwrpc_admin()` helper, covering Commissioner and League Manager while denying players, captains and anonymous access. Isolated PostgreSQL coverage exercises creation, list retrieval, editing, deletion, optional fields, relationship validation, manager/commissioner access, player/anonymous denial and service-role recovery. Feature tests also prove the schedule generator remains request-independent.

Local release gates pass: 1,340/1,340 tests, lint with zero errors and 11 existing warnings, production build, focused browser rendering and diff check. The requested LMS-0744 identity was already occupied by the accepted End-of-season-only standings release, so this implementation correctly advances to LMS-0745 / 0.1.568 rather than reusing a production version. The additive migration and exact application commit `73ad31c` were deployed as `dpl_2sxwuuHJND9YNDWBycFAAyiahU2u`; normal-first and feature acceptance passed with zero Special Request rows, zero runtime errors, unchanged advisor baseline and all 19 protected fingerprints unchanged. LMS-0745 is production accepted.

## 2026-09-18 — Scheduling default and option order / LMS-0746 / 0.1.569

Scheduling now opens on Schedule Settings and presents its options in the established operational order: Schedule Settings, Court Unavailability, League Blackout Dates, then Special Requests. This is a navigation/default-only application correction. Special Requests CRUD, filters, validation, database access and informational-only scope remain unchanged, as do schedule settings, court availability, blackout dates and the schedule generator. No SQL, migration, permission, RLS, scheduling-engine or business-data change is included.

Focused scheduling coverage passes, the complete suite passes 1,340/1,340, lint has zero errors and 11 existing warnings, the production build passes, and the diff check passes. The release advances the application identity to LMS-0746 / 0.1.569.

## 2026-09-18 — Division Standings and Division Schedules pages / LMS-0747 / 0.1.570 (local)

The People & Teams Division Standings and Division Schedules entries now use durable App Router destinations at `/division-standings` and `/division-schedules`. Both pages enforce the established League Manager-or-higher boundary, load the same active league/division or pool choices, and retain the existing read-only standings and published-schedule queries and displays. Direct URL entry and optional Division/Team query parameters initialize the requested scope, while the shared LMS sidebar, Administration Desktop cards and browser history now use ordinary route navigation.

The obsolete Administration Desktop popup state, loaders and render paths were removed. The reusable team schedule display gained a page presentation so the dedicated route shares its existing match, bye, rating and team-detail rendering without copying that logic. Contextual schedule overlays used inside Player, Captain, Teams & Rosters and League Standings remain intentionally available for their separate dashboard/team-detail workflows. League Standings and its End of Season Points implementation are unchanged. No SQL, migration, RLS, permission, standings formula, schedule generation, match, scoring or production business-data change is included.

Five dedicated navigation controls and the existing active-team/End of Season Points controls pass; the complete suite passes 1,345/1,345, lint has zero errors and 11 existing warnings, the production build exposes both routes successfully, and the diff check passes. Local unauthenticated direct-route verification confirms both pages retain the existing login/League Manager boundary. Authenticated production acceptance and deployment remain pending because this navigation architecture release is outside the FAST FIX lane.

## 2026-09-18 — Teams & Rosters data-load correction / LMS-0748 / 0.1.571 (local)

Production read-only diagnostics found all 119 team rows intact and readable, with unchanged authenticated SELECT policies and grants. The Teams & Rosters page nevertheless displayed `0 of 0` because its single PostgREST request embedded Division → League → Season, Location, and four Member relationships. The authenticated production request was killed by the PostgREST timeout manager; the page discarded the query error and converted the resulting null data to an empty array. Completed-query counters around a clean reproduction showed both paginated Member requests and the roster-count request completing while the embedded Team request did not complete. The expanded embed was introduced in LMS-0741d commit `7b67fec`, so the defect predates LMS-0747 and is unrelated to the new Division page navigation.

The local correction replaces only that fragile embedded Team request with explicit shallow reads through the same authenticated Supabase client and rebuilds the existing nested display shape in memory. It still starts exclusively from team rows returned by Supabase, preserves team/division/league/season active filtering, roster counts, export fields, captain/club-pro display, roster locking, and the existing Captain-or-higher authorization boundary. Assigned leaders are fetched in bounded ID batches so inactive assigned leaders remain displayable without loading unrelated inactive members. Load errors now render an explicit retry state instead of a legitimate-looking zero count. The contextual Team Schedule action remains the existing modal overlay and does not navigate to `/division-schedules`.

Six focused LMS-0748 controls cover the existing Captain/Club Pro/League Manager/Commissioner authorization hierarchy, authorized-scope preservation, cross-scope exclusion, active/inactive hierarchy filtering, listing counts, shallow-query/error handling, and contextual overlay behavior. A final read-only replay of the corrected local hydrator against the current production rows produces 119 hydrated teams, 99 under the unchanged active hierarchy, and all 119 with Include Inactive; no team is missing its Division or League. The complete suite passes 1,351/1,351, lint has zero errors and the same 11 existing warnings, the production build passes, and unauthenticated local browser entry retains the login redirect and reports LMS-0748. No SQL, migration, RLS, permission, role, production business-data write, or deployment was performed by this work.

The final 20-table production comparison kept 18 fingerprints unchanged, including all 119 teams and all 16 team-member rows. Two tables changed during the diagnostic window: `member_season_ratings` gained one row at 17:29:52 UTC (1,421 → 1,422), and existing `members` rows received updates from 17:32:20 through at least 17:39:22 UTC. Every diagnostic issued SELECTs only, and the application correction remained local, so these are concurrent production activity rather than LMS-0748 writes; exact actor attribution is unavailable. The production Teams contextual-overlay gate remains pending because LMS-0748 was intentionally not deployed. The Captain and League Standings overlay gates remain not exercisable with the current production data as previously documented.

## 2026-09-19 — Division Schedules format and leadership display (local)

The owner requested removing the unhelpful Summary/Detail toggle on People & Teams / Division Schedules and checking the same format on the Captain Dashboard. Live LMS-0748 reproduction confirmed that the toggle only rearranged the desktop team list (points/record versus record/points, with optional rank); neither the schedule nor selected-team details changed. The shared TeamScheduleModal now uses the existing rank/name plus record/points format consistently, and its Selected Team header separately labels Captain and Co-Captains with wrapping names. Captain, Player, Teams and Standings contextual schedule overlays share this presentation; match details, BYEs, selection, Close behavior and existing authorization remain intact.

The standalone Commissioner/League Manager page omitted leader relationships from its team data. It now reads the three existing assignment IDs, fetches only assigned members' name fields through the same authenticated Supabase client in bounded batches, and supplies the established shared display shape. It preserves active-team/division filters, preferred-team selection, published-match queries and existing standings/ratings. A leader-name lookup error is visible while the schedule remains available. The Captain Dashboard already supplies these names and requires no loader or authorization changes. No SQL, migration, role/RLS/grant change, production business-data mutation or deployment is included.

Live baseline identity was verified as LMS-0748 / 0.1.571, commit 06c62ab2fd67d17dab113e5d4e8c48b3213728e6, production deployment dpl_5BY65jZgTup9o8zed69ztZDWMhZ4. Thus earlier roadmap local-only labels for LMS-0747/0748 are superseded by observed owner deployment. The retained LMS-0748 artifact is an available rollback candidate; no rollback was executed.

STOP BEFORE DEPLOYMENT: normal-first checks of the unchanged production app twice showed Admin Dashboard / Active Seasons at 0 teams and 0 roster assignments, while Teams & Rosters successfully loaded 100 active teams of 120 total. This pre-existing discrepancy prevents declaring the normal LMS gate passed; it is recorded for separate investigation and is not corrected in this display change. The connected Commissioner's own Captain Dashboard has no active team, so live Captain schedule acceptance could not be exercised in that session. No View-As session or production test rows were created.

Verification: 25/25 focused tests pass, including nine new render/loader controls and the existing navigation, active-team, roster and names-only privacy controls. Final npm run lint passes with zero errors and 11 existing warnings; npm run build and git diff --check pass. Browser verification of the real shared component with isolated synthetic data passes for desktop page and 390px Captain overlay: captain plus two long co-captain names wrap visibly, team changes clear/replace leader names, rank/record/points remain, verified-match expansion shows the existing games/ratings, BYEs remain, overlay Close works, and no browser warnings/errors were captured. The owner confirmed live Captain schedules are expected to be empty until schedules are created; that is an accepted current-data limitation, not a regression. Application version remains LMS-0748 because this change is local and deployment is held at the normal-live baseline gate.

## 2026-09-19 — AI Feedback Drill-Down & Question History / LMS-0749 / 0.1.572 (local only)

The existing AI Feedback & Review page now provides scope-consistent Total Requests, Helpful, Not Helpful and No Feedback cards, with a separate Ambiguous state for tied conflicting official votes. Date/origin/search controls filter a read-only 25-row server-paginated interaction list. Detail shows the complete retained question/effective question/answer, feedback, available recorded context and sanitized existing diagnostics. Existing review queues, Live feedback and Approved Answers remain in place.

Architecture inspection found that normal unvoted and Live request outcomes deliberately lack saved question/answer/user text. This enhancement reuses ai_request_outcomes, ai_answer_feedback_events, ai_review_occurrences and ai_live_private.feedback instead of adding capture. Current member names are joined only through an already-recorded feedback member ID and explicitly labeled current; historical role/page/team labels are not invented. Existing redaction/purge decisions override old feedback payloads. Some historical text was already truncated by capture and cannot be reconstructed.

The additive local migration 20260919105119_ai_feedback_question_history.sql adds only the stable SECURITY INVOKER ai_review_interactions(jsonb) read function. No existing table, index, policy, grant or function changes. The existing server authorization limits both new GET operations to Commissioner/League Manager and preserves View As denial; browser database roles cannot execute the function. Service output uses field allowlists and credential-text omission. POST cannot write through the history operations. A measured cached-plan slowdown at 5k outcomes/1k votes was corrected using force_custom_plan scoped only to this new function; final isolated warm queries were 31–86ms, with no new index justified.

Verification passes: full suite 1,402/1,402, final database controls 11/11, history/review/View As service controls 54/54, lint with zero errors and 11 existing warnings, production build and diff check. Browser verification of actual page/HTTP service/SQL using isolated synthetic identities and records confirms card-to-row matches, full text, search/date/origin, next/previous pagination, Commissioner/League Manager access, unauthorized denial, keyboard dialog behavior and 390px layout. No production deployment, migration application, business-data changes or AI retrieval/generation/feedback-write behavior changes occurred. Detailed schema mapping, files, limitations and evidence: docs/ai-feedback-question-history-local-review.md. This is explicitly local-only per the owner's request; earlier normal-live dashboard discrepancy remains a separate unresolved release consideration.

## 2026-09-19 — LMS-0749 production preflight scope clarification

Authenticated preflight of the owner-deployed LMS-0748 commit efce2939c9a36279a994517cf43ce30a875ee893 resolved the earlier Admin Dashboard count concern. Active Seasons is intentionally based on today's inclusion in the season start/end date window, not just is_active flags. Production 2026 Fall Season starts October 14 and 26/27 Saturday Season starts October 17, so zero Active Seasons teams/rosters on September 19 is expected. All Seasons correctly displays 100 active teams and 16 roster assignments, agreeing with Teams & Rosters (100 of 120 total). The earlier scope discrepancy is not a normal-LMS regression. No application or business-data correction was needed.
## September 20, 2026 — Ask LWR multiple-team retrieval correction, local only

The live read-only diagnosis found active Rule 3.7 searchable and embedded; a concurrent Rules activation added the general player multi-roster permission as Rule 5.1.2 (the supplied Team Structure text had been called Rule 1.1). Original hybrid searches ranked the direct rules below the .35 gate or omitted them, and answerability rescue could select team-registration instructions. A bounded membership-concept retry now reuses the original embedding in the existing Supabase RPC and requires a direct `league_rules` permission passage; six live wording variants select Rule 5.1.2, and four pass live source revalidation. No score weights, threshold, database, corpus, or business rows changed. Final clean full protected tests 1,407/1,407, lint 0 errors/11 existing warnings, build pass. After specific user authorization, four read-only model replays produced the correct qualified answers and Rule 5.1.2 citations; token usage was not retained by the first harness. The concurrent corpus activation triggers FAST FIX release review, and normal LMS production gates remain; no deployment or production acceptance. See [detailed before/after diagnosis](ai-multiple-team-retrieval-correction.md).

## September 20, 2026 — PBCC ten-player two-court balancing, local review

The eight/nine-player whole-night planner excluded ten players, whose fallback produced 4/1 court splits in a six-game batch and three consecutive trio exposures in a seven-game Next Game replay. A local extension plans two byes per round while retaining unique-partner, court-balance and group-spacing priorities. Synthetic six/seven-game batch and next-game outputs have 1–2 byes per player, court differences at most one, unique partners and no consecutive trios/quartets. An edited-history test checks saved partners and immutable inputs. No live PBCC assignment, score, roster, notification or database change; scheduling is outside FAST FIX deployment authorization. See [review and before/after metrics](pbcc-ten-player-two-court-review.md).

## September 20, 2026 — PBCC ten-player manual-bye continuation (local only)

Post-LMS-0751 verification found that Next Game persisted unchecked joined players as byes but passed only checked players into the planner. Two manually selected byes from ten joined players therefore caused eight-player planning for that round, excluding the full ten-player history. A scoped correction passes all ten players and constrains the immediate bye pair; verified saved-prefix plans are reused when compatible, and the saved-history reader now includes past trio/quartet/adjacency/streak totals. Synthetic six/seven-game manual-bye nights, saved reloads, edited rounds, impossible histories, and protected eight/nine/ten scheduling regressions are covered locally. No deployment or production business-data change was made. See [local manual-bye review](pbcc-manual-bye-history-local-review.md).

The pre-release nine-player follow-up found the same eight-player planning gap when one of nine joined players was manually byed. The scoped path now plans with all nine and locks the selected single bye. A valid alternate bye sequence also exposed a 5/2 court split; the existing complete-night design can be reused under a roster rotation when every saved court/bye prefix matches, preserving the bounded edited-history fallback. Eight new nine-player tests and the eleven ten-player tests pass together (19/19), as does the protected 12-case 8/9/10-player scheduling matrix. The full suite passes 1,431/1,431, lint has zero errors and 11 existing warnings, and the production build passes. The owner requested a pre-deployment report; LMS-0751 remains production, with no commit, deployment, or PBCC business-data change.

## September 21, 2026 — PBCC nine/ten-player manual-bye history / LMS-0752 production accepted

Commit `f92bdca7d11053f9922b8dcf371791f062170f88` deployed as LMS-0752 / 0.1.575 in READY Vercel deployment `dpl_Dn5HnVTevoPd6v2PTn1EavbXEog2`. Production-safe Commissioner smoke checks passed for Dashboard, Teams & Rosters, Members, and PBCC Admin. The 19 focused manual-bye cases and protected 12-scenario 8/9/10-player matrix passed again against the deployed commit; the predeployment full suite passed 1,431/1,431, lint passed with zero errors and 11 existing warnings, and the production build passed. All 10 PBCC operational table counts and hashes matched before/after. Live Next Game/manual-bye actions were not exercised because they persist real assignments; synthetic verification against the deployed commit covers those flows. LMS-0751 deployment `dpl_4F3Ch2FMsD18NuosH7gXTP8LzdpF` remains the rollback target. See [production acceptance](pbcc-manual-bye-history-production-acceptance.md).

## September 21, 2026 — PBCC next-round action label / LMS-0753 production accepted

The later-round selection modal now says **Create Next Round** and **Creating Next Round...**, while the initial workflow retains **Start Match**, **Start and Generate First Game**, and **Starting...**. The planner remains unchanged; measured planner-only time is about 2.8–3.0 seconds for nine players and 3.9–4.3 seconds for ten players before network, persistence, and refresh overhead. Commit `507338323a198ca69bedea1bd1d4464fcd3e9750` deployed as LMS-0753 / 0.1.576 in READY deployment `dpl_AjDzPziNLEqW3ycdz89wHPx1KDPK`; LMS-0752 deployment `dpl_Dn5HnVTevoPd6v2PTn1EavbXEog2` is the retained rollback target. Focused coverage passes 1/1, the protected scheduling matrix passes 12/12, lint has zero errors and 11 existing warnings, and the production build passes. Normal-LMS smoke checks passed. A concurrent user completed the September 21 PBCC match and opened its weekly repeat during deployment, followed by two player responses; timestamped activity logs fully explain the changed PBCC fingerprints, and the UI-only release/browser verification performed no write action. Live later-round replay was intentionally omitted because it would persist real assignments. See [production acceptance](pbcc-next-round-label-production-acceptance.md).

## September 21, 2026 — Season Ratings large CSV upload / LMS-0754 production accepted

Season Ratings now accepts one to 5,000 CSV rows and gives both preview and commit RPCs a bounded 30-second client timeout. An idempotent source-control migration represents the already-applied production planner state: a 5,000-row bound and 20-second function timeout, with strict old/desired-state guards. Existing CSV validation, DUPR-only matching, populated-field protection, final-rating protection, signed preview receipts and explicit commit confirmation remain unchanged. Realistic 1,800/5,000-row files remain within the existing 2 MiB CSV, 2.8-million-character receipt and 4 MiB request bounds.

Commit `2d8de88927636f34477d5fe61396bca9a4de3eb3` deployed as LMS-0754 / 0.1.577 in READY Vercel deployment `dpl_36FKQJ5hyceQmwFdEztoVxNVMVXg`; LMS-0753 deployment `dpl_AjDzPziNLEqW3ycdz89wHPx1KDPK` is retained for rollback. Focused tests pass 51/51, the full suite passes 1,437/1,437, lint passes with zero errors and 11 existing warnings, and the production build passes. A signed-in Commissioner previewed 1,800 synthetic nonmatching rows across 36 pages without the old 1,000-row error. No final import action was available or invoked, browser logs were clean, and all five ratings workflow table fingerprints matched before/after exactly. See [production acceptance](season-ratings-large-csv-production-acceptance.md).

## September 21, 2026 — Season Ratings review-only preview filter / LMS-0755 local review

The Season Ratings CSV preview now opens with the existing complete row list and offers a display-only **Show Review / Invalid Only** control. The filtered view uses the existing action classification and includes only `REVIEW` and `INVALID`, excluding ordinary `FILL`, `RECORD`, `SKIP`, and deferred unmatched rows. Pagination is calculated from the filtered list, toggling resets to page 1, and the page reports the filtered and total counts. CSV parsing, matching, counts, receipt signing, protected fields, commit behavior, ratings calculations, database functions and authorization remain unchanged.

Focused ratings tests pass 56/56, including the expected 8-of-1,800 display, filtered pagination, reset behavior and unchanged import receipt/callback controls. The full suite passes 1,443/1,443, lint passes with zero errors and 11 existing warnings, the 84-page production build passes, and the release diff is limited to the preview component, a small pure view helper, tests, version files and review documentation. Predeployment Commissioner smoke checks passed on LMS-0754 for Season Ratings, Teams & Rosters and Members; fresh ratings workflow fingerprints were captured after the recent real import. See [local review](season-ratings-review-filter-local-review.md).

## September 21, 2026 — Season Ratings review-only preview filter / LMS-0755 production accepted

Commit `cab2a8127779fcddc3455dafb762f4479aa5f457` deployed as LMS-0755 / 0.1.578 in READY Vercel deployment `dpl_GSqHMdNV4qfudbin18nTY77K2sGK`; LMS-0754 deployment `dpl_36FKQJ5hyceQmwFdEztoVxNVMVXg` is retained for rollback. Normal Commissioner checks passed for Members, Teams & Rosters and Season Ratings. A deterministic 1,800-row production preview produced 8 ambiguous `REVIEW` rows and 1,792 deferred unmatched rows. The default view showed all 1,800 rows across 36 pages; the review-only view showed exactly the 8 review rows on one page with reasons intact and reset correctly from page 2. No import action was available or invoked, browser logs were clean, Vercel reported no runtime errors, and all five ratings workflow table fingerprints matched exactly before and after. The owner's actual CSV was unavailable, so production acceptance used the controlled file and records that limitation. See [production acceptance](season-ratings-review-filter-production-acceptance.md).

## September 22, 2026 — Controlled PBCC VAPID rotation / LMS-0756 local review

LMS-0756 / 0.1.579 adds a server-derived, non-secret public-key identifier to new App Notification subscriptions, filters push delivery to the current VAPID generation, and migrates an existing browser subscription only when its actual application-server-key bytes differ from the current public key. Null and stale subscriptions are not deleted or disabled by the migration and naturally retain SMS fallback; browsers without a subscription are not automatically opted in. The additive nullable-column migration has no backfill or business-data write, and no index is justified for the six-row starting subscription set.

Focused tests pass 13/13, the full suite passes 1,456/1,456, lint passes with zero errors and the same 11 existing warnings, and the 84-page Production build passes. READY Preview `dpl_7mX4yrekaNp1Yt44Qz5FfCzPoR5N` visibly reports LMS-0756 with a clean browser console; its notification endpoint is fail-closed because Preview has no private VAPID key or Production service-role credential. Production remains unchanged and READY on LMS-0755 deployment `dpl_GSqHMdNV4qfudbin18nTY77K2sGK`. Work is stopped at the owner credential-entry gate: the Production database migration is unapplied, no VAPID key or Vercel environment changed, no Production deployment occurred, no device/subscription or business data changed, and no notification was sent. See [local review and credential gate](pbcc-vapid-rotation-local-review.md).

## September 22, 2026 — Controlled PBCC VAPID rotation / LMS-0756 production accepted

After the owner installed the matched VAPID pair with the private key restricted to Sensitive Production scope and matching public variables in Production plus Preview, metadata-only verification passed without retrieving any value. Additive migration `20260922131117_app_notification_vapid_key_id.sql` was applied once and recorded remotely as `20260922160002 app_notification_vapid_key_id`; it added only a nullable text column with no backfill or subscription/business-data mutation. Commit `755177c56acaa6244eb650c22db9dd1894da1798` deployed normally from GitHub as READY Production deployment `dpl_AVnoqaaCjucKNTgKmUsKFqKUG3Bd`, serving LMS-0756 / 0.1.579 on both Production aliases.

The Production public-key endpoint reports configured without exposing private-key data. The first natural PBCC reminder request after cutover reached the new deployment at 16:15 UTC and returned HTTP 200; Match Setup reminders retained an all-200 24-hour status sample, and Vercel reported no runtime-error clusters. Subscription counts remained 17 total, 6 enabled across 5 players, with all 17 intentionally null-generation and therefore excluded from new-key push delivery while SMS fallback remains available. All 29 protected LMS/PBCC counts and whole-row fingerprints matched across final before/after snapshots. No notification was sent and no business data changed. No authorized signed-in subscribed device was available, so stale-device unsubscribe/resubscribe acceptance remains pending rather than manufactured. See [Production acceptance](pbcc-vapid-rotation-production-acceptance.md).

## September 22, 2026 — LMS-0757 Season Ratings identity and Member Administration fixes, local review

The Production ratings Upload planner currently treats one active member plus an inactive historical DUPR duplicate as ambiguous and expands one CSV row into multiple preview rows. LMS-0757 resolves exactly one candidate per CSV row: one active match wins over inactive history; multiple active or inactive-only matches and repeated CSV IDs remain review cases. Transfer uniqueness, source provenance, signed receipts, field protection, Clean Ratings, and the 5,000-row limit remain unchanged. The member directory Current Rosters filter now requires active team membership on an active team, while retaining leadership relationships in team detail. A Duplicate DUPR IDs review shows active and inactive partners, and Member Administration gains Copy Email plus shorter Eastern Last Login display.

The function-only migration is state-guarded and reruns cleanly in isolated PostgreSQL tests. Fourteen focused/protected tests pass, the full suite passes 1,461/1,461, lint has zero errors and 11 existing warnings, and the 84-page Production build passes. Read-only Production baseline shows 14 duplicate groups (11 one-active-plus-inactive, 3 multiple-active), 14 distinct current-roster members, and no change to the 17 VAPID subscriptions. See [local review](season-ratings-member-admin-fixes-local-review.md).

The owner restored a Commissioner session, allowing normal LMS preflight before migration and normal LMS regression again after deployment. Function-only migration `20260922204254_lms0757_ratings_identity_member_directory.sql` applied without changing the 34 protected table fingerprints. Commit `42a3dedf87d597da09d336ad0e872e01124744a6` deployed through GitHub/Vercel as READY Production `dpl_yTMcqhqv8tvJPJBgvZeBYViuitJZ`, with LMS-0756 retained for rollback. A four-row ratings CSV preview produced one active historical-duplicate match, one multiple-active REVIEW, and two repeated-CSV REVIEW rows; the review-only filter displayed three rows. No import was committed. Member Administration showed the exact 14-member active-roster set and all 28 duplicate-DUPR records; Copy Email, short Eastern Last Login, search, sort, pagination, and read-only detail tools passed. PBCC reminders returned HTTP 200, Match Setup Production sample was 41/41 HTTP 200, and no Production runtime-error cluster appeared. The owner confirmed a concurrent expected update of 78 member records; ratings, rosters, teams, and VAPID subscriptions remained unchanged, and final 34-table fingerprints stabilized. See [Production acceptance](season-ratings-member-admin-fixes-production-acceptance.md).
